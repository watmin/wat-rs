# ORACLE for hoist-where-into-condition — HISTORY, never the tool.
#
# The substantive hoist (the `where`s' predicates moving into the `:wat::grep::Node` condition) is
# grok's own #630 (feb5fae91), copied verbatim from wat-scripts/grep/bare-variant-constructors.wat
# (the `:bv::head` rule — a genuine JOIN case, 2 ordinary conditions, so #630's join-only gate
# does not exclude it and this fixture stays correct at every step from #627 through the tip).
# Updated here at #628: `backward-trim`'s own refinement lands, so the fixture's expected output
# now matches grok's real post-image line-for-line (the closing bracket merges onto the preceding
# condition's own line) — no whitespace-artifact spec entry is needed any longer.

history feb5fae91 wat-scripts/grep/bare-variant-constructors.wat
