# ORACLE for namespace-bare-top-level-names — HISTORY or header spec, never the tool.

# spec: no readable history form this codemod alone changed (see SCORE).
spec (:wat::core::defn :wsh::arith [] -> :wat::core::i64 1) :: every remaining bare top-level definitional name
