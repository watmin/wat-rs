# ORACLE for to-faithful-clojure — HISTORY or header spec, never the tool.

# spec: no readable history form this codemod alone changed (see SCORE).
spec (wat.core/defn u/hit [x :- wat.type/i64] :- wat.type/nil :: (:wat::core::if … → (wat.core/if …)
spec   (wat.core/if x :: (:wat::core::if … → (wat.core/if …)
spec     wat.type/Tuple(i64) :: (:wat::core::if … → (wat.core/if …)

spec-before (:wat::core::defn :u::hit [x <- :wat::core::i64] -> :wat::core::nil :: (:wat::core::if … → (wat.core/if …)
spec-before   (:wat::core::if x :: (:wat::core::if … → (wat.core/if …)
spec-before     :wat::core::Tuple(i64) :: (:wat::core::if … → (wat.core/if …)
