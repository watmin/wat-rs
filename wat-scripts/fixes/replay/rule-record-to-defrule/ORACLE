# ORACLE for rule-record-to-defrule — HISTORY or header spec, never the tool.
# Whole where-grid files are unreadable (PersistentVector<…>); this form is the
# readable top-level rule-arith the landing rewrote ONLY by this tool.

history 5b86828f8 wat-scripts/perf/grid/where-shapes.wat
