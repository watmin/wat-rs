#wat.type/ImpureFieldInPureAggregate {
  :message "containment rule (arc 293.W): pure aggregate \":w::Bad\" may only hold pure fields — field \"c\" has impure (struct) type \":w::Conn\". A struct cannot be reconstructed from EDN bytes across a comms boundary; a record or holon holding a struct field could never cross — it must not exist."
  :location #wat.core/Span {
    :file "tests/types/probe_arc293_W_containment.wat"
    :line 15
    :col 1
    :end #wat.core/Option.Some {
      :value #wat.core/Pos {
        :line 15
        :col 73
      }
    }
  }
  :causes []
  :aggregate ":w::Bad"
  :field "c"
  :field-ty ":w::Conn"
}
