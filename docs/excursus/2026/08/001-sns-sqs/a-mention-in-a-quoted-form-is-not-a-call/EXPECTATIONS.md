# EXPECTATIONS — a mention in a quoted form is not a call

⛔ **Row 1 gates the change.**

| # | what | how it is judged |
|---|---|---|
| 1 | ⭑⭑ **Arc 198's purpose, in its own words, and a corpus census** | what was the widening defending against? Does any site rely on a non-call mention firing? **If yes, STOP.** Form-aware census, never text. |
| 2 | ⭑⭑ **The witness dies** | `{:restricted-to [:my::]}` on `:user::main` → `"hi"`, exit 0. And the second witness (`:user::spawn::service-locus`, 10 bodies). |
| 3 | ⭑⭑ **A REAL violation still fires** | a genuine restricted call from a disallowed namespace. Driven, not argued. |
| 4 | ⭑⭑ **An UNQUOTED mention inside a quasiquote still fires** | `~(:name)` is evaluated now — 198's hole must stay shut. Driven. This is the boundary the fix most likely gets wrong. |
| 5 | ⭑ **The stale comment at `check.rs:722` is corrected** | it says "call head"; the code means something else. Whatever the rule becomes, the comment must match it. |
| 6 | ⭑⭑ **Floor** | Summary verbatim, `.floor/<stamp>/`, `ARM.txt` or not. |
| 7 | clippy + `--no-run` | **read the number from the output.** |
| 8 | ⭑ **Circuit byte-identical** | `distinct=8000;dup=0`, `timeout=yes;…;retry-on=fresh`. |
| 9 | ⭑ **Is the eighth door closed?** | report whether `CheckEnv::get_binding_metadata` can still be reached for a stdlib body by any user write. Do NOT re-attempt Tier B. |
| 10 | ⛔ **Out-of-scope items untouched** | the blame diagnostic, the rendezvous message, Tier B, `runtime.rs:2112`, `check.rs:6068`, `env.rs:456`, `.config/nextest.toml`. |
| 11 | ⭑ **What you could not settle** | honestly, with what would settle it. |
