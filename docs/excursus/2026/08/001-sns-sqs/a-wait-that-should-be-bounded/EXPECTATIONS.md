# EXPECTATIONS — a wait that SHOULD be bounded (the classification)

Written **before** the strike. Graded on the orchestrator's **own** reads of the cited sites.

⚠ **Executor: a spawned Opus subagent** (grok's credits are exhausted for two days). Named per
examinare — a brief that implies one tier and runs another lies about what ran. **Opus and not Sonnet
deliberately:** mis-sorting a PARK as a BOUND would break a struck feature, so accuracy outranks cost
here.

Baseline: `recv 0/259` corpus-wide; the orchestrator's hand count of **live** sites is **24**, derived
from census rows and therefore suspect (four rows shared one enclosing-function name).

| # | what | expected |
|---|---|---|
| 1 | ⭑⭑ **all three controls stated with verdicts** | `await-timer-ms` = TIMER · `park-receive!` = PARK · `child-main` = **absent from the bare-recv list** (it now calls `recv-by-deadline`). A control misclassifying voids the run |
| 2 | ⭑⭑ **every row cites a `file:line` that was OPENED** | not a census row trusted. The orchestrator will re-read a sample |
| 3 | ⭑ **the live site count is RE-DERIVED from code** | stated, and said to differ from 24 if it does. The code wins |
| 4 | ⭑ **the BOUND list is restated on its own** | that list is the deliverable; totals must add up |
| 5 | ⛔ **UNKNOWNs are their own class** | with what defeated the classifier. Never folded |
| 6 | ⛔ **nothing was edited** | `git status --porcelain` shows only the FINDING file |
| 7 | **each row says what the site is WAITING FOR** | one phrase from the code — the evidence for its class |
| 8 | **the non-live count is reported** | a number only; tests/probes not classified |
| 9 | scope stated | a classification was produced. **No wait was bounded** |

## Runtime prediction

**30–60 minutes.** ~24 sites to open and reason about; no build, no floor, no code change.

## Trap-door risks, ranked

1. ⭑⭑ **A PARK sorted as BOUND.** It would put a struck feature (`queue-long-poll`) on a later stone's
   chopping block. STOP-2 makes UNKNOWN the safe answer, and UNKNOWN costs nothing.
2. **Trusting the census rows.** They are pointers. Four shared one function name, so the 24 may be wrong
   in either direction — and this session's most repeated failure is exactly quoting a structure after
   reading one token of it.
3. **`recv-by-deadline` caught by a loose pattern** — it is already bounded and would inflate the list.
4. **`child-main` appearing at all.** It was fixed hours ago; its presence means stale code was read.
