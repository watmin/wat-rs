# EXPECTATIONS — a send that should be bounded (classification, REPORT-ONLY)

| # | what | how it is judged |
|---|---|---|
| 1 | ⭑⭑ **Instrument disclosure comes FIRST** | before any table, state what your instrument sees and what it cannot — the recv twin's §0 is the model, and it is the row most likely to save the stone. |
| 2 | ⭑⭑ **Every one of the 32 has a row** | `file:line`, enclosing form with ITS line, what the send is waiting to hand over, class, and the reason. Every row from a site **opened and read**, never inferred from a name. |
| 3 | ⭑⭑ **Totals reconcile** | BOUND + PARK + REPLY + UNKNOWN = the live total, and that total **re-derived by you**, not quoted from the DESIGN. If it is not 32, say so and show the delta. |
| 4 | ⭑⭑ **Three controls, predicted then read** | name 3 sites whose class you predict BEFORE reading, then report agreement or disagreement. A disagreement is the most valuable line in the SCORE. |
| 5 | ⭑ **`wat/spawn.wat:527` and `:597` are classified explicitly** | the send halves of the handshake whose recv halves `ab419aaa3` bounded. Say whether leaving them unbounded is half a fence. |
| 6 | ⭑ **Helper-consumed sends are followed, not dropped** | `:user::send-ok!`, `send-keep-serving?`, and any other site whose `SendOutcome` is a parameter. Piece A was drawn for 4 blind sites and found 3 because of exactly this. |
| 7 | ⛔ **Nothing is bounded, nothing is fixed** | `git status` shows no `.wat` and no `.rs` change — only the finding document. |
| 8 | ⭑ **REPLY sites are called out as a protocol question** | any site where a bound would change what a caller receives, not merely when. That is a different decision from a timeout. |
| 9 | ⭑ **UNKNOWN is permitted and expected** | a class you cannot settle is a finding. Do not force a site into BOUND to make the table tidy. |
| 10 | floor + clippy | report-only, so both should be untouched; confirm rather than assume. |
| 11 | ⭑ **What the ruling now needs** | one paragraph: given the counts, is the 215-arm variant worth it, is the retry loop enough, or does the split suggest something else? A recommendation, explicitly NOT a decision. |
