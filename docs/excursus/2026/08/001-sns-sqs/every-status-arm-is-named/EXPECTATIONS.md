# EXPECTATIONS — every Status arm is named

Report every row with what you actually observed. A row you could not drive is reported ABSENT, not
assumed — `[[feedback_permit_the_null_and_it_gets_used]]`.

| # | what | how it is judged |
|---|---|---|
| 1 | ⭑⭑ **All 9 Status sites name all six variants; zero wildcards remain** | the balanced reader (comment-stripped, arms at depth 1) reports **0** wildcarded Status matches in `wat/service.wat`. Report the count before AND after. |
| 2 | ⭑⭑ **A second `Faulted` no longer raises** | the 4 second-recv sites return `StopOutcome::GaveUp` / `GateOutcome::GaveUp`. Quote the new arm for `stop` verbatim. |
| 3 | ⭑ **Every remaining raise names the variant that arrived** | no two arms in one match may share a message. Quote one. |
| 4 | ⛔ **The success path is byte-identical** | `git diff -U1` shows no change inside any success arm or inside a first-recv `Faulted` drain. |
| 5 | ⭑ **The three per-surface sites are untouched and now MARKED** | `:2759`, `:2940`, `:3040` keep their wildcard and gain a comment saying it is deliberate and why. |
| 6 | ⭑⭑ **Floor** | `./scripts/floor.sh`, the **Summary line** verbatim, plus the `.floor/` path and whether an `ARM.txt` exists. Every service in the corpus is generated by this macro. |
| 7 | happy path | `printf '#fanout/Input {…}'` one-liner from BREADCRUMB → `distinct=8000;dup=0` and the `timeout=…;retry-on=fresh` line unchanged. |
| 8 | clippy + tests compile | `cargo clippy --release --workspace --all-targets`; `cargo nextest run --release --no-run`. |
| 9 | ⭑ **A DRIVEN second fault, or an honest ABSENT** | try to make a service emit two `Status::Faulted` before its `Stopped` and show the owner receiving `GaveUp` instead of dying. If you cannot construct it, say so and say what you tried — **do not report a fixture you did not run**. |
| 10 | blast radius | `git status --short`; no `src/`. |
