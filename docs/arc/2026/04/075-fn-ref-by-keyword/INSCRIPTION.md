# Arc 075 — RETIRED — INSCRIPTION

**Status:** retired 2026-04-28. This arc was withdrawn, not
shipped — the diagnosis was wrong-shape. See [CLOSURE.md](CLOSURE.md)
for the full closure justification.

The user's surface complaint was real (passing a function as
`:fn(:f64)->:bool` felt like substrate friction in arc 074
slice 1's inline-lambda workaround). But the answer turned out
to be elsewhere: the substrate ships canonical filter funcs
(`filter-coincident`, `filter-present`, `filter-accept-any`) so
users compose by passing them by name. That work shipped as a
slice-1-followup commit on arc 074, not as its own arc.

The DESIGN.md remains in this folder for archaeology — it
records what was *initially proposed*. CLOSURE.md records what
the substrate session decided after the diagnosis was reframed.
This INSCRIPTION exists to note "no implementation arc shipped
under this number" so the seal-status survey reflects current
reality.

**PERSEVERARE.**
