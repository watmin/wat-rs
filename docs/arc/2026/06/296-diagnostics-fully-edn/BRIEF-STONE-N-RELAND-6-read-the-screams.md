# BRIEF — STONE N RELAND 6: read the screams, and separate the fixtures that must stay wrong

> ⛔ **THE ORCHESTRATOR HAS BEEN GUESSING AT A LIST THE TOOL ALREADY PRINTS. That stops here.**

## WHAT RELAND 5 ACTUALLY SHOWED

```
2773 -> 473 -> 291 -> 128 -> 69 -> 50 -> 47
```

The full re-sweep was RIGHT and NECESSARY — 27 files held unmigrated sites in classes nobody had
named (`enums_tagged_variant.wat`, twenty `EchoResponse::Ok` probes, four `RunResponse::Ok`,
`InstallResponse`, unit variants needing `{}`). Pass 2 changed 0 bytes.

**But it closed 3 tests, not 47.** So the sweep was not the cause of the remainder, which is what I
predicted it would be. And the brief's named proof file was a FALSE POSITIVE from my own regex:
`enums_mixed_unit_tagged.wat:11` was already `(:my::Event::Open {:size 7.5})`; my pattern
`\(:my::Event::[A-Za-z]+[^{)]` consumed the SPACE before the map. **Right conclusion, wrong
evidence** — the twelfth unvalidated pattern of this campaign.

## THE WORK — ASK THE INSTRUMENT, DO NOT INFER

**1. Emit the scream list.** Run the sweep once more with every
`[positional-ctor] UNRESOLVED <head> <file>:<line>` captured to a file. **Commit that file.** It is
the worklist, it is exact, and nobody should ever again reason about the remainder without it.

**2. Classify every scream into exactly one of:**

```
(a) THE TOOL CANNOT SEE THE DECLARATION   defenum inside let / do / defmacro body / generated by
                                          defservice-defsurface. Hand-edit is authorised; say WHY
                                          the tool declined, per site.
(b) THE FIXTURE MUST STAY WRONG           a NEGATIVE fixture whose job is to hold a malformed form
                                          so a test can assert the rejection. ⛔ DO NOT MIGRATE.
                                          Its TEST may need its expectation updated instead.
(c) CENSUS BAIT                           retired tokens kept so another tool can match them.
                                          Already skipped; confirm the skip covers it.
(d) A REAL SITE THE TOOL SHOULD HAVE TAKEN   -> that is a TOOL finding, not a hand-edit.
```

⚠ **Class (b) is named because two are already visible by filename** —
`tests/types/enums_unit_pattern_on_tagged.wat` and `tests/types/enums_tagged_arity_mismatch.wat`
exist to be REFUSED. `map pattern has 1 key(s), variant ':my::Event::Pair' declares …` is one of
them talking. For these the question is never "migrate it" — it is **"is the error it now gets the
RIGHT error for what this test measures?"** If yes, update the expectation and say what changed. If
no, the wall is over-firing and that is a finding.

**3. `wat/spawn.wat` carries 14 sites and is STDLIB, not a test.** It is in the worklist. Either the
tool declined it (then it screams, and it is class (a) or (d)) or it was skipped. Say which.

**4. Then the floor.**

## STOP TRIGGERS

- **STOP-1 — a site is hand-edited without appearing in the committed scream list.** If the tool did
  not decline it, the tool should have taken it: that is class (d), a TOOL finding.
- **STOP-2 — a negative fixture is migrated to make a test pass.** It exists to be wrong.
- **STOP-3 — an expectation is updated without asking whether the new error is CORRECT.** The wall
  firing where it should not is the defect this class hides.
- **STOP-4 — the remainder is characterised by grep instead of by the scream list.** That is the
  orchestrator's repeated error and it is now a STOP.
- **STOP-5 — the wall is weakened.**
