# EXPECTATIONS 0a — written BEFORE the strike, so the result cannot move them

Base: `replay/grok-rete` @ `0388b8468` (floor 5373/5373, clippy 0). The 11 = the grep-rules codemods
named in the brief. The 5 = the ones `0b5742cc7` rewrote.

| # | what | command | expected |
|---|---|---|---|
| E1 | builds | `cargo build --release`, mtime of `target/release/wat` moved | rc 0 |
| E2 | `Written` carries the spelling | `grep -n 'text <- :wat::core::String' wat/grep.wat`; `grep -n ':text (:wat::core::ast-name node)' wat/grep.wat` | 1 hit each |
| E3 | the tracked probe still answers | `printf '["<abs>/headarg.wat"]\n' \| ./target/release/wat --grep wat-scripts/scratch-pad/probe-written-admits-fqdn-keyword-tokens.wat` (target = the brief's 2 lines) | written-string-verb 2 · span-string-verb 2 · written-fqdn 0 |
| E4 | the 11 moved | per file: `grep -c '(:wat::grep::Named'`, `grep -c '(:wat::grep::Span'`, `grep -c '(:wat::grep::Written'` | 0 · 0 · rules-in-file; the Written sum is **40** |
| E5 | literals restored | for each of the 45 pairs: folded-key count, verbatim-value count, in its file | 0 · ≥1 |
| E6 | the original failure is fixed | the brief's 2-line pre-image through the converted `rename-core-string-to-string` | both tokens renamed; a 2nd run changes nothing |
| E7 | codemods A and B are idempotent | re-run A over the 11 and B over the 5 | `git diff --stat` unchanged by the 2nd run |
| E8 | the gate passes | `cargo nextest run --release --test cli -E 'test(every_recorded_migration_replays)'` | all pass; fixtures run = **13**; ledger = **83** named; stems = **96** = 13 + 83 + 0 runes |
| E9 | the gate FAILS, ×4 (reverted after) | the brief's step 6 (a)–(d) | each RED, naming the offender; verbatim text in the SCORE |
| E10 | every rule is exercised | the SCORE maps each of the 40 rules to the fixture line that fires it | 40/40 |
| E11 | oracle provenance | the SCORE gives, per fixture, the commit + file its `after.post` came from (or "spec" for A/B) | 13/13 cited; none generated by the tool |
| E12 | floor | `scripts/floor.sh`, then read the Summary | 5373 + the new tests, **0 failed** |
| E13 | clippy | `cargo clippy --release --all-targets` | 0 warnings, 0 errors |
| E14 | blast radius | `git status --porcelain` | only the brief's files; no `src/` |

**Runtime prediction:** 3–6 hours. Most of it is choosing history-derived forms that exercise 40
rules, and writing codemod A's span-faithful line deletion.

**Trap doors, named in advance:**
- **The oracle temptation.** Running the codemod to produce `after.post` passes E8 and proves nothing.
  E11 exists to catch it.
- **A fixture form the reader cannot parse.** `facts-of` then reports `Unreadable` and the rules see
  nothing, so the fixture goes red for the wrong reason. Choose forms that today's reader parses.
- **Scope leaks.** Codemod A run over anything but the 11, or B over anything but the 5, including
  over themselves.
- **Comment prose.** The 11 headers' sentences about `Named` must be corrected, or the files lie
  about their own mechanism.
- **Shard runtime.** `positional-ctor-to-map` stays in the ledger in 0a, but the shards must already
  tolerate ~10 s fixtures.
