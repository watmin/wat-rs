# Stone 245.3d — `wat/stream.wat` DEFERRED (not warded)

**2026-06-04. stream.wat is NOT warded — deferred to the lazy-seqs arc.**

## The decision

`wat/stream.wat` (609 LOC, 34 thread-based combinators: map/filter/fold/collect/chunks/window/take/flat-map/with-state/…) is **slated for deletion** — builder direction: *"stream is gonna get killed when we work on lazy-seqs."* It is the eager, thread-per-stage implementation that the lazy-seq rework replaces (cf. `wat/list.wat`'s own header: arc-109's wind-down "move things to `:wat::list::*` then mirror for lazy seqs").

**Four-questions → DEFER:**
- **Ward it now FAILS Honest** on two counts: (1) warding a file slated for deletion is wasted work and a stamp attesting a ward on a corpse — DESIGN §5's "do not ward a file you are about to rewrite," extended to *about to delete*; (2) its `deftest-green` is **structurally unsatisfiable** — stream's forms are exercised only by `wat-tests/stream.wat`, which is **RED (0 passed / 13 failed)**, and the redness is the **arc-170 concurrency/thread-leak** category the green-gate deliberately excludes (`green-gate.sh:22-29`). Not fixable in 245; we are *on* the arc-170 deadlock-state branch.
- **Defer PASSES** all four: honest (don't ward the doomed), obvious, simple, good UX (the stamp set stays truthful).

## Consequences

- stream.wat carries **no `vigilatum` stamp**; this is correct, not a gap. An unwarded file is a defect *only* when it is a live, keepable member of the warded family (arc 244 doctrine). A file scheduled for deletion is not — it is deferred-by-design, recorded here.
- Its RED tests (`wat-tests/stream.wat`, the arc-170 leaky concurrency set) are **left alone** — they die with stream at the lazy-seq rework. Fixing them now would be polishing a corpse.
- **Re-evaluate at the lazy-seqs arc:** when stream.wat is killed/replaced, the replacement (lazy seqs) becomes a ward target under whatever the post-170 gate permits (the integration RUN may be gate-able by then if arc 170 has closed the leaks).

## 245.3 closes here

The remaining-core-stdlib slice: **list ✓, core ✓, edn ✓, Record ✓ warded; runtime retired; stream deferred (this doc).** Next in the corpus march: 245.4 (`holon/*` family) → 245.5 (`kernel/*`, +secare) → 245.6 (wat-tests, selective) → 245.N INSCRIPTION. (The arc-241 struct-rot class — `struct-to-form.wat` + counter-service fixtures still on retired `:wat::core::struct` — remains banked for those surfaces.)
