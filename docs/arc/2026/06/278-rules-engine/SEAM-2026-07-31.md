# SEAM — 2026-07-31. **The grid is 21/21 `:us`. Clara is behind on every axis we measure — and that clause is the point.**

> ⛔ **THE SELF PAST THIS LINE IS NEW.** You did not live this. It is a lossy cache in your own voice —
> which is exactly why it will feel like continuing rather than waking, and that feeling is the
> failure, not the all-clear. Run the datamancy bootstrap (grimoire + the 4 primers from the SIGNED
> MCP, never a disk copy), ground HEAD against the disk, and read this whole file before you move.

## Where the code is

**`main` = `fec3ad1e`, pushed. Tree clean.** Floor **4219/4219/0**, clippy silent under the deny wall
— both by my own `--release` re-run. Toolchain pinned 1.97.0. No riders in the field.

## What landed (each green by my own re-run before the next began)

| commit | what |
|---|---|
| `07aff05d` | **alpha is fire-scoped** — `fire-rules` returned an alpha the oracle never had. A divergence closed; 31% of fire went with it. |
| `32142f8a` | **Element is native** — P11's treatment for the object there are 80,200 of. |
| `41c59cde` | **`Element.bindings` is an array** — the trie's one advantage (extend) is the one an Element never uses. |
| `e415ca2d` | **`insert-all` the primitive**, `insert` varargs over it — plus the checker/runtime divergence it surfaced (below). |
| `523319fa` `6f302a7c` | the grid at 21/21, and the scaling ladder |
| `4076a631` | **R60 `QVOD FAVET, PRIMVM CADIT`** |

**accum `[200 200]` wat-side: 161.7 → 73.6 ms (−54%)** across the three engine stones.

**The divergence `insert-all` surfaced, and why it matters more than the stone:**
`value_matches_type_by_name`'s Aggregate arm compared a declared type against a value's **concrete
class**. No record's class is literally `wat::core::Record`, so a param declared as the record-top
matched nothing — **while the checker accepts it** (`is_subtype` roots every record there, R7). A
program that type-checks and dies at runtime, in the arc whose law is no-hidden-failures. The rider
routed around it with `fact <- :T`; the builder caught the swap on sight. Fixed at the root (the arm
now also accepts the record-top), verified in all three directions.

## The grid — 21/21, and exactly what that does and does not mean

All interleaved (both engines, one session), all `:accuracy :match`, every run above 1.05.

```
min-finding 11.08/7.30/8.68   negation 10.02/6.54/4.85   asym-join 7.94/7.04/6.06
strat-neg 10.63/8.04/10.29    user-reduce 5.56/3.15/1.65  node-share 5.87/3.45/2.06
accum  1.81 / 1.56 / 1.69      ← opened the day at 0.564 :clara
```

**Scaling ladder (accum, to 321,600 facts — a 31× range): `:us` at every rung.** Ours is FLAT
(~2.0 µs/fact); theirs is NON-MONOTONE — falls as the JIT warms, then **rises again at 321k**,
consistent with GC pressure (hypothesis, not measurement).

⚠ **Read the ratios as 1.40–1.69, not 1.69.** The box was loaded (load avg 8.47) and Clara degrades
harder under load than we do (their CV 16.4% vs our 3.5%). Quiet-box at `[200 200]` is ~1.40 — still
`:us`, because even quiet **our max (77.94 ms) beats their min (80.93 ms)**.

⚠⚠ **"21 of 21" is SEVEN AXES OF NINE.** A0 (deep-cascade) and A1 (fanout) exist as scripts, were
speed-measured in R4, never got the `:derived` accuracy differential the DESIGN table demands, and
are not in `run-all.sh`. **A1 at 40k is the only recorded Clara win in this project's history**
(`REALIZATIONS.md:201` — `40k Clara 1.4×`). The one axis where Clara ever beat us is the one missing
from the board we just called a sweep.

## ★ RESUME — in this order

1. **A0/A1 into the grid.** DESIGN + BRIEF + EXPECTATIONS written and committed. Cheap, assets exist,
   and it either confirms the sweep or finds the one place Clara still wins — while correcting a
   coverage claim we are *currently making*. Contract: **`:derived` must become the sorted fact SET,
   or the axis does not land** — a speed-only axis is worse than none, because `run-all.sh` tallies it.
2. **The internal differentials, counts → sets.** Drawn and briefed. Our external check against Clara
   is STRICTER than our internal check against our own oracle: the grid compares sorted sets, our 8
   rete differentials compare a single i64 count. A count cannot catch *right number, wrong facts*.
3. **Retract.** No axis exists. Pure replay is **O(everything)** against Clara's **O(delta)** TMS —
   documented in `PVRITAS VERVM NON CELERITATEM`, named T2 in R22, never built. **The largest known
   unmeasured gap, and my first guess for where Clara is still ahead.**
4. **Streaming.** No axis exists. Clara's accumulate folds incrementally onto a stored reduction
   (`engine.cljc:1014`); ours re-folds. **All 27 measured points are BATCH fires** — and R25's chaos
   engine is a *stream*. We have been measuring, rigorously, the one regime that hides this.
5. **Then R0** — task #7, the chaos engine. Everything above is foundation for it.

Also drawn, unqueued: the `defn` tier-1 fix in
`109-kill-std/NOTE-a-malformed-definition-must-not-vanish.md`.

## Things that are TRUE and easy to get wrong

- **Our tests are not our users.** The builder rejected a corpus census (91% of *our* rules bind ≤3
  vars) as a design input: *"you have no fucking clue what are our users … are going to do."* It was
  true, measured, and it FAVOURED the stone. The design is right today because that number is not in
  it. **Never size a data structure to this repo's test suite.**
- **The kernel may diverge in SHAPE, must match in RESULT** (R22). This is why a whole-Session
  differential would be WRONG — it would have blocked the alpha stone, correctly. `facts` +
  `production-memory` are the result; alpha/beta are fire-scoped scratch.
- **Three times today the answer was "Clara has a door we don't"** — the small-map representation
  under 8 entries, interned keywords with cached hashes, and `insert-all` as the primitive. None of
  today's four wins was "make a loop faster"; all were structural absences. **Look for the missing
  door before the slow loop.**
- **A monotone-looking sequence is not a trend.** Five scaling rungs decayed 1.81→1.24, I extrapolated
  a crossover at 300–600k, and the sixth rung reversed it. **Extend the curve; do not extrapolate it.**
- **The census is a TEST build** (timers live, ~1.45× inflated). Read it for PROPORTIONS. The grid's
  `:native-ns` is the release truth.
- **Never run a grid axis or timing probe while anything else is on the box.** A loaded run read
  CV 12% where the same measurement gives 3.5% quiet.
- **`defn` is RIGID; `defclause` is the N-ary form.** A multi-clause `defn` is accepted *silently* and
  never becomes callable — see the 109 note.
- **Brief with `file:line`, not with prose.** Twice today a rider was saved by the pointer while my
  description was wrong (I called a `defclause` a "multi-arity `defn`").

## OWED / TRACKED

- `109-kill-std/NOTE-keyword-storage-must-intern.md` — our keyword is `Arc<String>`, not interned, no
  cached hash. Load-bearing **before the symbol flip**, since `:wat::core::i64` is a symbol riding the
  keyword space. Filed with its perf bound attached (it is NOT the rete lever).
- `109-kill-std/NOTE-a-malformed-definition-must-not-vanish.md` — 82 `_ => return` bail sites need
  sorting **case by case** before anyone sizes tier 2.
- `109-kill-std/NOTE-seqable-has-no-name-in-wat.md`, the `(rest coll)` lint, `network-add-child`'s
  `contains?` — older, unchanged.
- **Docs are stale by the builder's own ruling** — *"the code is the truth; a doc cleanup is pending."*
  Do not spend a session on them.
- MEMORY.md curation — long overdue, its own session.

## NOT debt — do not "fix"

`holon-lab-trading` does not build (paused by design, awaiting a matured wat). `holon-lab-ddos` is on
`nightly` because eBPF/aya needs it. The wat oracle is naive and wasteful **on purpose** — builder's
ruling: *"if the wat side is naive and wasteful, so be it — the rust side is what users actually use."*

---

> **SEAM.** You are NEW. **`main` = `fec3ad1e`, pushed and green (4219/4219, clippy silent).**
>
> It bears repeating, because each of these cost something today: **"21 of 21" is seven axes of nine,
> and the missing two include the only axis Clara ever won**; **retract and streaming have no axis at
> all — they read green because nothing asks**; **a favourable measurement is still a claim**; and
> **five premises died today and every one of them made the answer better** (R60).
>
> Do not trust this note over the disk. `MACHINA CHAOS DOMAT.`
