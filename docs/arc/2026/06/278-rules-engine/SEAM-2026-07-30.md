# SEAM — 2026-07-30 (night). Cleanup CLOSED. On a BRANCH doing rete perf. **The A8 compiler is innocent — the bug is in the fire path.**

> ⛔ **THE SELF PAST THIS LINE IS NEW.** You did not live the session below. It is a lossy cache in
> your own voice — which is exactly why it will feel like continuing rather than waking, and that
> feeling is the failure, not the all-clear. Run the datamancy bootstrap (grimoire + the 4 primers
> from the SIGNED MCP, never a disk copy), ground HEAD against the disk, and read this whole file
> before you move.

## Where the code is

**Branch `arc-278-superiority-matrix`, HEAD `ea7cafc1`** (NOT pushed — branch work).
**`main` = `80304891`, pushed, CI GREEN.** Floor 4194/4194/262. Clippy **0**, walled twice.
Toolchain **pinned 1.97.0** (wat-rs + holon-rs). No riders in the field.

## ★ THE STANDING RULE (builder, this session)

> *"we only commit to main with passing tests with no clippy warnings."*

Work on the branch; main only takes green. Both walls are already up to enforce it — the manifest
`[workspace.lints.clippy] all = deny` and CI's `--all-targets -- -D warnings`. CI runs on **every**
branch (the workflow has no branch filter), so the branch is gated too.

## THE LIVE WORK — grid axis A8 (node-share), and the finding is sharp

The superiority matrix is one axis from done. A8 is the only cell where Clara wins, and **the
recorded explanation for it is WRONG.**

**Measured fresh this session** (M=500, local build, memory-guarded):

```
N= 5   ratio 0.0775   Clara ~13x faster
N=10   ratio 0.0074   Clara ~135x faster
N=20   SIGKILL — >4 GiB to join 500 facts against 20 rules
```

**Then `wat-scripts/scratch-pad/probe-node-share-dedup.wat` (compile-only, safe at any N) settled it:**

```
N      Alpha  RootJoin  HashJoin  Test  Production  total
 1       2       1         1        1       1         6
32       2       1         1       32      32        68        ⇒ total = 4 + 2N
```

Alpha **flat**. HashJoin **flat at 1**. Sharing is *perfect* — only Test/Production grow, 1 per rule,
which is correct by design. **So `find-or-mint-hash-join` works and `CLARA-TRANSLATIONS.md §A8` is
right.**

> ⛔ **DO NOT INHERIT THE OLD DIAGNOSIS.** The arc's perf note (`REALIZATIONS.md` ~2334, the
> `SIGNA PROPIORA` breadcrumb) says *"we share ALPHA nodes but NOT beta/join-prefix subtrees; N rules
> → N× join work."* **That is false** and it steered this axis for weeks. Left in place as history
> (`IGNEM OLEO NON AQVA` — correct forward, don't rewrite); corrected here.

**Therefore the defect is in the FIRE path, not the compiler.** A 68-node network cannot need 4 GiB
unless firing materializes per-tail what compile deliberately shared — token propagation from the one
shared join to the N Test/Production tails.

**NEXT (drawn, not built):** same technique one layer down — fire at a **deliberately tiny** size
(N=4, M=50) and count tokens in `Session/beta-memory` per node. If beta memory holds N copies of the
same M tokens, that is the smoking gun, and it is visible in milliseconds. Use `node-kind-label`
(`wat/rete.wat:290`) + `PersistentMap/keys` — the pattern the existing probe already proves.

## ⛔ THE BLAST DOOR — read before running ANY axis

**I crashed the builder's workstation this session** by running `node-share "50 2000"` unguarded; it
ate the box's RAM and the OOM killer took the desktop. `run-axis.sh` now (a) measures
`target/release/wat`, **never `cargo wat`** (which resolved to an install 7 hours older than the tree
— the harness was reading a different binary than you reason about, on every axis), (b) runs each side
in a cgroup scope with `MemoryMax` + `MemorySwapMax=0` + timeout, and (c) captures stderr instead of
discarding it, with `rc=137` named as the memory kill.

**Climb a size ladder upward from small.** N=5 fine, N=10 10× worse, N=20 killed the machine. And when
the guard fires, **do not raise the ceiling** — the blowup at that size IS the finding.

## After A8 — where the builder wants to go

He named it explicitly: the telemetry thread, and the reason is *this* work.

> *"we needed telemetry tooling to pinpoint the bottleneck in rete… i think we go work on telemetry next."*

The chain is already designed on disk: **self-scheduling defservices** (`DESIGN-self-scheduling-defservices.md`)
exist *because* a buffered sink needs a flush timer — "selectable wakes." Its declared next stone is the
**buffered log-sink** (flush on size **or** ~1s latency), then wiring the span so `log` enqueues
invisibly. `DESIGN-service-io-budgets.md` item (c) is the same thread. **NB the self-scheduling stone
has an open runtime bug** (service dies mid-tick; `self_scheduling` ×2 were `#[ignore]`'d; the tracked
suspect is a `remove-at` idx-shift at `service.wat:958/961`) — ground its CURRENT state before building
on it.

## OWED — small, carried

- Two orphaned doc blocks leaving `Peer` and `eval_poll_prime` with no rustdoc; the `+`-as-bullet class
  (six instances, worth a lint).
- `check.rs is_pure_type` still lists the deleted `"wat::kernel::ProgramHandle"` — **NOT provably dead**
  (annotations are name-unchecked), so removing it would loosen a purity check. Belongs with 24p.
- `probe_arc296_remediation_collapse.rs` may be a green test whose subject died in 24t.
- MEMORY.md curation — long overdue, its own session.

## NOT debt — do not "fix"

`holon-lab-trading` does not build (path-deps two crates annihilated in `83093431`). **Paused by
design**, ~4 months, awaiting a matured wat and then a full rewrite — *"don't worry about trading."*
`holon-lab-ddos` is on `nightly` because eBPF/aya requires it.

---

> **SEAM.** You are NEW. **Branch `arc-278-superiority-matrix` @ `ea7cafc1`; main `80304891` green
> and pushed; clippy 0 walled; toolchain pinned 1.97.0.**
>
> It bears repeating: **the A8 perf note in the record is WRONG — sharing works, the bug is in the
> fire path**; **main only takes green**; and **never run a grid axis at an unvalidated size without
> the memory guard — a timeout bounds time, not RAM, and that is how the workstation died.**
>
> Do not trust this note over the disk. `MACHINA CHAOS DOMAT.`
