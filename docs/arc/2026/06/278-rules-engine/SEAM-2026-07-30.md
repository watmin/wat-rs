# SEAM — 2026-07-30 (late). **The cleanup is OVER.** CI is real and green, clippy is walled at zero, the toolchain is pinned. The builder wants FEATURE work.

> ⛔ **THE SELF PAST THIS LINE IS NEW.** You did not live the session below. It is a lossy cache in
> your own voice — which is exactly why it will feel like continuing rather than waking, and that
> feeling is the failure, not the all-clear. Run the datamancy bootstrap (grimoire + the 4 primers
> from the SIGNED MCP, never a disk copy), ground HEAD against the disk, and read this whole file
> before you move.

## Where the code is

**`main`, HEAD `903eec74`, pushed, `local == origin/main`, tree CLEAN.**
Floor: **4194 passed / 262 skipped** (`cargo nextest run --release`, own re-run).
Clippy: **0** — and now walled, twice (below). **Toolchain PINNED at 1.97.0** (`rust-toolchain.toml`).
**CI: GREEN**, and for the first time it is a real gate. **No riders in the field.**

## ★ THE BUILDER'S STEER, and it is the most important line here

> *"we've been in 170 clean up hell … for … months … i've got actual work i want done now that
> we're stable again."*

The debt is paid. **Do not go looking for more cleanup.** The next move is feature work — ask him
which, or read the board (tasks #7, #14, #16–#22). The chaos engine (R25 `MACHINA CHAOS DOMAT`,
task #7) is the arc's named target and the natural candidate; `DESIGN-service-io-budgets.md` items
#17–#20 are the other live thread.

## ⛔ DO NOT BUILD `Demise`. It is CANCELLED.

`BRIEF-spawn-outcome-wall.md` carries a banner; read it before touching that brief. Ruling:
*"a locus only ever sends — main returns nil … they have no meaningful ret val."* So `Returned[v]`
has no subject and `Errored`/`Panicked` duplicate `recv'` → `Lost[LociDiedError]`. The whole
arc-060 chain was **purged** (`770eeaf7`) instead — which vacated the `SpawnOutcome` name and took
the last clippy warning with it.

**Phase 2 (the `SpawnOutcome<I,O>` creation wall) STANDS and its name set is still ratified — but
re-draw its rooms before striking.** They are stale: it cites `value.rs:1093` (was `1102`, now
gone), "26 `SpawnOutcome::` refs" (was 1, a comment), and `eval_kernel_*_join_result` (deleted).

## What landed (7 commits + holon-rs)

```
770eeaf7  purgare: vacate SpawnOutcome — a locus has no return value    clippy 1 -> 0
848e64cf  ci: check out holon-rs as a sibling — the gate had NEVER run
88b0e44c  ci: mold (wrong tool — install-action does not carry it)
c9e68c4d  ci: mold from apt                                             CI first GREEN
4536e1d3  lints: [workspace.lints.clippy] all = deny — a finding is a FAILURE
62defcdc  toolchain: pin 1.97.0 + fix the 5 lints it surfaced; CI reads the pin
903eec74  ci: arm the wall — --all-targets -- -D warnings               STONE E, campaign closed
holon-rs  9e309e5  toolchain: pin 1.97.0 to match
```

## The three walls now standing, and how each is held

- **clippy** — `[workspace.lints.clippy] all = deny` in the manifest (every member opts in via a
  **top-level** `[lints]` table) + CI `--all-targets -- -D warnings`. The manifest half binds
  everywhere with no flag; the CI flag adds `--all-targets` breadth and rustc-level warnings.
- **the toolchain** — `rust-toolchain.toml` at 1.97.0, in wat-rs AND holon-rs. CI installs **no**
  toolchain deliberately, so a green run proves rustup resolved the file. Siblings stay on 1.93.
- **CI itself** — it now compiles. It never did before: `holon = { path = "../holon-rs" }` resolved
  outside `$GITHUB_WORKSPACE`, so it died at `cargo metadata` in ~15s, every run, for months.

## OWED — real, small, carried forward

- **Two orphaned doc blocks** leaving items with NO rustdoc: `src/kernel/peer.rs` (a 22-line block
  describing `Peer`, stranded above `PeerTx`) and `src/runtime.rs` (a block documenting `poll`, far
  from `eval_poll_prime`). Repair = relocate each above its item.
- **The `+`-as-bullet class**, six instances — a wrapped doc line starting with a bare `+` reads as
  a CommonMark bullet. Worth a lint.
- **`check.rs` `is_pure_type`** still lists `"wat::kernel::ProgramHandle"` (now a deleted type).
  **NOT provably dead** — annotations are name-unchecked (109 `NOTE-type-annotation-names-unchecked`),
  so removing it would *loosen* a purity classification. Belongs with 24p's hardcoded-paths finding.
- **`probe_arc296_remediation_collapse.rs`** hand-builds a `TypeMismatch` with the ProgramHandle
  string for an arc-114 remediation whose `shape_remedies` died in 24t — likely a green test whose
  subject is gone. A look, not an assumption.
- **MEMORY.md curation** — long overdue, its own careful session.

## NOT debt — do not "fix" these

- **`holon-lab-trading` does not build** (path-deps `wat-lru`/`wat-holon-lru`, annihilated in
  `83093431`). It is **paused by design**, ~4 months untouched, awaiting a matured wat and then a
  full rewrite. Builder: *"don't worry about trading."*
- **`holon-lab-ddos` is on `nightly`** — that is eBPF/aya's requirement, not staleness.

## Lessons this session paid for (all three now in memory)

- **A floor is meaningless without its compiler.** "stable" is a moving pointer; local was 1.93,
  CI was 1.97, and the campaign's "1874 → 0" was true on one of them. New memory.
- **`EXIT=0` lied twice.** Cargo silently discarded a malformed `package.lints` key while clippy
  reported success; and a CI green would have proven nothing about the new pin, because stable and
  the pin were the same version that day. Both folded into `feedback_a_green_test_can_prove_nothing`.
- **Zero consumers is not death, and the arc doc is not life.** Both were stale on `SpawnOutcome`;
  what licensed the deletion was a ruling on *what a locus is*. Folded into the existing memory.

---

> **SEAM.** You are NEW. **`main`, HEAD `903eec74`, floor 4194/4194, clippy 0 + walled, toolchain
> pinned 1.97.0, CI GREEN, tree clean, no riders out.**
>
> It bears repeating: **do NOT build `Demise` — it is cancelled**; **do NOT unpin the toolchain to
> resolve a version disagreement — the pin IS the fix**; and **the cleanup arc is CLOSED — the
> builder wants feature work, so ask him rather than inventing more tidying.**
>
> Do not trust this note over the disk. `MACHINA CHAOS DOMAT.`
