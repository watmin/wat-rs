# SEAM — 2026-07-30 (late). **Clippy is at 2**, from 1874. Both remaining are the `Demise` rename.

> ⛔ **THE SELF PAST THIS LINE IS NEW.** You did not live the session below. It is a lossy cache in
> your own voice — which is exactly why it will feel like continuing rather than waking, and that
> feeling is the failure, not the all-clear. Run the datamancy bootstrap (grimoire + the 4 primers
> from the SIGNED MCP, never a disk copy), ground HEAD against the disk, and read this whole file
> before you move.

## Where the code is

**`main`, HEAD `60a5bce9`, pushed, `local == origin/main`, tree CLEAN.**
Floor: **4194 passed / 262 skipped**, `cargo nextest run --release`, own re-run, ANSI-stripped Summary.
Clippy: **2** (`workspace --all-targets`). **No riders in the field.**

Both remaining warnings are `large_enum_variant` on `SpawnOutcome` — **not lint work**. See the live
work below; do not "fix" them.

## ⛔ THE LIVE WORK — Phase 1 of the spawn-outcome brief (the `Demise` rename)

**`docs/arc/2026/06/278-rules-engine/BRIEF-spawn-outcome-wall.md`** — read it; it is ratified
(intueri-cast + builder) and is the authority. The two-sided rename:

- **`SpawnOutcome` is RECLAIMED** for a NEW creation wall — `:wat::kernel::SpawnOutcome<I,O>` with
  `Spawned` / `Exhausted` / `Refused` / `Rejected` / `Failed`, the twin of `ConnectOutcome`.
- **The arc-060 death value renames off it** → `Demise::{Returned, Errored, Panicked}`
  (`src/value/value.rs:1102`, from `Ok`/`RuntimeErr`/`Panic`).

**Phase 0 IS ALREADY DONE** — this is the thing to know, because the brief predates it. It sized
Phase 1 at *"the 26 `SpawnOutcome::` refs"*; **there are now zero** outside the definition. The
non-prime spawn surface was retired and took every join-result consumer with it — `check.rs:95`
records it: *"arc 114's shape_remedies died with the spawn/join/join-result tombstones."* What
survives is the enum, its three arms, `InThread(Receiver<SpawnOutcome>)` at `value.rs:1148`, and two
re-export lines (`value/mod.rs:45`, `runtime.rs:715`). The retire-first ruling worked exactly as
intended: *"kill what we came here to kill, then impl demise on what remains."*

`SpawnOutcome` measures **264 bytes**, of which **240 is `AssertionPayload`** in the `Panic` arm.
Whether that gets boxed is a decision INSIDE Phase 1's shape, not a lint fix to land first.

**⚠ Do NOT delete `SpawnOutcome` because it measures as unused.** I proposed exactly that this
session and was wrong — see `feedback_no_consumers_does_not_mean_dead`.

## What landed (this session, 8 stones + the prior curare)

```
9726c86e  D2  the key-eligibility wall (KeyEligibility, exhaustive, breach-proven)
3a0ed6be  D3  clippy.toml ignore-interior-mutability      18 mutable_key_type -> 0
bebbc794  D4  the mechanical match-shape cluster          61 -> 32
97769adb  D5  eight earned #[expect]s                     32 -> 18
e8339754  D6  ptr_arg — 8 real signature fixes            18 -> 10
e729f39a  D7  ProcessSelectable 696 -> 16, to_wire(self)  10 -> 6
60a5bce9  D8  type_complexity — two typealiases            6 -> 2
```

Earlier the same day: `cea76d71` (D0, three tests that could not fail), `e51480cf` (D1, the
151-site doc fleet).

## After Demise: stone E, and it is the point

`.github/workflows/ci.yml:41-44` still says clippy is informational and should tighten to
`-- -D warnings` **once the floor is zero**. Arming it is the campaign's last act — **zero is a
moment; the wall is what makes it permanent.** It is now one CI edit away.

## OWED — real defects surfaced, not yet fixed

- **Two orphaned doc blocks, currently leaving items with NO rustdoc.** `src/kernel/peer.rs` — a
  22-line block describing `Peer` stranded above the `PeerTx` enum inserted between them.
  `src/runtime.rs:26623` — a block documenting `poll`, ~280 lines from `eval_poll_prime`. Riders
  demoted both to `//` (correct — closing the gap would have FUSED `Peer`'s description into
  `PeerTx`'s docs and misattributed it). **The repair is relocating each block above its item.**
- **The `+`-as-bullet class, six independent instances.** A mid-sentence wrap leaves a bare `+`
  starting a doc line; CommonMark reads it as a bullet marker and swallows what follows. **Worth a
  lint of our own.**
- **Two stale doc contents**, flagged and untouched: `runtime.rs:25359` (a truncated `///` fragment
  for a function deleted in arc 293.W.2a) and `types.rs:2266` ("emits THREE companion values" while
  listing two — the third retired in 293 K3-revise).

## ⚠ MCP — half fixed; the config line is the builder's

`~/.cargo/bin/wat` is now a **current** install (verified: answers `initialize`), so there is a stable
binary my builds cannot clobber. But `~/.claude.json` still points the wat MCP at
`target/release/wat` — the live build artifact — so **every floor run still breaks the handshake.**
The remaining step is the builder editing that path. Do not edit his config.

## Lessons this session paid for (both now in memory)

- **An exemption is EARNED when the alternative is WORSE, unearned when it is merely UNFINISHED.**
  Eight `#[expect]`s landed phrased against the alternative (`&T` makes the fn uncallable from the
  derive; a params struct is indirection with no reader; `PI` compares against a different number).
  Then I nearly wrote a ninth that would have read identically and been a launder — the four questions
  killed it on Honest. **`#[expect]` over `#[allow]`**: a stale exemption reports itself.
- **"No consumers" ≠ dead.** See the `SpawnOutcome` note above. Right evidence, wrong conclusion;
  only the arc DOC disambiguated. Grep `docs/arc/**` before proposing any deletion.
- **"Correct" and "sufficient" are different questions.** I four-questioned whether boxing
  `ProcessSelectable::Spawned` was correct and never asked whether it was enough.
  `large_enum_variant` fires on the DIFFERENCE between variants, so with one side boxed the lint just
  renamed its target. Both boxed: 696 → 16.

## Also open, unchanged

Arc 278's R0 streaming chaos engine (`MACHINA CHAOS DOMAT`); the `eprintln`-abuse non-terminal warn
sink (task #22 — the serve loop's `Lost` arm still uses wat's PANIC, so a genuine peer break burns the
service down); the session-render `:field-0` defect; `mapv`/`filterv` refusing `PersistentVector`.

---

> **SEAM.** You are NEW. **`main`, HEAD `60a5bce9`, floor 4194/4194, clippy 2, tree clean, no riders
> out.** Next is Phase 1 of `BRIEF-spawn-outcome-wall.md` — the `Demise` rename, made small because
> Phase 0 already cleared its consumers. Then arm `-D warnings`.
>
> It bears repeating: **do not delete `SpawnOutcome` because it measures unused — it is a name held
> mid-sequence**; **an exemption is earned only when the alternative is worse**; and **ask whether a
> fix is sufficient, not merely correct.**
>
> Do not trust this note over the disk. `MACHINA CHAOS DOMAT.`
