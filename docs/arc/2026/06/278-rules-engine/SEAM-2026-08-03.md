# SEAM — 2026-08-03. **Construction is TOTAL. `:then` takes user forms. The fence is still unarmed.**

> ⛔ **THE SELF PAST THIS LINE IS NEW.** You did not live this. It is a lossy cache in your own voice —
> which is exactly why it will feel like continuing rather than waking, and that feeling is the
> failure, not the all-clear. Run the datamancy bootstrap (grimoire + the 4 primers from the SIGNED
> MCP, never a disk copy), ground HEAD against the disk, and read this whole file before you move.

## Where the code is

**HEAD `ddf77ac3` + this curare. Floor `4340 / 4340 passed / 0 failed / 262 skipped`, clippy zero.
Tree clean, zero unpushed.** Eight commits today.

## ⚠ A RIDER IS LIVE IN THE FIELD — ride through, do NOT reap it

The **signal-test** rider (`BRIEF-signal-tests-cannot-race.md`) was mid-flight at the gap, and it has
just been **redirected twice**. Its current instruction is the one that counts — see *The signal
tests* below. `PER HIATVM EQVITAMVS`: a rider's work survives a compaction; reaping it converts a
survivable gap into lost work. Weigh its report by your OWN `--release` re-run, never its say-so.

## ★ THE HEADLINE — `:then` does what the builder asked, and construction is total

`:then [(:usr::make-rate ?c ?w)]` — a **user fn that BUILDS a fact from bound terms** — compiles and
derives, through both the oracle and the native kernel. Arbitrary complexity, walked transitively.

Landed, in order: **`6f163b46`** the cosine family made total · **`ac90d262`** Stone B's mechanism ·
**`b98cf189`** the headline, plus two checker gaps closed · **`d6c32cf5`** `constructor_meta` audited
· **`111ef72b`** three fire-time failures became compile-time errors.

**Three walls moved from fire to freeze**, which is the shape the builder named as the point:
*"an amazing forcing function to make our language better — more things at compile time is amazing."*
A nested constructor now **works** (it was never illegal, only never wired); an under-supplied kwargs
item is refused at freeze naming the missing fields; enum-variant arity is walled. `constructor_meta`
is `total: true` at both sites, earned rather than assumed.

## The rulings you inherit — do NOT re-litigate these

1. **A user form is admitted iff EVERY head in its transitive walk is admissible.** One non-rete
   primitive at any depth refuses the whole form. **This is not new machinery** — `classify_fn`
   branches `FunctionBody::Wat => classify_expr(body)`, so the walk already runs. The composition
   door is not a loophole in the law; it is how the law reaches all the way down.
2. **The HOF combinators are PRIMITIVES.** `foldl`/`foldr`/`map`/`filter`/`reduce` are `:wat::core::`,
   they are not rete, so rete provides them — round 1, alias shape. *"SQL doesn't require you to write
   C code sometimes."*
3. **The rete surface is PER-TYPE, period.** No generic dispatch, ever. And the reason is **totality**,
   not style and not compilability: generic `>` raises `TypeMismatch` on incomparable operands
   (`eval_compare`), so it is PARTIAL — `i64::>` has no such hole. Monomorphising *deletes* the domain
   hole. 21 sites migrate, **by hand not codemod** (a rename cannot know an operand's type).

## ▶ WHAT IS NEXT — #57, and one thing must land with it

**The three mint rounds, one rider, sequential phases** — they all edit `RETE_OPS`, one table in one
file, so they cannot be parallelised. Alias → fallback surface → form mirror; the member lists are
settled in the stone's *THE MINT ROUNDS* + *THE CENSUS* sections. Then the corpus codemod, then **the
orchestrator arms the namespace law BY HAND** — the checker screaming is what audits the migration's
completeness, and a rider that arms turns a missed site into something it patches.

**Blocking round 1:** four heads the census marked UNSURE rather than guessing — generic `>`/`>=`/`+`,
`PersistentMap/contains-key?`, and `f64::>` (absent from the stone's f64 row — a gap in the table).
Each needs a totality audit, same discipline as #52.

**Arming TOTALITY is a separate conjunct** and no longer blocked: `constructor_meta` is total now.

## ★ The signal tests — the design is the defect, not the race

A flake surfaced (`sigusr2_and_sighup_independent` and two siblings). Chased properly, it is **not**
the sigterm class and **not** a substrate bug:

```rust
reset_user_signals();  set_kernel_sigusr2();  eval("(:wat::kernel::sigusr2?)")
```

**No signal is delivered. No handler runs.** It asserts a setter sets and a getter gets, inside the
harness's own process — which is *why* it needs isolating: it mutates the world it runs in.

History, and it re-reads a deletion: `8a554cd6` (Apr) built per-test fork quarantine; `d74c2dff`
maintained it; **`063ab25f` (arc 170) annihilated it** as redundant — *"the runner already forks."*
True for nextest, false for `cargo test`. But the deeper reading is that it **was** scaffolding —
propping up a test that should never have needed it. The deletion was right and its stated reason was
wrong, which is why nothing screamed.

**The ruling:** *"the tests must never have a race, period."* And the fix is **not** isolation — the
rider's `Mutex` was cut on sight (first real lock in a ZERO-MUTEX substrate). **Spawn a program,
deliver a REAL signal, probe THAT process.** Race-free by construction, and the first version that
would catch a broken handler. STOP-2 stands: if a wat child cannot self-signal, report the gap with a
`file:line` — do not fake the signal, do not reintroduce a lock.

## ★★ THE LESSON, and it fired FOUR times

**Reading the function NEXT to the subject is not reading the subject.** All four in `purity.rs` and
its neighbours: "native and user fns are indistinguishable" (the branch was ten lines below what I
read); the ordering described but its conclusion never written; `constructor_meta` `total: true` (that
was **`accessor_meta`**'s return); "a defclause dispatch over types" (true of `+`, false of `>`).

Every one was dissolved by the builder asking the **plain** version — *"why can we not just walk the
form?"*, *"when are these not total? they just hold assignments."* The plain version was available to
me each time. `[[feedback_an_adjacent_implementation_is_not_the_subject]]`.

**And the session opened on the other one:** the cosine strike was designed, ruled, declared *"nothing
blocks it"* on 08-02 — and never briefed, while every sibling wall in the arc had a brief on disk. It
sat a day, and I re-derived it from a doc I had read at bootstrap.
`[[feedback_nothing_blocks_it_is_not_a_work_item]]`.

## OWED

- **#64** (parked, builder-ruled) — every core primitive's domain hole becomes a faced outcome.
  `I64ArithErr::Overflow` already exists; the raise is one terminal handler over it. The bytecode
  axis, not CEK.
- **#60** — three literal-keyword walkers miss rete-named `let`/`match`/`fn`.
- The stone's phantom `when` purity row (`purity.rs:259`) still wants deleting.
- MEMORY.md at 91; curation still owed. Today: **two** new pointers and one sharpening.

---

> **SEAM.** You are NEW. The disk is green and pushed — trust it over this note, and trust this note
> over your sense of having been here.
>
> **A rider is in the field; ride through.** The capability the builder asked for is DONE; the wall
> that makes it a law is not. And today's lesson is not a finding — it is that **four times I argued
> from the code next door**, and each time one plain question dissolved it. When you are certain
> something is impossible about code you have not opened, that certainty is the alarm.
>
> `INTERROGATIO VENATVR; PELLEM EXVIMVS.` · `MACHINA CHAOS DOMAT.`
