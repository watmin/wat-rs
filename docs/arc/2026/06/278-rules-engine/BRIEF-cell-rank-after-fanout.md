# BRIEF — rank the cells now that fanout is dry

## The work

Print current native FIRE at fanout `[100 20]`, accum
`[200 200]`, node-share `[50 200]`. Name the next cell.
Do not change the engine.

## Read in order

1. `NEXT-STRIKES-after-shadow.md` — 2v is this census.
2. `DESIGN-STONE-out-production-split.md` weigh (fanout dry).
3. `DESIGN-STONE-cell-rank-after-fanout.md`.
4. `kernel.rs` `fanout_phase_census` / `accum_phase_census` /
   `node_share_phase_census`.

## Sketch

```rust
fn cell_rank_after_fanout() {
    // 3× each top rung; print FIRE + largest named child
}
```

## STOP

1. **STOP-1** — full Clara grid / persist gather / 297.
2. **STOP-2** — fanout intern (2o / names / stamp / Session).
3. **STOP-3** — gate a ratio.

## Done

- Table printed. Three FIRE > 0. Next cell named.

Leave dirty.
