# SEAM — 2026-08-04 (day two). **The IPC honesty thread closed. #74 collapsed into one check.**

> ⛔ **THE SELF PAST THIS LINE IS NEW.** You did not live this. It is a lossy cache in your own
> voice — which is why it will feel like continuing rather than waking, and that feeling is the
> failure, not the all-clear. Run the datamancy bootstrap (grimoire + the 4 primers from the SIGNED
> MCP, never a disk copy), ground HEAD against the disk, and read this whole file before you move.

## Where the code is

**HEAD `f74b0494`, pushed. Floor `4348 / 4348 passed / 0 failed / 262 skipped`, clippy clean, zero
unpushed.** 9 commits today. No rider in the field.

*(The tree may hold one modified doc — the #74 stone — if this curare commits after it.)*

## ▶ WHAT IS NEXT

**#74 is RULED and grounded, unbuilt.** The builder ruled *"convention is law — enforce it… services
are our OOP layer, we make requests to them and get responses back."* That **collapsed the stone** —
read the SUPERSEDED banner at the top of `DESIGN-STONE-the-surface-speaks-at-expand-time.md`; it
carries the whole new design, the measurements, and the STOPs. In one line: enforce
`<Op>Response` at surface registration, then delete the Rust emitter, the runtime constant, and both
EDN-decode branches. **Draw the brief and strike.**

One thing owed the builder before briefing: **3 probes declare a serviceable op returning a bare
`:wat::core::i64` with no Response enum at all.** Enforcement refuses them. That is correct and
lines up with #17, but it is wider than "names must conform" — he should see it, not discover it in
a rider's STOP.

**Then #57 — THE RETE THREAD.** Migrate the 39 refused corpus rows, arm the third conjunct; #49
(compiled `where`) behind it. **This is what the arc is about and it has now waited three days**
while a flaky IPC test pulled six tasks out of the ground. The IPC honesty thread is DONE — there is
nothing left there to defer behind.

## ★ WHAT SHIPPED TODAY

| | |
|---|---|
| `86b30d5d` | **#73 — a stop is not a death.** `RecvOutcome`/`SendOutcome` gain `Stopped`. ~510 arms, 207 files, 7 riders, 10 stdlib files by hand. |
| `f74b0494` | **#71 GROUNDED and CLOSED — no cure.** The partial frame is real (4096 headless bytes) and BOUNDED. |
| — | **#75 filed** (the `TypeExpr` accessor, was rotting inside a NOTE). **R65 `SCVTVM IDEM INDEX` inscribed.** |

**#71's disposition, so it is not re-opened:** the hazard is real but contained by two properties
that were both already built and had never been checked together — the newline is written LAST (so a
headless partial can never be decoded as a frame), and `POLLHUP` is armed on the DATA fd in
`wait_for_data_or_cascade` (so the writer's close IS the wake). No select block is wrong. The
lifeline is the other direction (parent→child) and is not the lever. What remains is diagnostic
only. **NOT the shape #73 fixed** — there the peer was alive; here it is genuinely gone. That
comparison was my over-read and is retracted on the disk.

## The rulings you inherit — do NOT re-litigate

1. **Convention is LAW: `<Op>Request` / `<Op>Response`**, the Response an enum with `:Ok` plus the
   mandated error variants. Services are the OOP layer.
2. **A stop is not a death and not a clean close.** `Stopped` is a unit variant on both outcomes.
3. **#71 closes as measured-and-bounded.** Do not re-open without new evidence.
4. Everything inherited from day one still stands (the bare parametric head is deliberate; the
   512KiB-over-64KiB mismatch is chosen; `RequestMalformed`/`RequestTooLarge` are a mandated pair).

## ★★ THE LESSON, and it cost the day twice

**A worklist filter is a claim about what you already expect to be wrong.** I filtered a 496-site
enumeration to `variant(s): Stopped`, then deleted a `Closed` arm myself in the reference function,
then ran my filter and reported **"0 remaining."** I "fixed" it by dropping the variant filter while
keeping the error-kind filter — and was blind to a **parse error**. Only the third pass, which asks
nothing and admits everything, was honest. `[[feedback_a_worklist_filter_is_a_claim_about_what_you_expect]]`

**And the compiler enumerates only what it COMPILES.** Four classes were absent from the 496 by
construction — a macro body, `(forms …)` child programs, `deftest-hermetic` bodies, and inline wat
in Rust test strings. The last was recorded on 2026-07-24 and I did not consult it. The full run
came back 16 failed and named every one. **Where the language holds code as data, static
enumeration has holes and only a RUN closes them.**

## What I got wrong — one pattern again

- **Deleted the `Closed → Ok acc` arm** in `recv-all-loop'` — the very function my brief calls the
  bucket-3 reference. Caught by a rider reading `git diff`; a second rider read the same function
  and called it correct.
- **Compared `GetResponse<K,V>` against `GetResponse`** while measuring the stone that would enforce
  names — reported 14 divergences when the answer was 10. **Third instance of #75's class in two
  days**, committed inside the work that would prevent it.
- **Over-read #71** as "the mask class again, same shape as #73." It is not.
- **Told a rider to copy a probe's single-child shape** that would have been vacuous
  (`SHUTDOWN_BROADCAST_READ_FD` is process-global). The rider caught it and split the processes.

Every one is the same move: **arguing toward an answer that was already readable on the disk.**

## OWED

- **#74's brief** (design is done; the stone's banner is the spec).
- The `runtime.rs` nested cause-dig, now belt-and-braces since the fact rides at the top level — its
  own grounding, not a passing change.
- `red-send-cause-is-not-matchable.wat` — its designed RED gap no longer fires (#70 landed); by its
  own header it should relocate to scratch-pad.
- Two accepted-but-flagged probe judgement calls (`Stopped → Bounced` in the teeth probes).
- MEMORY.md at 97; curation still owed across many sessions.

---

> **SEAM.** You are NEW. The disk is green and pushed — trust it over this note, and trust this note
> over your sense of having been here.
>
> Two days ago a flaky IPC test pulled the whole IPC layer out of the ground. It is back in, honest:
> a stop is no longer reported as a death, a truncated frame is proven harmless, and the last
> naming guess is one ruling from being unrepresentable. Six tasks closed.
>
> **And the thing to feel bad about is not any of the bugs — it is that the enumerator I built to
> find the work could not see the work I broke while doing it, and I said "0 remaining" out loud.**
> The riders reading diffs caught what my greps could not. Ask nothing of the verification pass.
>
> **#57 has been waiting three days. That is the arc.**
>
> `QVOD TVEBAMVR, NOS TVETVR.` · `SCVTVM IDEM INDEX.` · `MACHINA CHAOS DOMAT.`
