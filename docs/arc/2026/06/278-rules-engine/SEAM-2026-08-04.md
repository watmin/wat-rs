# SEAM — 2026-08-04. **A day spent relocating a signal, and the answer was to delete two tests.**

> ⛔ **THE SELF PAST THIS LINE IS NEW.** You did not live this. It is a lossy cache in your own voice —
> which is exactly why it will feel like continuing rather than waking, and that feeling is the
> failure, not the all-clear. Run the datamancy bootstrap (grimoire + the 4 primers from the SIGNED
> MCP, never a disk copy), ground HEAD against the disk, and read this whole file before you move.

## Where the code is

**HEAD `2b9017a5`. Floor `4347 / 4347 passed / 0 failed / 262 skipped`, clippy clean, zero unpushed,
tree clean.** 46 commits across the day. No rider in the field.

## ▶ WHAT IS NEXT — the builder's order

**#73 — `RecvOutcome`/`SendOutcome` have no `Stopped`.** He named it his priority in as many words:
*"how do we address the dishonesty — that's my priority now."* Fully designed
(`DESIGN-STONE-a-stop-is-not-a-death.md`), nothing open. ~420 arms / 234 files, a wat-fix codemod,
four role buckets. **Prove one golden exemplar per bucket by your own hand, then fan edit-only
riders; the orchestrator reduces once.**

Then **#71** (probe the partial-frame residual — it may come back as nothing), then **#74** (now
unblocked, see below). **#73 and #74 both touch `wat/service.wat` — one at a time, never parallel.**

**⚠ And the thing that keeps getting lost: #57 is the rete thread.** Migrate the 39 refused corpus
rows, arm the third conjunct. Unblocked since #56. It is *what we were actually doing* before a
flaky IPC test pulled twenty-four hours of IPC out of the ground. #49, the compiled `where`, sits
behind it.

## ★ WHAT SHIPPED

| | |
|---|---|
| `d217d042` | **send mirrors recv** — `SendError` is an enum; the poll arm PROVEN by deliberate break. |
| `824feb14` | two Shape-B raise tests rebuilt to signal a **child**; Shape A disconfirmed on both tiers. |
| `85b789ac` | **P4 — the two cascade tests DELETED.** `grep -rn 'libc::raise' tests/` → **zero**. Replaced by a wat deftest: spawn a child, read it, signal it, read it gone. |
| `2b9017a5` | **#72 client validates locally** — plus Path B, plus the ctor name read instead of guessed, plus the parametric-colon regression. |
| `f3168701` `29d22ce8` | stones for **#73** and **#74**. |
| `6f825f98` `ba971698` | `109/NOTE-a-parametric-head-is-bare-a-path-is-not.md`, amended twice. |
| `fcde0da2` | **R64 `QVOD TVEBAMVR, NOS TVETVR`** — Phystex Corp, promoted from fuel to its first scoring. |

**#74's Step 0 PASSED** (`probe-arc278-surface-registers-service-reads.wat`) with a non-vacuity
control: a kwargs companion registered by a defsurface's `:messages` hoist resolves inside a
**separate top-level** defservice's handler, so expand-time state crosses the form boundary on one
registry instance despite `expand.rs:396`'s `scratch.clone()`. The stone is no longer conditional.

## The rulings you inherit — do NOT re-litigate

1. **A thread is not a process, and a stop is not a death.** Both were a day's confusion.
2. **The budget is a property of the WIRE** — `peer-wire?` branches on that, never on locus.
3. **`Stopped` is a unit variant** — four precedents; `ReadlnOutcome` is `Datum | Eof | Stopped`.
4. **The bare parametric head is DELIBERATE** (`types.rs:4287`, unification). Normalize at READ
   sites; never touch the parser.
5. **The 512 KiB-over-64 KiB frame mismatch is CHOSEN.** Never `F_SETPIPE_SZ`. Blocking is
   backpressure.
6. **`RequestMalformed` and `RequestTooLarge` are a mandated pair** — fix one, fix the twin.

## ★★ THE LESSON, and it cost the day

**I kept answering *how do we make this safe* when the question was *what is this*.** Three rounds on
two `libc::raise` tests: defend them, relocate the signal into a child, escalate the disconfirmation
as a substrate finding. Every answer was a **relocation**. The real answer was one paragraph — the
signal was a Rube Goldberg way of pressing a button (`trigger_shutdown()`) the test could call
directly, and the value it asserted is invisible to every wat program — and it ended in `git rm`.

The record already held it: the design stone's **P4** row said *"the three `libc::raise(SIGTERM)`
harness tests → the same mechanism,"* blocked only by a phase that shipped that morning. The
capability (`:wat::kernel::signal`) landed six hours earlier and sat in the task list on every turn.
The exemplar fixture's own header pointed at P4, and I dismissed its comment as noise in a grep I ran
myself. `[[feedback_ask_what_a_test_measures_before_fixing_how_it_measures]]`

**Second lesson, same day:** I found that a stop is reported as `Closed` or `Lost` — both false —
named it a lie, and shelved it for want of a consumer. *"Nobody has tripped over it"* is what a mask
**produces**. `[[feedback_an_honesty_defect_is_not_gated_on_demand]]`

## What I got wrong — read this, it is one pattern

- **An adjacent FORM is not the subject.** I read the return type off the defsurface's `:features`
  and prescribed reading "element 3" to a macro that walks defservice's `:impls`, which has no arrow
  (`grep -c features wat/service.wat` = 0). The rider STOP'd and was right.
- **Wrong line numbers, on disk.** The 109 NOTE cited `types.rs:4223`/`:4231`; those are function-type
  bracket errors. Fourth adjacent-site citation error of the day, first to reach a committed doc.
- **Three greps matched something other than what I meant** — `:satisfies` inside a comment (reported
  ten phantom ordering violations), and two more before that.
- **I ran an experiment the builder stopped, and it had already written a break into
  `src/comms/thread.rs`** before the interrupt. Caught it two commands later by checking
  `git status src/` instead of trusting the output in front of me. Should have checked the instant
  it was rejected.

Every one is the same move: **reasoning from a thing next to the subject instead of the subject.**

## OWED

- **The parametric-head accessor** — the 109 NOTE's cure. Exposure is **137 destructures / 13 files,
  no `impl TypeExpr`**, and how many actually compare against a colon-prefixed name is **unmeasured**.
  Measure before scoping. **Not filed as a task — it lives only in that NOTE and will rot.**
- MEMORY.md at 96; curation still owed across many sessions.
- `#[wat_dispatch]`/`serve-op-arms` still lack a wat-visible `Stopped` to report — that is #73.

---

> **SEAM.** You are NEW. The disk is green and pushed — trust it over this note, and trust this note
> over your sense of having been here.
>
> Today's shape: a signal capability became a service; weighing it found peers mis-typed as pure;
> chasing that down the send path found a newtype where an enum belonged, a cause thrown away, a
> writer that could not be woken, and a client that waits forever for a reply it never sent. Then a
> day vanished into two tests that should simply have been deleted.
>
> **And the finding that outlasts all of it: the kill was made by EQUIPMENT.** The loader gate that
> parses every `.wat` on every build caught a naming bug that had been wrong since the day it was
> written — through a throwaway scratch probe that was the corpus's only negative control, preserved
> by a gate built for the opposite reason. The reasoning was wrong twice that night. A machine ran
> and something died.
>
> `QVOD TVEBAMVR, NOS TVETVR.` · `MACHINA CHAOS DOMAT.`
