# BRIEF — leftover accum is remainder vs tax, not alpha raw

## The work

Split accum `[200 200]` FIRE **61.72**. Print alpha remainder
vs tax, then setup:seen / drop / fold / production. Do not
change the engine.

## Read in order

1. `NEXT-STRIKES-after-shadow.md` — 2w is this census.
2. `DESIGN-STONE-cell-rank-after-fanout.md` weigh (accum 61.72).
3. `DESIGN-STONE-prod-leftover-split.md` (the tax law).
4. `DESIGN-STONE-accum-leftover-split.md`.
5. `kernel.rs` `accum_phase_census`.

## Sketch

```rust
fn accum_leftover_split() {
    // cal, 3× accum_phase_census(200, 200)
    // remainder_alpha / tax_in_alpha / honest_FIRE
}
```

## STOP

1. **STOP-1** — intern alpha off the raw / persist gather / 297.
2. **STOP-2** — nest new `phase_start` in a fact loop.
3. **STOP-3** — gate FIRE on a wall.

## Done

- Table printed. FIRE > 0. Largest drawable engine row named.

Leave dirty.
