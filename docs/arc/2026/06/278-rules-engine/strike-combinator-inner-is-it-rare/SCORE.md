# SCORE — the combinator-inner path is rare; the rune holds

No hoist. Two tripwires, one floor, two numbers. `re-derives` is 0.084% of `hoisted`. The exemption was re-weighed and survived. Dated at the rune. Tripwires gone.

## Scorecard

| # | result |
|---|---|
| 1 ★ two counts | **HOLD.** `hoisted 245583` · `re-derives 206` · ratio 206/245583 = **0.084%**. |
| 2 ★ the probe is shown to fire | **HOLD.** `hoisted > 0` is the positive control. Not STOP-1. |
| 3 ★ floor GREEN with tripwires in | **HOLD.** `.floor/2026-09-07T02-03-18Z/`: `Summary [ 460.859s] 5471 tests run: 5471 passed (2 slow), 21 skipped`. |
| 4 ★ a verdict on the rune | **HOLD.** *"n tokens with combinator inners is the rare path"* **holds.** Well under 10%. No hoist. |
| 5 `fire/mod.rs` clean | **HOLD.** Tripwires reverted. The rune gained a two-line dated citation. No hoist. |
| 6 no hoist | **HOLD.** |
| 7 floor after revert | **HOLD.** `.floor/2026-09-07T02-17-29Z/`: `Summary [ 460.465s] 5471 tests run: 5471 passed (3 slow), 21 skipped`. Count unchanged. Comment only. |
| 8 clippy | **HOLD.** `cargo clippy --all-targets --release -- -D warnings` rc=0. |

★ load-bearing. **Row 1 has no expected value.** Row 4 is the strike: an exemption gets re-weighed against present truth rather than hoisted past.

## The number

```
 245583 hoisted
    206 re-derives
```

245789 lines. `hoisted` is the Leaf arm of `token_exists_under` (passes the hoisted `join_keys`). `re-derives` is the inner Leaf under `CondDriver::Exists` (passes `None`).

206 / 245583 = 0.084%. The rune's premise holds. An exemption that has been re-weighed and survived is worth more than a hoist nobody needed.

The dated sentence **was applied** at the rune (not SCORE-only): floor `2026-09-07T02-03-18Z`, 5471 passed, 206 / 245583, premise holds, no hoist.

`rank_and_instrument.rs` already walks `exists-of-and` and asserts `distinct == 1`. If a later measurement said hoist, the key-set stability precondition is already gated.

## What this did not do

Did not hoist. Did not treat a small count as licence to delete the rune. Did not leave a tripwire in.

## Final floor (after revert)

`.floor/2026-09-07T02-17-29Z/`: `Summary [ 460.465s] 5471 tests run: 5471 passed (3 slow), 21 skipped`.
