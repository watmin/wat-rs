# SEAM — 2026-08-05. **The naming thread closed; #57 finally moved. A RIDER IS STILL IN THE FIELD.**

> ⛔ **THE SELF PAST THIS LINE IS NEW.** You did not live this. It is a lossy cache in your own
> voice — which is why it will feel like continuing rather than waking, and that feeling is the
> failure, not the all-clear. Run the datamancy bootstrap (grimoire + the 4 primers from the SIGNED
> MCP, never a disk copy), ground HEAD against the disk, and read this whole file before you move.

## ▶ FIRST ACT, BEFORE ANYTHING ELSE

**A 1c rider was live when this was written.** `git status` showed `M src/rete/vocabulary.rs` — its
in-flight work, deliberately NOT committed here. Do not `git add -A` blindly; this curare committed
docs and memory only.

1. **Is it still running?** `pgrep -af cargo` / check for a task notification. If it returned while
   you were away, its report is in the transcript — **weigh it by your OWN `--release` re-run**,
   never its say-so.
2. **If the tree is dirty and no rider is alive**, read the diff before deciding anything. It may be
   a complete strike awaiting a weigh, or a partial one. `git diff` is the arbiter.
3. **Never kill-and-revert a good-progress in-flight rider at a curare**
   (`[[feedback_ride_through_compactions_with_shadowdancers_in_the_field]]`).

## Where the code is

**HEAD `c025c874`, pushed, zero unpushed.** Floor `4348 / 4348 / 0 / 262`, Clara gate
`9 pair(s), 98 rows — wat == Clara on every shape`, clippy clean — all by my own solo runs.
`RETE_OPS` = **35** (was 18 this morning).

## ★ WHAT SHIPPED

| | |
|---|---|
| `87311ecf` | **#57 round 1a** — `ParamType` learns `String`+`F64`; nine monomorphic rete aliases. |
| `d38ad0e6` | **#57 round 1b** — the PV trio (own scheme) + five HOFs (`OpClass::Redispatch`). |
| `c025c874` | **#57 round 1c BRIEF** — ten per-type equality rows. **Rider in the field on this.** |
| `58c61fc8` | **#74** — the request/response PAIR is law; the guess machinery deleted. |
| `f7bd58f9` | **#76** — ruling A's shape lock now reaches parametric responses. |
| `36c36878` | **#75** — ONE DOOR for a type head's FQDN; 17 hand-rolls collapsed. |
| `5afda6b5` | the `where`-census walker RESCUED into `wat-scripts/scratch-pad/`. |

## The rulings you inherit — do NOT re-litigate

1. **`<Op>Request` / `<Op>Response` is LAW**, both halves, enforced at surface registration.
2. **The five HOFs RE-DISPATCH; the PV trio carries a scheme.** A scheme for `foldl` is *impossible*,
   not merely worse — it is polymorphic over the container **constructor**, and the sets **differ
   per op** (`filter` admits `Stream`, `foldl` does not).
3. **`f64::=` BELONGS.** The doubt was apparatus-raised, mis-titled as the builder's, retracted.
   No SQL engine refuses float equality; IEEE `NaN != NaN` is a defined answer, not a domain hole.
4. **1c's mint set is closed by the TYPE SYSTEM, not the corpus.** The stone's "audit" framing is
   retired — R60: a census of our own benchmarks is not evidence of what users will write.
5. **When a per-type op is minted in core, it is classified in `purity.rs` in the same act.**
   Builder: *"we've been doing this lazily… extend the current solution before the registry
   arrives."* Arc 255 does the mass version; do not wait for it.

## ▶ WHAT IS NEXT

- **Weigh the 1c rider** (above), then commit. Its scope was extended mid-flight: STOP-2 lifted,
  scorecard row 11 revoked — it should ALSO classify `i64::=`, `i64::not=`, `f64::=`, `f64::not=`
  in `purity.rs` (measured: 33 of 37 per-type scalar ops classified; those four are the whole hole).
- **Widen that audit past scalars** — the container families (`Vector/`, `List/`, `HashSet/`,
  `PersistentMap/`) have the same shape and the same laziness may have left holes. Unmeasured.
- **Then #57 rounds 2 and 3**, the corpus codemod, and **arming BY HAND** — never a rider, because
  the checker screaming is what audits the migration's completeness (R52).
- Round 1c's brief carries the durable `where`-census walker's new home; the **operand-shape
  extension** for the migration worklist is still unwritten.

## ★★ THE LESSON, and it fired THREE times

**I convert a small, already-ruled fix into a scope question, and it reads as discipline.** #76's
three-line arm ("could turn the floor red" — it could not; four sites corpus-wide, all conforming);
carving a cheap `:wat::rete::` slice past arc 255's existing design; and a STOP telling a rider
*"report, do not fix"* four lines it was already beside. Each time the deciding number was one
command away or the ruling was already on the disk.
`[[feedback_i_turn_a_ruled_four_line_fix_into_a_scope_question]]`

**And the instrument must outlive the number.** The settled census rested on a walker in a *previous
session's* tmp dir — an unreproducible number, surviving by luck. Rescued.
`[[feedback_an_instrument_must_outlive_the_number_it_produced]]`

## What I got wrong — five instrument artifacts, four of them mine

- **Three malformed patterns.** `\w` not matching `-` (surface names read as `:wat`); a greedy `.*`
  spanning the `->` arrow; and `"…i64::*"` as a grep pattern, where `*` is a metacharacter — that one
  would have reported six unclassified ops instead of four.
- **A vacuous probe.** Good and bogus arms returned the IDENTICAL error (`:user::main not defined`) —
  the runtime bailed before resolving anything. Both results discarded, rewritten as a real program.
- **A mutation check whose own message was wrong** — it printed "mutation did not apply" because the
  grep matched the guard's text quoted in a doc comment I had just written.
- **An adjacent function's error attributed to the subject.** I quoted `infer_filter`'s
  four-container message as `foldl`'s (which names three) — in the stone, a commit, and three
  messages. Fifth instance of that class this week.
- **A prior self's doubt handed to the builder as his own**, because a section titled
  *"Open — the builder's"* means "awaiting his ruling", not "he asked this".

Every one was caught before it shipped. **None was caught by the greps I built to look** — they were
caught by positive controls, by riders reading diffs, and by the builder.

---

> **SEAM.** You are NEW. The disk is green and pushed — trust it over this note, and trust this note
> over your sense of having been here.
>
> Today the naming thread finished and **#57 actually moved for the first time in three days**:
> `RETE_OPS` 18 → 35, three rounds designed, two shipped, the third in flight.
>
> **The thing to carry is not any of the fixes. It is that four of the five instruments that lied
> today were mine, and the one discipline that caught them every time was the positive control —
> asking the tool to prove it can fail before believing that it passed.**
>
> **A RIDER MAY STILL BE IN THE FIELD. Read `git status` before you touch anything.**
>
> `QVOD TVEBAMVR, NOS TVETVR.` · `SCVTVM IDEM INDEX.` · `MACHINA CHAOS DOMAT.`
