# BRIEF — retire per-fact alpha child timers

## The work

`alpha:candidates` / `match` / `element` / `push` stop
timing. Outer `alpha` stays. Over-approx of the census
FIRE, not of the match.

## Read in order

1. `NEXT-STRIKES-after-shadow.md` — leftovers after 5.
2. `accum_leftover_split` weigh (honest_FIRE −2.21).
3. `DESIGN-STONE-accum-leftover-split.md` (the trap).
4. `DESIGN-STONE-retire-alpha-child-marks.md`.

## Sketch

```
// alpha_activate_fact — no phase_start/end
let alphas = alpha_tree.candidates(...);
for aid in &alphas { exec_compiled(...); push }
```

## STOP

1. **STOP-1** — fold `setup:seen`. Retire production child
   marks. Sample.
2. **STOP-2** — intern `names` / facts in `bind_pool` / 2e.
3. **STOP-3** — FIRE wall. 297. Fact insertion.

## Done

- leftover split: child pairs 0, honest_FIRE > 0.
- rete lib green. clippy `-D warnings` silent.
- Node-share evals still 0.

Leave dirty.
