# VIGILIA 2026-09-05 — the work list

> **Cast at HEAD `21530efab`, branch `grok-rete`.** 19 wards: 17 inward in parallel, then
> `experiri` (which drives), then `circumspicere` (which surveys what the rest turned their backs
> on). `mora` out per the standing orders in `VIGILIA-LOOP.md`; `cohaerere` / `consonare` /
> `nesciens` dropped under the builder's code-only ruling. **81 L1 + 97 L2.**
>
> The prior full cast was 2026-08-30 (41 L1 + 70 L2, 19 wards). The gates were green throughout:
> floor **5420 run / 21 skipped**, 0 fail, clippy rc=0 at cast time and at write time.

> ⛔ **STATUS IS EDITED HERE, IN PLACE. Never append a closure below a row.** A row's status living
> in two places IS the defect — `exigere` found exactly that in this arc's TRACKED DECISIONS.
> One row, one place.

> ⛔ **DO NOT WORK THIS AS 178 ITEMS.** Nineteen wards found six classes. `circumspicere` found the
> class above all of them, and it is CLASS Ω below. Pulling that root is worth more than the rest
> of the list combined.

> **Every ward's verbatim return is in `reports/`.** The 2026-08-30 cast lost all nineteen of its
> returns because they lived only as subagent messages — its own work list records the loss:
> *"the 19 ward returns were subagent messages and evaporated."* That cannot recur here.

---

## ⛔ THREE CORRECTIONS TO THIS ARC'S OWN RECORD — read before using any figure below

**1. "265 lints" is not 265 gates.** `circumspicere` re-derived it: **265 test fns = 99 gates + 166
unit tests of the gates' own parsers**, and 32 of the 99 are `macro_rules!` shards of just two
gates. **~67 distinct structural gates.** Every ward in this cast — and the 2026-08-30 work list —
wrote "measured against the 265 lints" while calibrating against a figure that is **63% self-test**.
Live demonstration: `no_unknown_ward_rune` contributes 8 detector unit tests and 1 real gate.

**2. The 5420/4381 gap is closed.** `complectens` reported its scanner found 4381 tests against the
floor's 5420 and correctly refused to reconcile it away. **5420 = 4978 Rust + 442 `.wat` deftests**
generated by `crates/wat-macros/src/lib.rs:701` → `discover.rs:70` from `wat-tests/`, which exist in
no `.rs` file. The 21 skips = 15 `#[ignore]` + 5 default-filter + 1 wat-side. **That one wat-side
skip is `wat-tests/lint.wat:97` — the wat-side stdlib lint is currently OFF.**

**3. `intueri` returned 17 L1, not 8.** It answered in two messages; the first never reached the
orchestrator's context and was recovered from its transcript when this directory was written. Any
count taken from the live session before that recovery is low by nine.

---

## ⛔ CLASS Ω — THE PROCESS IS NOT MEASURED. Nothing enters `run_with_args`.

**`circumspicere` L3, and it is the root above every other class here.**

> Every instrument in this repo measures `wat` as a **library under cargo**. Nothing measures the
> **program the user runs.**

Not one of 5420 tests enters `distribution::run_with_args` — the actual `main`, and the only place
the process is configured: RLIMIT_STACK, signal handlers, the argv ambient, the exec image fd, and
the four-way mode dispatch. `grep run_with_args tests/` returns comments only.

**Why eighteen inward wards could not see it: ordering is not a line.** A lens asks *"is this line
wrong?"*, and a line is only wrong against a spec the lens can see. A **prefix of setup** has no
line to be wrong. Four entry modes each run a different prefix; two return before the stack raise.
Nobody chose that — the returns were added above it, one arc at a time.

| id | site | what | status |
|---|---|---|---|
| **Ω1** | `src/distribution/mod.rs:351` vs `:401-408` | **`--check` is a broken oracle in BOTH directions.** Empty file: `--check` rc=0, runtime rc=4 (`MainSignatureError`). Depth-1000 non-tail recursion: `--check` **134 ×6**, runtime **0 ×6**. Ceiling bisected 6 samples/depth: 400 ✓ 600 ✗; the full runtime clears 70,000. The mode's own comment sells it for *"editor save hooks and agent sweep loops."* | OPEN |
| **Ω2** | `src/distribution/mod.rs:317-319` | **The MCP server evaluates caller-supplied wat with no sandbox**, and returns before the stack raise. Arbitrary file read driven through the wire. Any turn deeper than ~600 kills the session (`134` ×6, bisected 550/560/570/580 ✓); the same program via CLI clears 70,000 — a ~100× gap produced by nothing but which side of `:401` the mode returns on. **The installed `/home/john/.cargo/bin/wat` is dated Aug 23 and is NOT the HEAD build (`cmp` differs)** — the live server's behaviour is unmeasured. | OPEN — **builder's call, not a rider's** |
| **Ω3** | `src/distribution/mod.rs:397-399` | **The arc-261 stopgap removed the diagnostic and its own comment denies it** (*"This only RAISES the ceiling; it does not remove the class"*). Single-variable, 6 samples/arm: hard limit unlimited (**the default**) → exit `139`, **0 bytes of stderr**; capped 8 MiB → exit `134` + `fatal runtime error: stack overflow`. Out of the box the failure is a **bare silent segfault**. Capping the limit *restores* the message. | OPEN |
| **Ω4** | `src/config.rs:465-477` | **One typo'd setter silently discards every setter after it.** `(:wat::config::rete::setmax-fire-rounds! 5)` — one missing hyphen — fails the shape predicate, takes `_ =>` at `:470`, sets `remainder_start` and **breaks**; the valid `set-dim-count!` behind it is never processed. `dim-count` stays 10000, exit 0, no diagnostic. **The guard for exactly this is unreachable**: `remainder_start` is assigned at one site immediately followed by `break`, so `if remainder_start.is_some()` at `:477` can never see `Some`. `SetterAfterNonSetter` is dead. | OPEN |
| **Ω5** | `src/distribution/mod.rs` | `--help` / `--version` are unimplemented — both fall through to the file-open path: `wat: read --help: No such file or directory`, exit **66**. First contact with the tool. | OPEN |

### ★ The cure, and it is the top rung

**Hoist the RLIMIT_STACK raise and the signal wiring above every mode return in `run_with_args`, so
a mode that skips them cannot be written.** Then **one gate that drives the real binary in all four
modes over the same fixture set and asserts they agree on verdict and exit code.** That gate is a
surface eighteen wards had no way to reach, and it closes Ω1, Ω3, Ω5 structurally.

**This is the recommended first strike, ahead of every row below.**

---

## CLASS A — two pieces of state that must agree, held apart. **D2's class, ~10 instances.**

D2 — cured at this HEAD, a shipped 18-rows-where-12-was-correct — was exactly this shape. The cure
made the bypass unrepresentable at **one site**. `session.rs:224-231` already ruled on the general
case: *"⛔ THE CURE IS STRUCTURAL, NOT CONVENTIONAL. Bumping the counter at the two bypass sites
would have cured today's two writers and left a third free to appear."*

**Both cure shapes are already in the tree**: `JoinRightIndex` (`session.rs:242`) and
`ClassIntern::intern` (`export.rs:1671`). `solvere` L3: *"the repo should say so once — naming the
pattern would make these look like violations rather than like ordinary code."*

| id | site | pair | consequence | found by | status |
|---|---|---|---|---|---|
| **A1 ★** | `hash_join.rs:120`, `:273`, `:429`; `fire/pass/mod.rs:107` ← `filter_after_join.rs:75` | `join_keys_cache` membership doubles as the catch-up latch; `left_idx` is not built by the second writer | **DRIVEN — a query loses rows.** native `OutW=1`, oracle `OutW=2` | `solvere` + `sequi`, **driven by `experiri`** | OPEN |
| A2 | `src/check/env.rs:366-375`; `check.rs:8414/8417/8435` | `defined_values` / `_spans` / `_asts` — the redef path advances two of three | stale AST **at HEAD, today**; false `DefRedefForbidden` after a redef-flag cycle | `solvere` | OPEN |
| A3 | `src/rete/compiled_cond.rs:219-234`; `export.rs:1415` | `slot_keys` / `output_slots` parallel arrays, hand-checked at one of two writers | a rule silently stops matching — the guard returns `None`, indistinguishable from "did not match" | `solvere` | OPEN |
| A4 | `src/value/value.rs:848-857`, `:1031-1047`; `fire/delta.rs:188-196` | `identity` memo vs. a walk that computes a **different function** on miss; `seen_ids`/`seen_rest` are two halves of one set | fixpoint dedup breaks if any cross-variant `eq` arm is added | `solvere` | OPEN |
| A5 | `src/process/handle.rs:82-84`, `:98-125` | `reaped` / `cached_exit` — three writers, one maintains the pair | **unbounded 100% spin**, no timeout, no yield | `struere` | OPEN (dead code today — see E4) |
| A6 | `src/process/exec_plan.rs:143-166` | `ENTERED_WAT_ENTRY` set **before** the fd it asserts | `spawn-process` poisoned for the whole process; the `io::Error` discarded | `struere` | OPEN |
| A7 | `src/distribution/mcp.rs:127-143` | `ticket_seq` / `ticket` — the minting verb maintains one | the anti-dual-fire guard | `struere` | OPEN |
| A8 | `src/rete/kernel/fire/pass/alpha.rs:85`, `:139-145`, `:217` | `class_ids` `(Vec<u32>, bool)` + `any_mixed`, two disjoint `&mut` arms | **fact loss**, not duplication. Site header says *"THE CURE IS THE `bool` BELOW"* — a bool cure, maintained by convention | `perspicere` | OPEN |
| A9 | `src/rete/kernel/session.rs:208`; `hash_join.rs:273`, `:375` | `JoinLeftIndex` — **the un-cured half of the D2 pair**; hands out `&mut` via `entry().or_default()`, which its sibling deliberately does not | weaker than its sibling's cure; the invariant it carries is temporal and no Rust type enforces it | `perspicere`, `sequi` L2-c | OPEN |
| A10 | `src/runtime.rs:329-332` vs `:506` | `SHUTDOWN_INIT_PID` stored **after** what it guards | two shutdown workers, one deaf; `extra_input_fds` silently discarded on a second call | `secare` | OPEN (unreachable today via the 3 production callers) |

---

## CLASS B — a doc-comment that ENUMERATES, invalidated by a later addition. **~10 instances.**

`typing.rs:570` has already written this failure down about itself and the header above it is still
wrong: *"The three sources this function documents as 'exhaustive' were written before fix-list F
made a nested call a legal operand, and nothing re-read them."*

| id | site | claim | truth | status |
|---|---|---|---|---|
| B1 | `src/rete/validate/typing.rs:544` | "THREE outcomes, all explicit" | 5 variants | OPEN |
| B2 | `src/runtime.rs:9729` | "Three canonical shapes" + "all three variants" | 4 variants (`HashDestructure`) | OPEN |
| B3 | `src/rete/vocabulary.rs:293-295` | "the five `Redispatch` rows, which carry no scheme at all" | **13** rows, 4 with non-empty `type_params` | OPEN |
| B4 | `src/value/numeric_order.rs:3, 11, 24, 161` | "three call sites", four times over | 4 — the missing one is `<`/`>`/`<=`/`>=`, the busiest path in the language | OPEN |
| B5 | `src/rete/kernel/fire/pass/mod.rs:24-28` | "thirteen sites → one act, so a future site cannot push without counting" | **4 still open-code it**, one in the file making the claim. **The D2 cure at `session.rs:236` cites this by name as its precedent.** | OPEN — **mutation-proved invisible, see D3** |
| B6 | `src/rust_deps/custodia.rs:112-113` | "`eval_peer_select_prime` (the sole caller)" of a `ref_guard` on an `unsafe impl Send+Sync` | **5 callers in 2 fns**; `eval_poll_prime` runs arbitrary user wat between guard and deref — its compliance was never argued | OPEN |
| B7 | `src/runtime.rs:21631-21688` | an entire doc block attached to the wrong function; ends mid-sentence, tail resurfaces as another fn's opening line | the real `Bundle` verb has **no header at all** | OPEN |
| B8 | `src/runtime.rs:1956`, `src/value/value.rs:201` | body calls `:wat::core::enum-new` | that head has never existed; it is `:wat::core::variant` | OPEN |
| B9 | `src/check.rs:395-397` | `args(callee, expected, got)` | the fn takes one parameter; the only use site writes `args(callee)` | OPEN |
| B10 | `src/config.rs:377-378`, `:229-231` vs `:16`, `:34-36` | "Required fields (`dims`, `capacity-mode`) set" | nothing requires them; `RequiredFieldMissing` is constructed nowhere. The same file says the opposite 3 lines apart. | OPEN — see Ω4 |

### ★ The gate, and it is cheap

**A cardinality word (`three`, `four`, `both`, `the sole`, `N sites`) in a doc-comment above an
`enum` or a caller list is mechanically comparable to the item count below it.** `intueri` costed
it: **catches 5 of its 7**, and it is a smaller build than widening any lint root. Recommended
second strike.

---

## CLASS C — a validation each layer defers to the other

| id | site | what | status |
|---|---|---|---|
| **C1 ★** | `src/resolve/walk.rs:268`; `check.rs:4884`, `:5558`, `:5585` | **The `:wat::` vocabulary is OPEN at load.** Resolve returns `true` by prefix commenting *"leaf validation is the type checker's concern"*; the checker accepts commenting *"Resolve pass validated the name"*. **DRIVEN, 7-cell grid: no position rejects a `:wat::` phantom at load.** Refusal exists only at runtime dispatch. | OPEN |
| C1a | 5 × `tests/reflection/*.wat`, `wat-scripts/scratch-pad/arc109-type-equal-acceptance.wat:16` | two phantoms riding C1: `:wat::kernel::abort`, `:wat::kernel::panic!`, both in the arm written to REPORT failure. **`experiri` corrected the severity:** the arm raises `UnknownFunction` rather than going silent — a diagnostic defect, not a false green. | OPEN |
| C2 | `crates/wat-reader/src/identifier.rs:78-79` | *"The lexer now **REJECTS** all raw control characters … this invariant is **ENFORCED** by the lexer, not merely conventional."* **DRIVEN: U+0001 inside an identifier loads and runs, rc=0.** U+0001 is the separator `env_key` splices between a name and its hygiene ScopeIds. The second claimed layer is a `debug_assert!` — and `Cargo.toml` has **no `[profile.release]`**. **New vs C1: a positive assertion that enforcement exists**, on a macro-hygiene boundary. Guard proven absent; **not** proven exploitable. | OPEN |
| C3 | `src/load.rs:653-674`; root `Cargo.toml` | `ed25519-dalek`/`sha2`/`base64` compile in unconditionally (no `[features]` table); **the verification mode is chosen by the head keyword the source itself wrote** — `:wat::load-file!` → `verification: None`. Zero `signed-load!`/`digest-load!` opt-ins in the whole corpus. Arc 295's own DESIGN says *"Signing is opt-in today."* | OPEN |
| C4 | `src/kernel/spawn.rs:882-933`; `comms/process.rs:2066` | a declared `max-message-bytes` binds **1 of 4** receiver legs; the 4th cannot be fixed at the call site — `sender_receiver_from_split_fds` is the only receiver constructor with no `_with_budget` sibling | OPEN |

---

## CLASS D — an instrument green over what it cannot see

| id | site | what | status |
|---|---|---|---|
| **D1** | `src/rete/kernel/tests/right_index_counter_invariant.rs` | **The D2 acceptance test is a tautology.** `indexed_n[J] == Σ\|buckets[J]\|` holds **by construction** — verified: `grep '\.buckets\|\.indexed_n' src/ \| grep -v session.rs` returns nothing. One possible outcome. | OPEN |
| **D1a** | `wat-scripts/scratch-pad/d2-derived-fact-axis.wat` | **The observable that caught D2 has no floor observer** — that file is parsed and type-checked, never run. **`experiri` refuted the urgency and sharpened the spec**: the 18-vs-12 no longer reproduces (`chain-rows=12` both engines — the cure holds), but the file is blind to A1 **by construction**: its wave-2 facts all carry *fresh* keys, so `old_left` is empty. **A floor observer needs KEY REUSE**, which `probes/probe_vig_left_idx_latch.wat` supplies. | OPEN |
| **D2** | `src/rete/reachability.rs:1659-1665` | a discrimination row that has **never executed** — the rewrite targets the miss face of a constant, so `replacen` no-ops and the `if` is never entered. Its two siblings use `assert_ne!`; this one uses an `if`, converting "the rewrite didn't happen" into silent success. **DRIVEN: swapping in `assert_ne!` goes RED on the first iteration.** | OPEN |
| **D3** | `fire/pass/mod.rs:151`; `fire/mod.rs:2070`, `:2080`, `:2104` | **`struere`'s unpaid claim, now paid by mutation pair** — same 100-test gate set: dropping `beta_written` at the open-coded site → **100 passed, INVISIBLE**; dropping it inside `record_token` (control) → **RED**, `recorded no beta writes — the instrument is not armed`. The gate works; no census world contains a `:where`, and all four bypass sites live behind a filter parent. | OPEN — see B5 |
| D4 | `probare`'s six | bare `#[should_panic]` over bodies with two setup panic sites; `assert!(n >= 0)` on a length (×3 — a class, incl. `wat-tests/deporder.wat:71`, a live 30s floor test); `gather_probe_cost.rs:706` asserting bucket count for both arms but element total for one | OPEN |
| D5 | `wat-tests/**/*.wat` deftests | land on the floor via `wat::test!{}` and are subject to **no** vacuity gate — a deftest with zero assertions returns Passed | OPEN |
| D6 | 12 gates | carry a **false provenance stamp**, e.g. `wat_scripts_fixes_load.rs:40-42` cites a count *"from `every_walking_gate_declares_non_vacuity.rs`, never from prose"* — that gate computes only `len()>=25` / `>=18` over `tests/lint` and **cannot produce 445**. Actual today: **452**. | OPEN |
| D7 | — | **the 265 → ~67 correction.** See CORRECTIONS above. Every coverage claim in this cast and the last is calibrated against a 63%-self-test figure. | OPEN |

---

## CLASS E — unowned surfaces. Not bounded-by-design; no arc ever landed there.

Of the ~265: 26 read `src/`, 17 `tests/`, 10 `crates/`, 9 `wat/`, 7 `wat-scripts/`, 5 `docs/`,
1 `benches/`, 1 `examples/` — and **0 read `scripts/`, `tools/`, `workflows/`, or `wat-migrate/`.**
`scripts/floor.sh` — the instrument the entire floor discipline runs through — is gated by nothing.

| id | surface | what | status |
|---|---|---|---|
| E1 | `examples/console-demo/wat/main.wat:29` | uses `:wat::core::enum`, HARD CUT at Stone 241.9. **Fails `--check` with 4 errors.** It is a workspace default-member whose `src/main.rs` is `wat::main! {}` — the crate compiles green forever while its `.wat` is dead. **And `src/check/error.rs:706`, a shipped runtime diagnostic, points users at it as "the canonical ambient-stdio shape."** | OPEN |
| E2 | `src/check/error.rs:621` | shipped diagnostic mirrors `wat/std/stream.wat → wat/stream.wat`; that file was annihilated 2026-06-27 | OPEN |
| E3 | **268 `.wat.bad` files** | match **no** `*.wat` sweep in this repo — not the load gate, not the codemod path lists. Two heads that recorded codemods declare *eliminated* are still live in one. `fixture.bad.wat` would put them back inside every existing sweep for the cost of a rename. | OPEN |
| E4 | `src/process/handle.rs` (whole file) | `ChildHandle` never constructed; `Value::wat__kernel__ChildHandle` never constructed. **Two live doc comments claim it is the substrate's zombie reaper** — `runtime.rs:32387` says it is one of two reap paths and is contradicted 10 lines below in the same block. ~12-site cascade. | OPEN |
| E5 | `wat-migrate/fix-decl.wat` + 6 under `tools/` | 7 `.wat` gated by nothing; `fix-decl.wat` still cited as live doctrine at `src/types.rs:2770` | OPEN |
| E6 | `crates/wat-source-derive` | in `members` but not `default-members`, while `Cargo.toml:13-18` claims `default-members` *"cover[s] every workspace member … keeps every crate honest at every checkpoint"* | OPEN |
| E7 | `Cargo.toml:41` | *"There is no rust-toolchain.toml pin, so the toolchain floats."* `rust-toolchain.toml` exists and pins `1.97.0`. | OPEN |
| E8 | 9 doctests | compiled by nothing — nextest does not run doctests and no job passes `--doc` | OPEN |
| E9 | `src/kernel/spawn.rs:693`, `crates/wat-macros/src/lib.rs:881` | spawn threads running user wat with no `.stack_size()`. `RUST_MIN_STACK` appears in `.cargo/config.toml:19` and `.github/workflows/ci.yml:9` and **zero source files** — the 8 MiB every test measures on is supplied by a cargo config. A library embedder gets 2 MiB. | OPEN |

---

## CLASS F — divergences where the SPEC is the wrong half

`conferre`'s standing warning: *"'spec side' does not mean 'presumed right' — the oracle was the
wrong half in two of the three closed precedents in this tree."*

| id | site | what | status |
|---|---|---|---|
| **F1 ★** | `wat/rete/oracle/explain.wat:10-49`, `:53` | **`fire-rules-explain$oracle` is NONDETERMINISTIC.** 8 samples, 8 producing rules: native stable `vex::aaa` 8/8; oracle returns **4 distinct rules** (`ggg`,`zzz`,`aaa`,`ccc`), agreeing 2/8. Single-producer control stable on both. `harvest-support` folds `PersistentMap/keys` (HAMT order) with no sort; its sibling `fire.wat:157` sorts and says why in writing. **The referee for explain is not a function.** The differential compares only `PersistentMap/length`. | OPEN |
| F2 | `wat/rete/oracle/insert.wat:100` | `retract` removes **every** equal fact; `insert` stages one. **DRIVEN:** 2 identical inserts → 3 facts; 1 retract → 1 fact; the derived consequence vanishes. Ranked L2 by `experiri`: the docstring says "by value equality", so it is a documented asymmetry, not a divergence from its own spec. | OPEN |
| F3 | `conferre` L2-1..3 | leading accumulate re-seeds every round into a cumulative beta; `insert-all`'s hardcoded `OP`; stratify `+1` divergence for `:exists`/accumulate-`:from` over a derived type | OPEN |

---

## PARTIRE — module seams (verdicts, not defects)

| file | verdict | cuts |
|---|---|---|
| `src/freeze.rs` (2646) | **SPLIT** | `process_runtime` (+ `src/runtime.rs:195-750`, one domain in two hosts) · `user_main` · `deftest`. ⚠ the same-OS-thread invariant is prose-only and becomes cross-module. **Refused cut:** constrained-eval — it restates the freeze contract. |
| `src/string_ops.rs` (1254) | **SPLIT** | `name_case` · `uuid` → `src/intrinsic/uuid.rs` · relocate `List/of` → `src/collection/eval.rs`. |
| `src/load.rs` (1894) | **SPLIT** | `source_loader` — seam verified one-directional (zero refs to `LoadError`/`LoadSpec`/`WatAST` in `:993-1279`). ⚠ **must land a boundary test on `LoadedSource::canonical_path`**, or a future loader that forgets to canonicalize silently disables cycle detection. |
| `src/io.rs` (1991) | SPLIT (L2) | `io::backend` · stray `eval_stdlib_sources` → `src/stdlib.rs`. ⚠ `read_frame`'s shutdown lockstep is a secret spanning the seam. |
| `src/rete/validate/mod.rs` | **LEAVE** | validate+reorder is one secret. |
| `src/runtime.rs` (40,883) | **LEAVE the proposal** | arc 255's `#[wat_intrinsic]` registry is the declared decomposition and is closing families one at a time. The one region it will never reach is the shutdown block — folded into Ω/partire above. |

---

## Verification status

**Driven by `experiri` or `circumspicere` with command + verbatim output:** A1, C1, C1a, C2, C3
(corpus grep + absent `[features]`), D2, D3, D1a (refutation), F1, F2, Ω1, Ω2, Ω3, Ω4, Ω5, E1.

**Verified by the orchestrator directly this session:** D1 (the `grep` returning nothing), the
`intueri` recount, the docs-`.wat` gate contract, and the four gates re-run green after this
directory landed (`every_docs_wat_loads_or_declares_why_not`, `no_unknown_ward_rune`,
`every_wat_scripts_file_loads`, `every_rete_name_in_wat_scripts_code_resolves` — 12 tests, 12 pass).

**Passed through on the ward's citation, not independently re-derived:** everything else. A row's
evidence is its ward's report in `reports/` until someone re-reads it.

⚠ **`experiri` refuted four leads, and two were the wards' own mechanism:**
1. The A1 collider is **`left_activate_join` from `filter_after_join.rs:75`** (pass 3.7), NOT
   `keyed_join_persistent` from `join_after_filter` (pass 3.6) — those joins have a *filter* parent
   and `kind_ids.join_parent` is `RootJoin | HashJoin` only, so pass 3 never visits them. **Two
   independent wards converged on a real defect through a false citation.**
2. The `:not` variant of A1 does **not** reproduce (`OutN=2` on both engines). Only `:where` holds.
3. D2's 18-vs-12 does **not** reproduce; the cure holds.
4. The F1 probe **agrees** at two producers and needs **eight** to shift HAMT order off ascending-id
   order. A single sample of it is worthless.

⚠ **`circumspicere` could not prove the binary it drove matches HEAD byte-for-byte** — it drove the
prebuilt `target/release/wat` (mtime after HEAD's commit). Every Ω row rests on that binary.
