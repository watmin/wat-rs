# ward `intueri` — vigilia 2026-09-05

> Cast at HEAD `21530efab`, branch `grok-rete`. Read-only unless the ward says otherwise.
> **Verbatim ward return, preserved unedited.** The 2026-08-30 cast lost its 19 returns
> because they lived only as subagent messages; this file exists so that cannot recur.
> Nothing here is adjudicated — see `../WORK-LIST.md` for what the orchestrator credited.

---

I have enough. The second sweep agent is still out; I'll flag that.

## L1 — names and structures that misdescribe what they are

**1. `Harness::run` returns a hardcoded-empty `Outcome`, and two green tests are named for the capture it does not do.**
`src/harness.rs:188` takes `stdin: &[&str]`; `:198` is `let _ = stdin;`. `:202-204` build `let stdout: Vec<String> = Vec::new(); let stderr: Vec<String> = Vec::new(); Ok(Outcome { stdout, stderr })` — the public `Outcome` (`src/harness.rs:59-62`, `pub stdout` / `pub stderr`) is **structurally always empty**. Module doc `:7-8` claims *"`run` builds StringIo stdio, invokes `:user::main`, and returns captured stdout/stderr"*; `:16-18` claims *"Harness enforces the same three-IO contract the CLI enforces"* — but `from_source` (`:104`) calls `validate_user_main_signature`, which enforces `[] -> :wat::core::nil` (`src/freeze.rs:1609-1613`, `let params = vec![]`). The module's own doctest (`:22-37`) is a `(:wat::core::define (:user::main (stdin …) (stdout …) (stderr …)))` program asserting `out.stdout == vec!["hello"]` — illegal form (`define` is hard cut, `src/runtime.rs:929`), rejected signature, and an assertion that `:202-204` makes impossible. It is `no_run`, so nothing compiles or executes the wat string.
Worse, the tests: `tests/kernel/wat_harness.rs:27` `fn harness_captures_stdout()` asserts `out.stdout.is_empty()`; `:41` `fn harness_injects_stdin_lines()` passes `h.run(&["alpha","beta","gamma"])` into the discarded parameter and asserts `out.stdout.is_empty()`. **What a reader gets wrong:** two rows of the 5420 floor read `harness_captures_stdout ... ok` and `harness_injects_stdin_lines ... ok`. They assert a literal `Vec::new()`. A caller who trusts the names writes `h.run(&["input"])` and gets silence with no error.

**2. `every_location_named_in_a_doc_comment_exists` walks three roots, not the tree — and the class it was built for is live outside them.**
`tests/lint/no_stale_path_in_doc.rs:243`, headline `:1` *"A SOURCE LOCATION NAMED IN A COMMENT MUST EXIST — THE PATH *AND* THE LINE."* `ROOTS` (`:83-87`) is `src/rete` (rs), `wat` (wat), `wat-tests` (wat) — 59 of 215 `src/*.rs` files, 47k of 193k lines, and **zero** of 743 `tests/*.rs`. Two hand-verified rots outside it:
- `src/intrinsic/io/reader.rs:461-465`: *"The exposure is real and is filed, not resolved here: `compile-condition` gates on pure ∧ deterministic ONLY — `wat/rete.wat:698` says in its own words that `total?` is UNARMED at the fence."* `wat/rete.wat` is **534 lines**. And the claim is false: `src/rete/purity.rs:350` — *"`total?` is ARMED at both `compile-condition` and `then-item-fence`"*; `src/rete/purity.rs:56-57`; `wat/rete/compile.wat:452-456` computes `is-total (:wat::rete::total? expr)`. A reader concludes a rete `where` fence still admits a nil-returning mutator.
- `src/types.rs:1927`: cites `comms/mod.rs:919`; `src/comms/mod.rs` is **529** lines (the variant is at `:310`).
This is the *same* defect the gate's own module doc recounts at `:18-21` (`wat/rete.wat:1508` in five comments while the file was 533 lines). **What a reader gets wrong:** the name says the tree is gated; a rider adding a `path:line` in `src/runtime.rs` or `tests/` is unprotected.

**3. `validate_arc170_legacy_callsites` checks no callsite, and its two comments contradict each other and the body.**
`src/check.rs:821`. The body (`:838` → `check_legacy_user_main_signature`) checks a **declaration signature**, never a call. Then:
- `:828-837` claims *"we look for a 3-binder `[stdin <- :T0 stdout <- :T1 stderr <- :T2]` params shape (3 typed slots) and treat it as legacy"* and *"Either head is acceptable here"*. The actual predicate is `:927-934`: fire unless `param_types.is_empty() && ret == ":wat::core::nil"` — a 1- or 2-param main fires too. And only `":wat::core::defn"` is handled (`:889`, `_ => return` at `:923`), stated 48 lines later by the code's own comment at `:885-887`.
- `:856-869` claims *"Two define shapes are supported"* including `(:wat::core::define …)`, and *"Both shapes converge"* — the define arm is DELETED (`:885`).
The correct predicate is stated at `:807-808`. **What a reader gets wrong:** they believe a 2-param `:user::main` passes the checker, and that the `define` shape is still handled. Name it `validate_user_main_signature_shape` (the domain noun already exists at `src/freeze.rs:1623`).

**4. `src/edn_shim.rs` module doc describes a writer-only module; half the file is the reader.**
`:1` *"render any wat value as EDN/JSON text"*; `:4-6` *"This module exposes its WRITER side to wat through **three** primitives"*; `:14` *"`:wat::edn::parse` (slice 2; **not yet shipped**)"*. In the same file: `:144/:168` `:wat::edn::read`, `:305/:317` `read-json`, `:347/:365` `read-foreign`, plus `ForeignRecord/get|class` (`:432`, `:474`) and `ForeignVariant/*` (`:497`, `:519`, `:540`); `:wat::edn::validate` is registered at `src/check.rs:2863`. **What a reader gets wrong:** they conclude EDN parsing is unimplemented and go build it.

**5. `register_defines`'s three-line doc is false on all three claims.**
`src/runtime.rs:902-904`: *"Walk `forms`, register every `(:wat::core::define ...)` into `sym`, and return the remaining (non-define) forms in order. Dupe registration halts with `RuntimeError::DuplicateDefine`."*
- `:929-932` — the define branch is DELETED; define forms pass through to the checker.
- Every arm in the 902-1084 span pushes to `rest` (`:926`, `:978`, `:1009`, `:1078`, `:1080`); `Ok(rest)` at `:1083`. Nothing is filtered out.
- `:962-971` — *"A collision … is NOT an error here"*, `Existing::Equivalent → NoOp`, *"a Duplicate never arises here"*.

**6. Orphaned doc glued to the wrong function.** `src/rete/matcher.rs:801-811` documents *"Resolve one operand from {bindings, fact-field, literal} … `Keyword(:field)` → the named field of the fact … **Generic over [`Bindings`]** … Monomorphised per call site"* — then runs straight into `:812` with no blank line, so it is `eval_computed_operand`'s doc (`:828`). That function is not generic (`bindings: &BindAcc`), and returns `None` unless the operand is a `List` (`:837`). The real owner of that prose is `resolve_operand` at `:915`, `pub(crate) fn resolve_operand<B: Bindings>`, which already has its own complete doc at `:906-914`, 100 lines below the section header `:799` the orphan sits under.

**7. `test_runner`'s discovery convention names a filter that was deliberately dropped — including in the message users see.**
`src/test_runner.rs:104-107`: *"A top-level `:wat::core::define` is a test iff: 1. The path's final `::`-segment starts with `test-`. 2. … 3. `ret_type` is the plain path `:wat::kernel::RunResult`."* All three false: `define` is hard cut; the name filter was dropped, documented **in the same file** at `:602-610` (*"the name filter has been dropped"*), with `fn is_test_function(_name: &str, …)` at `:614` underscoring the parameter it no longer reads; and `TestResult` is accepted too (`:595-597`). Repeated at `:86-87` and `:92`. **The user-facing cost:** `src/test_runner.rs:395` panics with `"wat test suite: no test- prefixed functions found under {}"`. A user whose `deftest` was not discovered is told to rename their functions — which cannot help.

**8. `wasted` is a subtraction across two different populations.**
`src/rete/kernel/tests/node_share_cost.rs:867` `let wasted = evals.saturating_sub(passes);`. But `filter:test-pass` is bumped on **two disjoint arms**: `src/rete/kernel/fire/mod.rs:2068` (the reuse arm — `exec_stashed_where` is never called) and `:2078`/`:2102` (after an eval). `filter:test-evals` is bumped only at `:2076`/`:2090`. So `passes ⊄ evals`. The test's own non-vacuity guard is disjunctive (`:857` `evals > 0 || reuse > 0`), and the sibling test asserts `fire_evals == 0` on this very axis (`:296-303`). With `evals == 0`: `wasted` saturates to 0, `waste_pct` takes the `evals == 0 → 0.0` branch (`:868-869`), `worst_waste` stays 0, and both gates — `:882` `evals <= passes*2` and `:894` `worst_waste < 50.0` — pass **structurally**. The doc at `:817-822` says the census *"gates the index"*. This is `compiled:calls` again: one key, two units, and a consumer whose arithmetic assumes one.

**9. A doctrine-struck claim still standing in code.** `tests/lint/wat_scripts_fixes_load.rs:8`: *"**ALL wat must remain correct, always** — a `wat-scripts/` file that no longer type-checks goes RED here, so the rot cannot hide."* `wat-rs/CLAUDE.md` strikes exactly this: *"⛔ 'All wat stays correct, always' stood here for months and was FALSE. Type-checking is not resolution."* The gate proves parse + type-check for `.wat` under `wat-scripts/` (`:36`, `:59`); a `def` body nothing forces can still name a head that has never existed. The claim is in the file the doctrine names.

## L2 — weaknesses

- **`dbeta:alloc` is a 0/1 flag under an allocation name.** `src/rete/kernel/fire/mod.rs:972` `census_count_n("dbeta:alloc", u64::from(!out.is_empty()))` — never exceeds `dbeta:calls` (`:970`), and a `Vec` grown by several `extend`s (`:964`) allocates more than once. Its one consumer already renames it in prose: `src/rete/kernel/tests/node_share_cost.rs:287` binds it to `fire_gathers` and `:315` reports *"non-empty gathers"*. **Remedy:** rename the key `dbeta:nonempty` — the consumer already knows the right word.
- **`TestSummary.failed` counts tests plus file-level errors.** `src/test_runner.rs:78-80`; `total` accumulates only `discovered.len()` (`:260`), while `failed` is bumped at `:189` (directory read), `:219` (file read), `:255` (freeze) — none a test. `:270` prints `"running {total} tests"` and `:353-355` `"{passed} passed; {failed} failed"`, so a run can report more results than tests. **Remedy:** a separate `file_errors` field.
- **`function_to_define_form` emits a `defn`.** `src/closure_extract.rs:3026-3027` — *"Build a `(:wat::core::define <signature> <body>)` AST"* — delegating to `function_to_define_form_with_body` (`:3045`) whose own doc at `:3044` says *"emits `:wat::core::defn` (not the retired `:wat::core::define`)"*. Rename both.
- **`src/distribution/mod.rs:87`** — *"A subprocess whose source is assembled at runtime may instead declare it via `:wat::core::define`."* Hard cut admits no bypasses (`src/special_forms.rs:183`, `src/freeze.rs:1917`).
- **`HarnessError::StdioSnapshot`** (`src/harness.rs:72`) is declared and `Display`ed (`:81`) and constructed nowhere — a public error variant naming a failure mode that cannot occur, left behind by the retirement noted at `:208-212`.
- **"oracle" is a name the tree has recorded as not an oracle.** `src/rete/matcher.rs:817` — *"Native and the oracle did not merely agree — they SHARED the defect … two engines agreeing proves nothing when the thing they agree on is the bug. Clara broke the tie."* The tree hedges to *"oracle / differential"* in most places (`src/rete/mod.rs:7`, `alpha_tree.rs:162`) but the directory is `wat/rete/oracle/` and the sigil is `$oracle`. *Differential twin* is what it is.
- **`dispatch_keyword_head_value`** (`src/runtime.rs:5360`) is **2217 lines** and carries no `rune:intueri(length)`; the repo has adopted the rune convention but holds only two runes tree-wide, so the convention is not yet load-bearing. It is a flat delegating dispatch table — a rune, not a split.

**Runes encountered (2, tree-wide):**
- `src/resolve/walk.rs:61` `rune:intueri(length)` — **clear.** Names the shape (call-head check + `match` on `Boundary`), says each arm is short, and states why it cannot drift.
- `wat/rete/oracle/fire.wat:54` `rune:intueri(naming)` — **questionable.** The reason's second half is *"Rename would fork every oracle fire caller"* — a cost argument, which the spell rules out. And the cost is refuted twice by this repo: `walk-filter-ids` has **8** call sites across `wat/`+`wat-scripts/`+`wat-tests/`, and `wat-scripts/fixes/` holds 25+ recorded `rename-*`/`reclaim-*` codemods (including `rehead-rete-callees.wat` and `rete-oracle-sigil.wat`) under the doctrine *"do not fear refactors, they are one-to-three shot."*

## L3 — judgement

The spark is unmistakable where the code has recently been under the knife. `src/alloc_counter.rs:1-80` argues **why bytes and not an item count**, with the measurement, the rejected alternative, and the builder's own question preserved because *"the answer is the module's shape, not a footnote to it"* — that module has internalised the `compiled:calls` lesson better than the census that produced it. `src/rete/kernel/session.rs:242-284` (`JoinRightIndex`/`RightIndexWriter`) makes the bypass unrepresentable rather than patched, and `:295-300` warns *"⚠ NOT a bucket count"* on `total_elements` — the exact warning the lying counter needed. `src/rete/kernel/arm.rs:632-637` and `src/rete/vocabulary.rs:1-70` are model naming: `merge_sorted_ids` says why a merge and not a re-sort; `Redispatch` is *"named for HOW the type is answered."* `tests/lint/no_ceiling_raise_in_rete.rs:86-125` carries two-sided non-vacuity.

The rot has one shape and it is not carelessness. **Every L1 above sits at a boundary the recent work did not cross**: `harness.rs` and `test_runner.rs` were left behind by arc 170's `:user::main` change; `edn_shim.rs`'s header by its own reader half; `check.rs:821` and `runtime.rs:902` by Stone 241's hard cut; `reader.rs:461` by the fence arming. The cured code is scrupulous about its own claims; the code *adjacent* to a cure keeps describing the world before it. The gates are aimed at `src/rete` and `wat/` — where the work is — and the shape they guard is provably live in `src/harness.rs`, `src/test_runner.rs`, `src/intrinsic/io/reader.rs`, and `src/types.rs`. **Widening `ROOTS` is the single highest-value move in this report.** The next one is a gate that no `#[test]` fn name may be an active-voice claim its body's own comment retracts — `harness_captures_stdout` asserting `is_empty()` is a lie a rider reads as a fact, at floor speed.

## What I could not check, and why

- **The second sweep agent (doc-comment contradictions across `runtime.rs`/`check.rs`/`types.rs`/`collection/`/`macros/`/`value/`) had not returned when I finished.** Its lane is my highest-overlap one; treat this report as under-covering those files. Everything above is my own read.
- **The 151 unresolved and 5 out-of-range `path:line` citations I measured outside `src/rete`** came from my mirror of `resolve()` (`no_stale_path_in_doc.rs:184-201`), anchored on one known-positive and one known-negative — **not** from the gate's own `citations_in_comments`, whose extraction rules I did not replicate. Only the two I hand-read (`reader.rs:461`, `types.rs:1927`) are load-bearing; the totals are indicative.
- **I ran nothing.** No build, no floor, no clippy. Every claim about behaviour is a claim about source I read at `21530efab`. The `Harness::run` finding in particular is a *reading* — `Vec::new()` at `:202-204` is unambiguous, but I did not drive it, and my memory's own warning applies: a reading cannot see an execution defect, only this one is the inverse.
- **I sampled.** 192,700 lines of `src/`, 743 test files, 20,905 lines of `wat/`. I read perhaps 3,000 lines closely, chosen by mechanical hunts (retired-form mentions, `every_*`/`all_*` gate names, `path:line` rot, counter increment sites, function length). **`wat/` is the thinnest coverage** — I read `wat/rete/oracle/fire.wat`, `wat/rete/compile.wat` (grep only), and headers. A lying name in `wat/service.wat` (2952 lines) or `wat/core.wat` (2152) would not have reached me.
- **I did not check whether any L1 is already tracked.** `docs/` was out of scope by the builder's ruling, so `NEXT-STRIKES` and the arc ledgers went unread; some of these may be known and filed.

---
*(continuation — this ward returned in more than one message)*
---

Verified. **Addendum to the report — seven further L1s, each read by me this session.**

**11. `src/runtime.rs:21631-21688` — two functions' doc comments are interleaved, and one function has no header at all.**
`:21631-21642` is the `:wat::holon::Bundle` verb's doc (*"Return type is `(:Result :- [...])`. Always. Under every mode. Callers are forced by the type system to acknowledge the possibility of failure"*). It ends **mid-sentence** at `:21642` — *"…statement exactly. There is"* — and `:21643` starts a different doc (*"Arc 294.c.2a — Kanerva width-bound verdict…"*) for the item it is actually attached to: `pub(crate) fn bundle_capacity_verdict(cost: usize, ctx: &EncodingCtx) -> Option<(i64, i64)>` at `:21661`. The severed tail resurfaces at `:21672` — `/// no codebook factor — under AST-primary…` — as the **opening line** of the doc for `fn eval_algebra_bundle` (`:21688`), the real `Bundle` verb, whose header therefore begins with a sentence fragment and never says what the function is. **What a reader gets wrong:** rustdoc renders a private `Option<(i64,i64)>` capacity predicate as the wat `Bundle` verb with a `Result` return and a type-system failure contract; the verb itself is undocumented. Same doc also carries an unresolved edit marker: `:21648` says *"ONE guard, two callers"*, `:21656-21660` says *"a THIRD caller"* and *"ONE guard, {two->three} callers"*.

**12. `src/value/numeric_order.rs` — "three call sites" four times over; the missing fourth is `<`/`>`/`<=`/`>=`.**
`:3-4` names three (`values_compare`, `walk_match_clause`'s `Compare` arm, `matcher::compare_values`); `:11` *"Each of the three callers"*; `:24` *"the three callers have three different policies"*. Actual sites: `src/runtime.rs:11700` (in `values_compare`, `:11680`), `src/rete/matcher.rs:1050`, `src/runtime.rs:15436` (in `walk_match_clause`, `:15347`) — **and `src/runtime.rs:11833`, in `fn eval_compare` (`:11803`)**, which is the dispatch target for `":wat::core::<"`, `">"`, `"<="`, `">="`, `i64::>`, `i64::<`, `i64::=`, `i64::not=` at `src/runtime.rs:6204-6252`. It has its own fourth policy (`Incomparable => false` at `:11835`; `NotNumeric =>` fall back to `values_compare` at `:11836`). The unit test's own doc, `:161-163`, then says *"callers 2 and 3 are unreachable through the checked path per the design stone's reachability ruling, so this table-level test is their only executable regression coverage."* **What a reader gets wrong:** they conclude the exact-ordering table is barely reachable from checked wat, when the most-exercised comparison path in the language routes through it — and that the coverage story is complete when the busiest caller was never counted.

**13. `src/config.rs:377-378` — "Required fields (`dims`, `capacity-mode`) set" — nothing requires them, and the error cannot be produced.**
Also `:229-231`: *"`dims` and `capacity-mode` are required."* Falsified in the same file: `:414` `unwrap_or(DEFAULT_DIM_COUNT)` (10000, `:66`) and `:719` `unwrap_or(DEFAULT_CAPACITY_MODE)` (`Error`, `:59`); the module doc says the opposite at `:16` and `:36`. `ConfigErrorKind::RequiredFieldMissing` is **constructed nowhere in `src/`** — declared at `:231`, rendered at `:275`, and otherwise built only by `tests/diagnostics/probe_arc296_derive_configerror_identical.rs:81`, which asserts its **EDN rendering** against a golden file. **What a reader gets wrong:** they believe an entry file omitting `set-dims!` is rejected; it silently gets 10000. And a green test row makes an unreachable error look covered — the test proves the *shape* renders, never that anything can raise it.

**14. `src/rete/validate/typing.rs:544` — "THREE outcomes, all explicit" on a five-variant enum, in a file that names its own defect.**
`enum OperandType` (`:552`) has `Resolved`, `NotComparable`, `UnboundInThisRule`, `ComputedNotDerivableHere`, `MistypedEnumVariant` — **five**. The header's whole argument (`:546-551`) is that `Option` was a catastrophic bug because *"an arm that discards while looking like diligence"* collapsed two situations into one. Two variants later, `:570-571` records: *"The three sources this function documents as 'exhaustive' were written before fix-list F made a nested call a legal operand, and nothing re-read them."* The file diagnoses the exact rot its own header is still exhibiting.

**15. `src/runtime.rs:9729` — "Three canonical shapes" for a four-variant `LetBinding`.** Three bullets (`Single`, `Destructure`, `StructDestructure`, `:9734-9746`) and `:9748` *"added per-name spans to all three variants"*. `enum LetBinding<'a>` (`:9750`) has a fourth: `HashDestructure`. **What a reader gets wrong:** believes `let` cannot destructure a map.

**16. `src/runtime.rs:1956` — a synthesized body documented as a head that has never existed.** *"Body `(:wat::core::enum-new :my::ns::E :Variant f1 f2 ... fn)`"*. The body is built at `:2027-2031` from `":wat::core::variant"`. `enum-new` occurs at exactly **two** sites in `src/`+`wat/`, both doc comments — this one and `src/value/value.rs:201` (*"whose body calls `:wat::core::enum-new`"*). **What a reader gets wrong:** greps for a nonexistent head when tracing `(:E::Variant a b)`.

**17. `src/rete/vocabulary.rs:293-295` — "the five `Redispatch` rows, which carry no scheme at all."** `grep -c "class: OpClass::Redispatch"` = **13**, and four carry non-empty `type_params`: `:811-818` (`PersistentVector`, `&["T"]`), `:820-828` (`Vector`), `:830-837` (`List`), `:838-845` (`PersistentMap`, `&["K","V"]`). **What a reader gets wrong:** treats `class == Redispatch ⇒ type_params == &[]` as an invariant and gates on `class` when deciding whether to read `type_params` — in the one table the module's own thesis says must be the single source of truth.

**Also worth naming (agent-found, spot-verified by me):** `src/check.rs:397` documents `args(callee, expected, got)` for a function whose signature is `type_error_remedies_via(callee: &str)` (`:398-400`); the only use site, `src/check/error.rs:99`, writes `args(callee)`. A reader copying the documented three-arg incantation onto a new variant gets an arity error, and believes `expected`/`got` feed remedy computation.

**Relayed but NOT verified by me** (I did not read these lines this session; treat as leads, not findings): the agent's item 8 (`src/freeze.rs:1229-1231` and `src/config.rs:390-391` naming deleted `run-sandboxed-ast` verbs), item 10 (`src/edn_shim.rs:18` and `:3713` intra-doc links to `value_to_edn` / `value_to_edn_string`, both reportedly deleted or renamed), and its lower-confidence group (`runtime.rs:23109/:23169` wrong-namespace config keywords; `types.rs:197-198` three-vs-four `Nature`; five stale `tests/probe_*.rs` paths in `src/hash.rs:63-64`, `src/collection/mod.rs:21-31`, `src/function/mod.rs:35-46`, `src/argspec/mod.rs:51`, `src/config.rs:89`). That last group is the same class as my L1 #2 and lands outside `ROOTS` — more evidence for widening it.

**What this changes in the judgement.** My L3 said the rot sits at boundaries recent work did not cross. Items 11, 14, and 15 sharpen that: three of them are **counts in a header that a later variant or caller invalidated** — three-vs-five, three-vs-four, three-vs-four. A header that enumerates is a claim that goes stale on every addition, and nothing in this tree checks an enumeration against the item beneath it, even where the file (`typing.rs:570`) has written down that exact failure. That is a gateable shape: a doc-comment cardinality word (`three`, `four`, `both`, `two callers`) sitting above an `enum` or a caller list is mechanically comparable to the variant count below it. It would have caught 11, 14, 15, 17, and the `numeric_order` header — five of the seven above — and it is a smaller build than widening `ROOTS`, though `ROOTS` is still the higher-value move.

The agent's clean-bill list (checked and honest) covers `remedy/rank.rs`, `value/pmap.rs`, `rete/kernel/arm.rs::merge_sorted_ids`, `rete/kernel/node.rs::sorted_node_ids`, `rete/validate/mod.rs:1270`, `rete/kernel/stratify.rs`, `rete/matcher.rs:978`, `check.rs`'s `CheckResult`, `fire/acc.rs::acc_var_i64`, `fire/mod.rs::key_of_el`, `string_ops.rs::is_canonical_uuid_string`, `collection/seq_container.rs::of_value`, `intrinsic/bytes.rs::decode_nibble` — I re-read only `merge_sorted_ids` of those and agree; the rest are its word, not mine.
