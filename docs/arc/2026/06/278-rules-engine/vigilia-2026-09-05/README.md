# vigilia 2026-09-05 — index

**Cast at HEAD `21530efab`, branch `grok-rete`. 19 wards. 81 L1 + 97 L2.**

This directory is a **map**, not a state dump. It names where the truth lives and forces the reads.

| path | what it is |
|---|---|
| **`WORK-LIST.md`** | **Start here.** The synthesis: three corrections to this arc's own record, then CLASS Ω (the root), then classes A–F. **Status is edited IN PLACE — never append a closure below a row.** |
| `reports/` | **19 ward returns, verbatim and unedited.** Primary evidence. Nothing here is adjudicated. |
| `recon/` | 3 sub-sweeps the wards commissioned. `census-name-audit.md` carries 13 unrowed name-vs-quantity findings on the counters every performance claim in this arc rests on. |
| `probes/` | `experiri`'s 15 probe files + the 3 things to fix before landing any of them. **Not wired into the build.** |

## Why `reports/` exists

The 2026-08-30 cast lost all nineteen of its ward returns. Its own work list records the loss:

> *"the 19 ward returns were subagent messages and evaporated."*

Its L2 findings survive only as a paragraph of leads that must be re-derived from scratch. **That
is the failure this directory is shaped to prevent.** Extract the returns to disk before
synthesising, every cast.

`intueri` demonstrates why it matters: it answered in two messages, the first never reached the
orchestrator's context, and **nine L1s were recovered from its transcript when this directory was
written**. A count taken from the live session alone would have been low by nine.

## The one-paragraph state

`circumspicere` found the class above all eighteen inward wards: **every instrument in this repo
measures `wat` as a library under cargo; nothing measures the program the user runs.** Not one of
5420 tests enters `distribution::run_with_args`. Four entry modes each run a different *prefix* of
the process setup and two return before the stack raise — so `--check` green-lights programs that
cannot start and aborts on programs that run, and the out-of-box stack failure is a bare silent
segfault. Eighteen inward lenses could not see it because **ordering is not a line**.

The recommended first strike is not a row: **hoist the stack raise and signal wiring above every
mode return, so a mode that skips them cannot be written**, then one gate that drives the real
binary in all four modes and asserts they agree on verdict and exit code.

## Gates run against this directory

It adds 10 `.wat` under `docs/arc/`, which **is** a gated tree
(`every_docs_wat_loads_or_declares_why_not`). Run at write time, green:

```
Summary [130.837s] 12 tests run: 12 passed, 5429 skipped
```

`docs_wat_loads_or_declares_why_not` · `no_unknown_ward_rune` · `every_wat_scripts_file_loads` ·
`every_rete_name_in_wat_scripts_code_resolves`. One file needed a declaration —
`probes/probe_vig_phantom_p5_unreserved.wat` carries `rune:lint(red-by-design)`, because failing to
load is precisely what it proves.

---

> ⛔ **You did not live the session this directory describes.** It is a record, not your memory.
> Every row is a claim until you re-read the `file:line` it cites — and `experiri` refuted four
> leads in this very cast, two of them the wards' own stated mechanism. Run `recolligere` against
> the disk before you act on any of it.
