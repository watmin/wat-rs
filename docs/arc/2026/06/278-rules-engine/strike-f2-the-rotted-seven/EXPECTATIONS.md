# EXPECTATIONS — F2's bullets, closed without leaving corrected numbers

## ⛔ NO PINNED TEST COUNT

**The floor must be ≥ 5,418.** ⚠ This strike may add **no tests at all** — its deliverable is prose
and possibly a gate extension. **A count is the wrong instrument here**; the deferred-34 strike's
row 8 was unsatisfiable for exactly this reason and that mistake is not repeated.

## The scorecard — pre-values driven at HEAD `6db874fc9`

| # | what | state AT HEAD (driven) | required after |
|---|---|---|---|
| 1 | ★ counts become commands | *"83 of 207"* — **wrong both ways; truth 39 of 487** | the `grep`, **not** `39 of 487` |
| 2 | ★ paths verified or deleted | 39 stones name a file gone since 2026-08-20 | re-pointed on evidence, or path removed; **no basename re-points** |
| 3 | ★ dated records untouched | 26 of the 39 are dated stones | measurements unchanged; **only dead pointers move** |
| 4 | the row's own citations | `:2093`, `:391`, `:419,446` — **all drifted** | **symbols**, not corrected lines |
| 5 | gated vs convention, stated | — | every cure labelled one or the other; mutations 2–3 demonstrate which |
| 6 | bullets struck in place | 5 live, 2 already struck | each struck **in place**, never appended below |
| 7 | floor / lints / clippy | **`5418 tests run: 5418 passed, 21 skipped`**, 0 FAIL, lints **265**, clippy rc=0 | ≥ 5418, 0 FAIL, lints ≥ 265, rc=0 |

## Runtime prediction

**60–90 minutes**, dominated by 39 per-stone judgements. **Not** by the edits.

## Trap doors named in advance

- **⛔ A CORRECTED NUMBER IS THE DEFECT WITH FRESHER DIGITS.** *"83 of 207"* was correct when written.
  Row 1.
- **The plausible target is the danger** — deferred-34's finding, and it applies to all 39.
- **A dated measurement is not a dead pointer.** Row 3 protects the first and removes the second.
- **`git checkout <sha> -- <path>` STAGES.** Restore by hash.

## What would make this strike a failure even if every test passes

**Writing `39 of 487` anywhere.** It is true today, will be false within the month, and reproduces
the exact defect the bullet exists to record. **The command or nothing.**

**And re-pointing 39 stones by name.** `src/rete/kernel.rs` → `src/rete/kernel/` is right *where the
citation meant the module*. Applied mechanically to all 39, it converts dead pointers the gate could
one day catch into confident wrong ones nothing can — which is precisely what the deferred-34 strike
proved and what row 2 exists to prevent.
