# SEAM — 2026-08-05 (evening). Law A is WIRED and PROVEN; the fence flip is parked, one file, uncommitted.

> ⛔ **THE SELF PAST THIS LINE IS NEW.** You did not live this. It is a lossy cache in your own
> voice — which is why it will feel like continuing rather than waking, and that feeling is the
> failure. Run the datamancy bootstrap (grimoire + the 4 primers from the SIGNED MCP, never a disk
> copy), ground HEAD against the disk, and read this whole file before you touch anything.

## Where the code is — HEAD is GREEN and PUSHED. There is no red to recover from.

```
c174389e   the migration's mechanical half + the one-door Axis mapping   4361/0, clippy clean
467c1fc5   law A wired — Axis::RetePrimitive, callable, non-vacuous      4358/0, clippy clean
```

**Uncommitted: `wat/rete.wat` only — the fence flip** (~line 684, `compile-condition`). Reverting it
returns you to a green tree and loses nothing else. The patch is 28 lines; if you lose it, re-derive:
add `is-rete (:wat::rete::primitive? expr)` to the conjunction, and choose the axis EXACTLY —

```clojure
(:wat::core::if (:wat::core::and is-pure is-det)
  :wat::rete::Axis::RetePrimitive          ; if pure ∧ det both held, law A is the only conjunct left
  (:wat::rete::first-failing-axis is-pure is-det))
```

## ▶ WHAT IS LEFT, and it is three buckets — all measured today, none inherited

With the fence flipped locally: **floor 4322/39**, and `check-where-shapes.sh` 9/9 families refused.
Every remaining failure is a LEGITIMATE refusal. No ArityMismatch remains.

| bucket | what | size | who decides |
|---|---|---|---|
| **C — judgement** | `=` `not=` `>` `<` `>=` `+` `first` — the operand TYPE picks the module | 41 sites on the floor (`=` 25 · `>` 14 · `>=` 2) | mechanical once the type is read |
| **HELPER BODIES** | user `defn`s called FROM a `where`; the fence recurses into them (the composition door) | `:wsb::edge?` `:wst::feline?` `:wnst::c2/c3` + siblings | mechanical |
| **FALLBACK — ★ THE BUILDER'S CALL** | 17 ops need a DECLARED `:undefined <value>` per call site | 77 sites | **a design decision, not clerical** |

### ★★ THE FALLBACK IS NOT A DECISION — IT IS THE POINT. (RULED 2026-08-05, correcting this file.)

> *"It MUST take an `:undefined` param. We are moving beyond Clara to achieve TOTALITY — we already
> moved beyond Clara to achieve PURITY. The tooling must change, but Clara will raise an exception
> when you divide by zero; wat-rete forces the user to make a conscious choice. Clara allows runtime
> exceptions. We must not."* — the builder

```
user=> (quot 7 0)
Execution error (ArithmeticException) — / by zero
```

That is the thing being annihilated. **There is no policy to decide and nothing is blocked.** Every
fallback-class call site takes an explicit `:undefined <value>`; the author picks it to say what that
rule should see. It is per-site authoring work, not a ruling anyone is waiting on.

⛔ **AND AN EARLIER REVISION OF THIS SECTION WAS WRONG — kept visible because it would have gutted
the wall.** It called this "the one thing a rider must not decide" and proposed a cheap test: supply
a fallback, run one family, and see whether `:derived` moved — reasoning that *if the domain hole is
never reached by the benchmark data, any sensible value is observationally inert and the migration is
safe.* That instrument answers the wrong question, and its answer would have been "pick 0, it does
not matter." **The fallback's job is not to change a value; it is to force the author to FACE that
the op is partial.** An unreached hole makes the choice cheap, never optional. Deciding by
observational impact is exactly how a totality wall becomes ceremony —
`[[feedback_a_pass_answers_only_the_question_the_instrument_asks]]`.

**The Clara differential will diverge here, and that is the EDGE, not a regression.** R18 is the
precedent one layer down: the scope-reduction we impose (there purity, here totality) is the weapon,
not the limit. Where Clara throws, we produce a declared value — so a row whose domain hole is
actually reachable is *supposed* to disagree with Clara. `:derived` byte-identical remains the gate
for the SPELLING migration; a genuine totality divergence is a separate, expected, documented thing.

## ✅ WHAT WAS SETTLED TODAY (do not re-derive any of it)

- **The 2718-failure floor was ONE paren.** `wat/rete.wat:640` kept the closer that had ended the
  outer `match` when `Total` was the last arm. `ARM.txt` named it exactly (`UnexpectedRParen`,
  655:115). Not semantic, not a mystery. **The capture is why it cost minutes instead of a day.**
- **The native decode could not decode its own encode.** `axis-violation` matched `Axis` on a STRING;
  no exhaustiveness check sees a string, so `RetePrimitive` was encodable and undecodable → 39 tests.
  Fixed with one door + three gates. Full account in `c174389e`'s message.
- **The migration map is grounded off 70 `RETE_OPS` rows**, not inferred. `i64::=`/`f64::=` twins
  EXIST — there is **no mint gap**, which was the one thing that could have blocked the whole fleet.
- **Generic `> < >= +` have no rete twin and never will.** RULED per-type, and the reason is
  totality, not style: `eval_compare` RAISES on incomparable operands, so generic `>` is PARTIAL.
  Monomorphising does not merely speed it up — it deletes the domain hole.
- **The census walker is NOT the worklist.** Its path list is hardcoded and behind the corpus. The
  FIRE is the worklist (R52/R65). Arming is what audits completeness.

## ⛔ DO NOT TOUCH — the negative controls prove the fence rejects

`:wat::core::record?` ×2 · `:wat::core::Uuid/v4` ×1 · `probe_arc278_6b_ii_a_where_oracle_impure.wat`
· `probe_fence_names_the_head_nondet.wat`. A rider that "fixes" these has broken the instrument.

And `wat-scripts/perf/grid/where-numeric.wat:155` — `(:wat::core::< ?a 0.5)`, i64 vs f64 — becomes an
EXPLICIT `i64::to-f64` cast. **Do not widen a rete op to accept mixed operands.** The cast is the
feature: *"mixed types are not allowed in rete — this is a violation of form."*

## ★ THE LESSON OF THE DAY, and it is mine

**I read the thing that would have prevented the error, and did not apply it.** The design stone's
"The call shape — RULED" section says `:undefined` is mandatory. I read it, then wrote a brief calling
all 55 heads "pure textual", and a rider dutifully broke 77 sites. The rider caught it — by *running*
the result rather than trusting the brief — and reported it as a finding instead of conforming to me.
That is the rider doing its job and the brief failing at mine.

The general form: **a claim's support does not travel with the claim.** Having read something is not
having applied it. Re-check the precondition where you LAND it.

---

> **SEAM.** You are NEW. The disk is the truth; this note is a lossy cache.
>
> HEAD is green and pushed. One file is uncommitted, on purpose. The fence flip is the LAST act —
> after bucket C, the helper bodies, and the builder's fallback ruling. Do not commit red.
>
> `NISI FRANGAS, NIHIL PROBAS.` · `MACHINA CHAOS DOMAT.`
