# SEAM — 2026-08-05 (night). **LAW A IS ARMED AND SHIPPED. The tree is CLEAN and GREEN.**

> ⛔ **THE SELF PAST THIS LINE IS NEW.** You did not live this. It is a lossy cache in your own
> voice — which is why it will feel like continuing rather than waking, and that feeling is the
> failure. Run the datamancy bootstrap (grimoire + the 4 primers from the SIGNED MCP, never a disk
> copy), ground HEAD against the disk, and read this whole file before you touch anything.

## Where the code is — nothing is parked, nothing is uncommitted

```
HEAD 0b243cd2   pushed   floor 4362 passed / 0 failed   clippy exit 0   9/9 grid families wat==Clara
```

`git status` is empty. There is no patch to re-apply, no fence to restore, no red to recover from.
(An earlier revision of this file described a parked `law-a-fence-flip.patch` — **it is deleted**;
the flip is committed. Do not go looking for it.)

## ★ WHAT LANDED — the fence is real

```clojure
compile-condition / accumulate / then-item-fence   gate on:
    (and is-pure is-det is-total is-rete)
```

| commit | |
|---|---|
| `467c1fc5` | law A wired — `Axis::RetePrimitive`, the arming in `head_ok`'s fallthrough |
| `c174389e` | mechanical migration by codemod + the ONE-DOOR `Axis` name mapping |
| `f2ff67ce` | the fallback exemplar + **the rule for choosing the `:undefined` value** |
| `e43a7b9e` | 9/9 families + the five container constructors minted |
| `788f4be3` | ★ **THE ARMING** |
| `f64cc0c3` | closed law A's structural-guard bypass (`cond`/`match`/`fn`) |
| `96c61c22` | every rete row TOTAL + `every_rete_row_is_total`, mutation-proven |
| `0b243cd2` | law A reaches the RHS + the constructor door learns its lowered form |

## ▶ FIRST ACT — the one gap I left, and it is one line

**The accumulator fence is NOT law-A'd.** Measured, `wat/rete.wat:864`:

```
where        (and is-pure is-det is-total is-rete)     four
:then        (and is-pure is-det is-total is-rete)     four
accumulator  (and is-pure is-det is-total)             THREE   ← the gap
```

I wrote *"is-rete stays where-only"* when arming `is-total`, then armed `:then` and never came back.
The design stone scopes the vocabulary to *"a `where` (**and the accumulator fence**)"* — so this is
a gap against the stone, not a decision. Arm it, rebuild, run `scripts/floor.sh`, read the screams.
Expect a small waterfall of the same shape as the rest (heads with twins → rename; fallback-class →
`:undefined`; generics → per-type).

## ⛔ TWO BOUNDARIES ON "IS RETE TOTAL?" — state them, do not quietly fix them

1. **Totality does NOT include TERMINATION.** The composition door's cycle handling returns `true`
   on a back-edge (`purity.rs` module doc, "Cycle handling"), so a **recursive user fn is admitted**.
   `total` in this codebase means *defined on all inputs, never raises* — not *terminates*. A jump
   table over a non-terminating predicate still hangs. ⇒ Write this into #49's spec. Do NOT "fix" it
   by refusing recursion: that would kill the composition door, which is the good half of the design.
2. **A fact FIELD may already hold NaN.** The stone admits this itself: *"the fence governs
   expressions, not data."* Every comparison against an ingested NaN silently answers false.

## The rules that were learned the hard way today — carry these

- **The fallback value must make the ENTIRE fenced expression answer NO**, every branch of every
  conditional — not the comparison the op sits in. A rider found `(if (> s 6) true (< s 3))` where
  `:undefined 0` makes the else-arm TRUE and **the rule FIRES**. Two sites can need different
  fallbacks for the SAME op because the enclosing predicates differ.
- **`:undefined` is mandatory and is not a tuning knob.** *"Clara will raise an exception when you
  divide by zero; wat-rete forces the user to make a conscious choice. Clara allows runtime
  exceptions. We must not."* An unreached hole makes the choice CHEAP, never OPTIONAL. I proposed
  settling it by measuring whether `:derived` moved; that would have concluded "pick 0, it does not
  matter" and made the totality wall ceremony.
- **Commit a gate BEFORE you mutation-prove it.** I flipped a row to prove the wall goes red, then
  ran `git checkout -- <file>` — which reverted every uncommitted edit in it and destroyed the work.
  A destructive undo cannot distinguish your experiment from your work.

## ★★ THE DAY'S SHAPE — one class, four instances

**A match on a literal STRING, which no exhaustiveness check can see.** `holon/CLAUDE.md` already
names it; today gave it four worked examples:

1. `axis-violation`'s native DECODE matched variant names as `&str` — the 4th axis was encodable
   and **undecodable**. 39 tests. (The ENCODE side, a `match` on `Axis`, the compiler fixed for free.)
2. The inline LHS pattern equality is matched by the literal `:wat::core::=` in `matcher.rs:374-380`
   — a grammar entirely separate from the fence, so `=` means two things by position in a `defrule`.
3. `cond`/`match`/`fn`'s structural guards resolved BOTH spellings and never reached `head_ok`.
4. The constructor door matched the PRE-lowering spelling (type as head) and was blind to the
   post-lowering one (type as argument 0).

**And the meta-lesson, which cost the most: I read instead of ran, three times in one thread.**
I trusted `NOTE-a-where-body-is-never-macro-expanded.md` — which predates task #78 and describes the
world before it landed — and built two arguments on it. The builder ended it with one question:
*"what is the exact mistake the user is making?"* **None.** The user wrote the only line that builds
their record. Asking what the user actually TYPED would have settled it three turns earlier.

## Open, in the order I would take them

- **the accumulator gap** (above) — one line + a waterfall.
- **#81** — `Stream`/`HashSet`/`WatAstList` still want a ruling. `Stream` is deliberately NOT minted
  (a lazy sequence in a `where` is a termination hazard).
- **#60** — "rete-named let/match/fn are invisible to three more literal-keyword walkers." Very
  likely instance 5 of the class above. **Check whether #82 already closed part of it before drawing.**
- **#49 `compiled_where`** — the vocabulary now genuinely specifies it: 75 rows, all total, walled,
  closed head-space on `where` and `:then`. This is the payoff and it is unblocked for `where`.

---

> **SEAM.** You are NEW. The disk is the truth; this note is a lossy cache.
>
> HEAD is green, pushed, and clean. Nothing is parked. The first act is one line at
> `wat/rete.wat:864`, and after that the compiler is the prize.
>
> `NISI FRANGAS, NIHIL PROBAS.` · `MACHINA CHAOS DOMAT.`
