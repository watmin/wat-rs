# BRIEF — rank the three fanout leftovers

## The work

Print which of harvest / compiled-rhs / OUT freeze
owns fanout `[40000]`. Do not change the engine
this stone. Census world has no query; the grid
does. That is the first measurement.

## Read in order

1. Grid `T20-37-11Z` — fanout `[40000]` wat-ns **58.1**.
2. `fanout_fire_phase_census` `[100 20]` FIRE **28.8**.
3. `DESIGN-STONE-fanout-three-leftover.md`.

## Sketch

```
A  harvest:query + out:query   (grid has q-Pair; census does not)
B  prod:compiled-rhs net       (40k Pairs, 2l pile)
C  out:production              (40k freeze, 2u: no drop-in)
```

Fire-only Instant, seed outside. Mean of 3.

## STOP

1. **STOP-1** — intern off production raw 19.6
   (2p instrument).
2. **STOP-2** — intern `names` / skip stamp /
   rewrite `seen` / Session-Vec / 297.
3. **STOP-3** — gate FIRE on a wall.

## Done when

- Table printed. A / B / C named. Next intern
  is the largest drawable ≥ 1 ms.

## Weigh (2026-08-22) — LANDED

A delta **39.22**. B 3.07. C 3.61. Intern is
class-scan harvest.

Leave dirty unless asked to commit.
