## TEMPERARE — Cast Report (TARGET 2 — the compile side and its spec)

> Written verbatim as returned, before any synthesis. Evidence only — this file carries NO status.

**SCOPE.** Read all 25 target files in full this session. I personally read `matcher.rs`, `mod.rs`, `compiled_cond.rs`, `compiled_rhs.rs` end-to-end plus targeted sections of `purity.rs`, `where_tree.rs`, `alpha_tree.rs`, `eval_insert.rs`, `compile.wat`, `acc.wat`, `rete.wat`; four forked sub-sweeps covered the remaining files, and every finding I did not personally ground was re-verified by me against the cited `file:line` before inclusion.

**Fire-vs-freeze separation, re-derived rather than trusted from the cast note:** the cast asserted `matcher.rs`'s `eval_clause`/`resolve_operand` family *"IS reached at fire time."* Reading `mod.rs:56`, `compiled_cond.rs:6` and `:943-962` shows **this is not accurate for native production fire**: `mod.rs:56` states outright *"Native authority is compiled exec; `alpha_match_inner` is the oracle / differential,"* and `compiled_cond.rs:960-962` says *"On a real fire `match:calls` still reads zero because the round loop's step 1 is this function."* Grepping every caller of `alpha_match_inner`/`eval_alpha_match` confirms the only callers are `kernel/tests/*`, the wat-visible `:wat::rete::alpha-match{,-local,-under}` primitives, and benches — **none is the automatic per-fact fire loop.** The real per-fact fire path is `compiled_cond.rs`'s `exec_compiled_with_key_ids`/`exec_ops`, `compiled_rhs.rs`'s `exec_compiled_rhs{,_at}`, `alpha_tree.rs`'s `candidates_into`, and `where_tree.rs`'s `candidates`/`walk` (the last confirmed load-bearing by prior art 2T1: its `proven` flag gates the pure-cmp fast path at `kernel/fire/mod.rs:2284`). **This correction changes the cost-dimension of several candidate findings below from "n facts/rounds" to "n oracle/differential calls."**

### FINDINGS (7 scored, 1 flagged-unconfirmed, 1 cost-lens-only)

**1. `where_tree.rs:29,41,84,145,159,225,294` — sibling asymmetry, real fire dimension.**
`where_tree.rs:84` types its equality fan-out as plain `std::collections::HashMap<Value, Arc<WhereDiscNode>>` (import at `:29` pulls only `std::collections::{HashMap, HashSet}`, no `rustc_hash`). Its structural twin `alpha_tree.rs:56` already made the identical fix — `type EqChildren = FxHashMap<Value, Arc<AlphaDiscNode>>` — with a measured rationale at `:66-67`: *"FxHash — SipHash of the field `Value` 40k times was the I−G walk (`DESIGN-STONE-alpha-tree-fxhash`)."* Dimension: **n tokens** — `WhereTree::walk` (`:276-318`) does one `node.children.get(&v)` SipHash lookup per tree level per token, and per prior art 2T1 this walk is on the real native-fire path, not oracle-only. Trip count visible: one hash lookup per level × tree depth, per token, every fire round. Tempered direction: swap `:84`'s `EqChildren` (and the maps feeding it at `:41,82-83,145,159,225`) to `FxHashMap`, matching the sibling fix exactly.

**2. `eval_insert.rs:148` and `:170` — redundant pure call, dead fallback arm.**
`build_insert_fact` calls `sym.types().and_then(|t| t.get(type_keyword))` at `:148` to select `names`, then calls the **identical** lookup again at `:170` for `field_names` — same registry, same key, same `TypeDef::Aggregate` arm. Since control only reaches `:170` when the `:148` lookup already returned `Some(Aggregate(_))` (the `_` arm at `:150` returns early), the `_ => Arc::new(Vec::new())` fallback at `:172` is **unreachable dead code**. Dimension: **n calls to `build_insert_fact`** — interpreter/differential door only, so the honest population is oracle/differential-test calls. Tempered direction: `let field_names = names.clone();`, deleting the second lookup and its dead arm.

**3. `purity.rs:1613-1636` used at `compile.wat:450-461`, `:589-607`, `:742-772` — 4× redundant AST descent, compile-time bounded.**
Each fence site calls `pure?`/`deterministic?`/`total?`/`primitive?` as four separate wat primitives, each doing its own single-axis `classify_expr(ast, &[Axis::X], …)`. `classify_expr`'s `List` arm (`:1371-1394`) shows the axis loop (`:1387`) sits *inside* one recursive descent — the recursion into children happens once per call regardless of how many axes are passed, confirmed by `apply_rete_defn_contracts` (`:1710,1772`) already using `&Axis::ALL` for the same walk. So 4 wrapper calls cause 4 full tree descents where 1 fused call would do. ⚠ I checked this against the builder's 2026-08-05 ruling at `compile.wat:273-290` (*"is-pure → is-deterministic → is-total → is-rete… NOT collapsed even where one arguably implies another"*) — **that ruling forbids skipping an axis because another implies it; it does not address sharing one traversal across four independently-computed booleans**, so a fused walk does not contradict it. Dimension: **n where/accumulator/:then clauses × expression-tree size**, once per rule at freeze time.

**4. `wat/rete/compile.wat:1018-1065`, `sort-lhs` — redundant pure call, compile-time bounded.**
`uses-result?` (`:1018-1029`) is invoked once per condition in the `independent` fold's guard (`:1038`) and again per condition in the `rest` fold's guard (`:1061`) — same predicate, same list, complementary boolean, two independent recursive `ast-qvars` walks per condition. Dimension: **n LHS conditions per rule**, freeze-time, doubled. Tempered direction: classify each condition's bucket once in a single fold.

**5. `wat/rete/acc.wat:107-122`, `acc::distinct` — quadratic, oracle-only dimension.**
`foldl` + `PersistentVector/contains?` (linear scan) before every `conj`. Confirmed `kernel/arm.rs:285` recognises `":wat::rete::acc::distinct"` by name and dispatches to a native `AccFold::Distinct` — **native fire never runs this wat body.** Dimension: **n elements gathered by one `:accumulate` node**, in the oracle's interpreted accumulate pass, once per accumulator per fixpoint round on every floor run — real, but test/oracle traffic.

**6. `wat/rete.wat:508-541`, `render-dag` — potential quadratic string build, very low priority.**
The outer `foldl` repeatedly does `(string::concat acc line)` (`:539`), growing `acc` every iteration. ⚠ Note `:524-527` carries an *unrelated* `rune:exigere(scope-affirmative)` protecting the fixed-depth nested-concat chain that builds one `line` — **that rune does not cover the outer accumulator growth this finding is about**, so it is not already excused. Dimension: **n network nodes**; a debug/diagnostic renderer, not on the compile or fire path.

**7 (flagged, not scored — insufficient reachability evidence in scope). `wat/rete/factbag.wat:49-55,66-84,103-115`** (`add-if-absent`/`remove-one`/`count-of`) — each is one O(n) linear scan per call. Two independent sub-sweeps noted the identical shape and both stopped short of scoring it: the only caller found (`merge-facts`) lives in `wat/rete/oracle/*.wat`, **outside this cast's 25-file list**, so whether it's applied once per newly-derived fact (making the aggregate O(n²)) can't be confirmed from files actually in scope. Naming it for whoever next scopes the oracle files, not claiming it here.

**Cost-lens addendum on already-rowed prior art (not a new waste finding).** `matcher.rs:733-741` vs `compiled_cond.rs:1403-1412` — the `CmpKind` table rowed as duplication (2S1). Per the cast's invitation: `matcher.rs`'s copy runs only through `eval_clause`, confirmed oracle/differential-only above; `compiled_cond.rs`'s copy (`eval_cmp`, from `exec_op`'s `Op::Cmp`/`Op::SeedCmp` arm) runs inside `exec_ops`, which `exec_compiled_with_key_ids` calls **per fact per compiled alpha condition on the real fire path**. Not a compute-waste finding by itself (both O(1) dispatch) — a note that a future edit touching only one copy would silently diverge **on the side that actually matters at scale**.

### Rune verdicts

Exactly one `rune:temperare` exists across all 25 files: **`purity.rs:1782`**, category `simplicity-win`, reason *"cycle is a second question (#87 recursion), not a fifth fence axis; merging it into classify_expr would complect them."* Judged against the model form (`fire/mod.rs:494`: dated, names its floor run, measured ratio 206/245583=0.084%): **weak** — `simplicity-win` requires citing a cost ceiling per the spell's own format rule, and this rune gives an architectural-separation argument with no date, no measurement, no ceiling. Low stakes in practice: the redundancy it excuses is compile-time-once-per-declared-`rete-defn`, human-bounded — I would ask for a better-formed rune, not force a fix.

### Prior art — not re-reported

`CmpKind` duplication (2S1, cost-lens only above), `export.rs` pack/unpack asymmetry (2S2), compile-rule/compile-query duplication (2S3), `matcher.rs`'s `sym: Option` never-`None` (2P3), `AxisViolation` unread fields (2P7 — checked for a cost lens: both fields populated only on a freeze-time refusal path, near-zero population even in a failing program, no fire dimension to add), `where_tree.rs`'s `walk` doc (2T1 — used as evidence *for* finding 1's fire-reachability, not re-rowed), the stale counts/labels, `compiled_cond.rs`'s test-fixture gap. `export.rs`'s documented O(n²) import build (capped by `MAX_IMPORT_NODES`) and `compiled_cond.rs`/`compiled_rhs.rs`'s own module-doc-described fixes (measured *"1.1% of a fact-heavy fire,"* *"240,000 heap allocations"* eliminated) were confirmed already substantively tempered. `compile.wat`'s whole-network-rebuild fold shape was not re-flagged per the binding scope ruling.

**FINDINGS** — 7 scored (1 fire-dimension, 6 compile/oracle-dimension), 1 flagged-unconfirmed, 1 cost-lens addendum, 1 rune judged weak.
