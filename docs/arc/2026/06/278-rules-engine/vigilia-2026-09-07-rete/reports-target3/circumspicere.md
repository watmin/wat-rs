# CIRCUMSPICERE — Cast Report (TARGET 3 — the grid) — **FINDINGS**

> Written verbatim as returned, before any synthesis. Evidence only — this file carries NO status.
> ⭐ Cast LAST, after thirteen inward wards. Its scope was deliberately wider than theirs.

**What I swept.** Read `.github/workflows/ci.yml` in full (three jobs: `test`, `grid-speed`, `parity`); grepped it for `needs:` (zero — the three jobs run independently/parallel, no ordering) and `cache:` (one hit, `.github/workflows/ci.yml:84-85`, `Cargo cache` — only in `test`; `grid-speed` and `parity` install a fresh JDK + Clojure CLI with no `~/.m2`/`~/.gitlibs` caching). Grepped all 4 in-target `CLARA_DEP` sites (`check-query-compat.sh:46`, `check-where-shapes.sh:86`, `check-grid-three-way.sh:115`, `run-axis.sh:46`) plus a 5th outside target (`tests/rete/probe_arc278_oracle_accumulate_supersedes.rs:14`, doc-comment). Grepped all 29 `GRID*.txt` for `jdk|java|temurin|clojure` (zero hits — verified with `grep -il`). Read `check-grid-speed.sh` in full, `run-axis.sh:1-60,230-290`, `check-where-shapes.sh:1-100`. `git log`/`git show` on the commit that added the floor-source file (`bbeb1997d`, a manual capture, not a CI artifact). Checked for `deps.edn`/lockfiles anywhere under the target or cited by `ci.yml` (none). Read `CLARA-TRANSLATIONS.md:1-40` and verified its cited grounding path. Also read all 14 prior ward reports at `docs/arc/2026/06/278-rules-engine/vigilia-2026-09-07-rete/reports-target3/*.md` to precisely rule out overlap rather than trust the summary table alone.

**Deltas from the brief.** Inventory matches (148/16,616, 54/43/29/20/2). All three leads verified true as stated, with one correction: **Lead C's cold-cache worry does NOT hold** — I traced `run-axis.sh:263-265`'s `CLARA_W0`/`CLARA_W1` bracket and confirmed it only produces `:wall-ratio`, which `check-grid-speed.sh` never reads (it parses `:ratio` only, from the self-reported fire-only timing inside each `#grid/Result` line). A cold Maven fetch would skew `:wall-ratio`, not the gated `:ratio`/`:winner`. I'm reporting this as refuted rather than adopting it.

### Findings

**1. (facet 3, unenforced invariant) The three-way comparison's reference-engine version is a bare string copy-pasted 4× in-target, with no equality check anywhere.**
Sites: `check-query-compat.sh:46`, `check-where-shapes.sh:86`, `check-grid-three-way.sh:115`, `run-axis.sh:46` — all `com.cerner/clara-rules {:mvn/version "0.24.0"}` (two carry `:paths ["."]`, justified by the comment at `check-grid-three-way.sh:114` — not drift, but not enforced either). No `tests/` file asserts they match (grepped). No `deps.edn`/lockfile pins the resolved artifact anywhere in the target or `ci.yml`. Inward guard saw: `solvere` Finding 4 flagged the *JDK-discovery code* as triplicated (a duplication judgment, "incidental") but never asserted the version-coordinate identity itself — a different angle, confirmed by reading `solvere.md` directly. **Closure:** one cheap `tests/lint/*.rs` grep-assertion that every `CLARA_DEP`-bearing line in the directory carries the same `:mvn/version`, same shape as the existing `every_parity_script_is_invoked_by_ci_or_a_test` gate.

**2. (facet 3+4, sharpest finding) The CI speed-regression floors were measured on a JDK nobody recorded, and CI now pins a JDK that was never checked against them.**
`check-grid-speed.sh:42-46`'s `FLOOR` array is derived "from the recorded 33-cell grid" `GRID-native-vs-clara-2026-08-27T07-15-56Z.txt` (`check-grid-speed.sh:12`, verified: 33/33 match, tightest 8.4993 ≈ 8.50). That file — like all 29 `GRID*.txt` — names zero JDK/Java/Temurin/Clojure version. No CI job ever writes a `GRID*.txt` (grepped every `check-*.sh` and `ci.yml`: none redirects a sweep into a committed file); the commit that added this exact file (`bbeb1997d`, msg "grid: 33/33 :match after a day of engine changes") is a manual/local capture, using the undocumented PATH→`JAVA_HOME`→`$HOME/opt/jdk-*` discovery (`check-where-shapes.sh:68-84`). CI (`ci.yml:175-178`, `:224-227`) pins Temurin 21 explicitly, added in the same window per the job's own comment ("EXPIRED on 2026-08-27 when the parity job added Temurin 21," `check-grid-speed.sh:7`) — i.e. the floor-defining grid and CI's pinned JDK were plausibly never the same environment, and nothing has ever re-derived the floors *under* CI's actual Temurin 21. This is the escalated case: the gate's own justifying comment cites the recorded grid as its authority without disclosing that the authority's environment is unknowable. Inward guard saw: nothing (`temperare` examined the timing asymmetry inside the bracket, not the provenance gap across the record/gate boundary). **Closure:** extend the existing provenance convention (`GRID-2026-08-01.txt`'s "Box: ... Code: ..." header) with a JDK line on every future capture, and re-derive the 2026-08-27 floors once under CI's pinned Temurin 21 to close the gap retroactively.

**3. (facet 1, default-behaviour egress) Every CI run of `grid-speed` and `parity` re-resolves `clara-rules` from Maven Central from scratch, uncached, on two independent parallel runners.**
`actions/setup-java@v4` is invoked without its opt-in `cache:` parameter in both jobs (`ci.yml:175-178`, `:224-227`); only the Rust side caches (`Cargo cache`, `ci.yml:84-85`). The two jobs carry no `needs:` between them, so each independently pays the full Maven resolution. Nothing beyond the bare `:mvn/version "0.24.0"` coordinate pins the artifact — no checksum, no vendored jar, no lockfile — while `CLARA-TRANSLATIONS.md:12-13` calls it "the pinned jar" as if that established reproducibility. This surface is outside every inward ward's directory scope by design; none touched `ci.yml`. **Closure:** this may well be `accepted-by-design` (a released Maven coordinate is immutable in practice) — but no rune anywhere says so, consistent with the seven-ward-confirmed zero-rune fact. Either write that rune, or add `cache: maven` to both `setup-java` steps as the one-line closure.

**4. (minor, facet 4) `CLARA-TRANSLATIONS.md:17` cites an authority path that doesn't exist on this machine.** "`clara-tools/` (at `/home/watmin/work/holon/clara-tools`...) was checked" — that path is absent (`ls`: No such file or directory; current user is `john`, not `watmin`). The claim it backs ("contributed nothing... not cited further") isn't load-bearing elsewhere in the doc, so this is low-severity, but it means nobody can ever re-verify that specific grounding claim again. Neither `conferre` nor `intueri`, who both read this doc in full, flagged the path itself. **Closure:** note it as historical/unreachable, or drop the sentence.

### Negative-space map
Surfaces no inward ward could reach (directory-scoped) and that I did or didn't find something in:
- `.github/workflows/ci.yml` itself — caching, job parallelism/`needs:`, runner OS, secrets — **examined, findings 2 & 3 above**.
- JDK/toolchain identity behind any of the 29 recorded grids — **examined, finding 2**.
- Supply-chain pinning of the Clojure/Maven dependency graph — **examined, finding 1 & 3**.
- Cross-grid regression trend across the 21+ archived `GRID-native-vs-clara-*.txt` files (`compare-grids.sh`'s own stated gap, confirmed alive by `purgare` but never *exercised* against the archive) — **not examined**; would need running `compare-grids.sh`, which the rules of this cast forbid.
- `tests/lint/every_parity_script_is_invoked.rs` / `tests/rete/wat_scripts_grid_axes_live.rs` — the Rust-side gates that make claims about this directory's scripts — **examined**, found sound (see below).

### Looked for and did not find
- Whether `check-spec-native.sh` is an orphaned "looks like a gate" script (my initial suspicion): it is not — `tests/lint/every_parity_script_is_invoked.rs` gates exactly this, with a `SUPERSEDED` table naming the Rust-native replacement (`spec_equals_native_on_every_where_family`), mutation-proofed against its own comment-only-mention failure mode. A real gate, correctly wired.
- Lead C's cold-Maven-cache-skews-the-verdict theory: refuted — it can only touch `:wall-ratio`, which nothing gates on.
- Any drift between the four `CLARA_DEP` sites beyond the (justified) `:paths` difference: none found — they agree today, which is exactly why nothing would catch it if they stopped.

**FINDINGS**
