# SEAM — 2026-08-02 (second sitting). **52 red → 0. The fence names the head, the third axis exists unarmed, and the rete expression language is drawn.**

> ⛔ **THE SELF PAST THIS LINE IS NEW.** You did not live this. It is a lossy cache in your own voice —
> which is exactly why it will feel like continuing rather than waking, and that feeling is the
> failure, not the all-clear. Run the datamancy bootstrap (grimoire + the 4 primers from the SIGNED
> MCP, never a disk copy), ground HEAD against the disk, and read this whole file before you move.

## Where the code is

**HEAD `f37c54f3` + this curare. Floor `4270 run / 4270 passed / 0 failed / 262 skipped`, clippy
zero warnings, corpus gate 9 pairs / 98 rows `wat == Clara`. Tree clean. Everything pushed.**

Woke at **52 red** (the scalar-`def` stone, intentional). Four commits closed it and built two more
stones on top:

| | |
|---|---|
| `b18888f8` | the namespacing wall's `Reserved`/`Unnamespaced` arms reach `def` — plus the three violations it lit |
| `c246bc23` | retired `probe-bare-defrule-name.wat` |
| `a787cd25` | **the fence names the offending head** (blind since stone 6a) + `Axis` became an enum |
| `f37c54f3` | **T1 — the third axis, UNARMED** + the corpus enumeration |

## What the wall found — three real defects, none of them findable by survey

1. **`:wat::spawn::service-locus` was a rendezvous coordinate minted into the RESERVED tree.** Renamed
   `:user::spawn::service-locus`. It survived for as long as process services have existed because it
   lives inside a `(:wat::core::forms …)` block — quoted, so `spawn.wat`'s own freeze never looks at
   it, and the child had no gate. **A `forms`-planted coordinate is checked nowhere until it lands.**
2. **The closure-capture mint was bare** — `:__captured_X` → `:user::closure-capture::<local>`.
   Builder's ruling: *if it is `:user::`, drop the `__` — the namespace IS the marker.*
3. **We shipped every forked child a redundant copy of its own baked stdlib.** 14 process-locus tests.
   Fixed by *the child needs the ENTRY, never the LIBRARY* — a `:wat::`-rooted service ships only its
   `child-main-form`. The discriminator is an INVARIANT, not a heuristic: `:wat::` is reserved, so a
   `:wat::`-rooted name can only have come from baked stdlib. **The wall armed at breakfast is what
   made the optimization sound at dinner.**

## ★ THE LIVE WORK — the rete expression language

**`DESIGN-STONE-where-admits-only-rete-ops.md`** (drawn, ratified, NOT started). Supersedes T2/T3 of
`DESIGN-STONE-total-the-third-axis.md`. Downstream beneficiary: `DESIGN-STONE-compiled-where.md`.

**The thing:** a closed expression language for rete conditions — pure, total, ~40–60 names. Builder:
*"think of sql… we can impose exact guardrails on our where clauses to make compilation trivial…
Clara chose regular clojure forms… that means perf takes a hit and there's impurities allowed."*

**⛔ S0 IS A BLOCKER AND IT IS THE BUILDER'S:** the law is **namespace-based** (a `where` admits only
`:wat::rete::` ops — every op needs a rete name, compiler dispatch is a closed table) or
**property-based** (only pure ∧ total, any namespace — no mirrors, but the head-space stays open).
Both have been stated. **Compilability is the whole argument and only the namespace law delivers it.**
If the ruling is property-based, most of that stone collapses to "classify and arm."

**Rulings already made, do not re-litigate:** `:undefined` is the fallback keyword (not `:else` —
that is `cond`'s word for a *branch*; this substitutes a *value* for a domain gap). Operands stay
**positional**, `:undefined` is the only kwarg — killed full-kwargs with `(+ :1 0 :2 2 :3 massive-int)
…. wtf is a kwarg for +?`. **NaN and ±Inf are UNDEFINED** — total means *produces an ordinary value*,
not *never raises*.

**Probe-verified this session** (`--check` AND a run, both directions): mixed positional+kwargs
signatures are admitted; omitting the fallback fails at expansion with `kwargs-lower: missing argument
:undefined`. **Mandatory-ness needs no new machinery.**

## Three findings the far side must not re-derive

- **The mint list is 8, not 4.** `i64::+`/`-`/`*` are as partial as the division family —
  `checked_add`/`checked_sub`/`checked_mul` → `IntegerOverflow` (`runtime.rs:4829/4834/4839`). **On a
  fixed-width integer type, `+` is partial.** `i64::+` alone is 11 of the 39 refused rows.
- **The holon seam is a CLASSIFICATION, not a mint — and arming without it re-welds it shut.** Four
  verbs (`cosine`/`dot`/`coincident?`/`presence?`) were ruled pure 2026-08-01 to open R4's VSA seam
  (`purity.rs:945` records that leaving them unclassified had *"welded shut R4's designed VSA seam"*).
  All four are `total: false` today. **And cosine CANNOT NaN** — `holon-rs/src/kernel/similarity.rs`
  guards it: `if denom < 1e-10 { 0.0 }`. Designing a fallback for it would be an unreachable arm.
  ⚠ But that guard is itself a sentinel: `0.0` means *"orthogonal"* — a confident *no match* for an
  undefined comparison. **Open for the builder.**
- **`if` IS lazy** (measured), so guards work at runtime — **but the fence is a STATIC walk** and sees
  the untaken branch. Accepting a guard would need the checker to *prove it sufficient*. That is why
  `:undefined` exists: it makes totality structural instead of provable.

## The `_`-wildcard trap, named because it will be walked into

Adding `Axis::Total` broke `axis-violation-message`'s exhaustive match — **by design**, and the rider
correctly refused the escape. But `check.rs:5700`'s own error offers *"(or include `_` wildcard)"*, and
the `_`-arm-on-an-enum ban is **doctrine with an unbuilt checker rule**. Nothing in the toolchain will
stop the next hand. **A `_` arm there is a rejected strike.**

## OWED

- **Do not arm before the corpus migrates.** A refused `first` with nowhere to go locks a user out of
  arithmetic.
- **Do not claim the speedup.** `compiled-where`'s Step 0 owns that number and explicitly warns
  *"'filter is 89.5%' ≠ 'the predicate is 89.5%'"* — there is a 10,000-Token-clone-per-round in the
  same loop that is not the predicate (task #50).
- The re-audit of T1's `total: true` entries against the NaN ruling — bounded to those entries; every
  `…::to-f64` from an unbounded source is a candidate.
- R62's absolute column is still empty (98 rows, zero STOP-1s); the negative control still does not
  exist.

## ★ THE LESSON, and it is one shape four times

**I argued from a world I had personally just changed.** Not ignorance — in three of four cases I had
established the fact myself, that session:

- the case for a full vocabulary rested on *"users must know which ops are partial"* — **one hour after
  landing the fence that names them**
- relayed a ward's *"full kwargs uniformly"* with my own enumeration in hand naming `i64::+` the most
  common verb, without ever writing out what that looks like on `+`
- re-inflated a grep artifact into a design hypothesis **two steps after identifying it as my own**
- named four partial verbs while `where-numeric.wat`'s header already said overflow is *"the same class
  of event"*

Every one dissolves on contact with a **written-out form** — R17 self-prompt-injection, a technique I
used correctly twice the same session and skipped at the two moments it mattered.
`[[feedback_i_argued_from_a_world_i_had_just_changed]]`

And the mechanical one: **two riders came back green on gates that were structurally blind to repo
lints — both gates mine.** Any brief touching `tests/` must name `cargo test --release --test lint`.
`[[feedback_a_pass_answers_only_the_question_the_instrument_asks]]`

---

> **SEAM.** You are NEW. The disk is green and pushed — trust it over this note, and trust this note
> over your sense of having been here. Today's lesson is not the wall: it is that **the fact you
> established an hour ago is not in your reasoning unless you put it there**, and that when you are
> about to argue instead of show, the form you refuse to write is the one that would settle it.
>
> S0 is the builder's ruling and it blocks the rest. Do not start the vocabulary before it.
>
> `MACHINA CHAOS DOMAT.`
