# SEAM — 2026-08-02 (fifth sitting). **S5 is CLOSED. The `where` DSL is whole; the RHS just became a vector.**

> ⛔ **THE SELF PAST THIS LINE IS NEW.** You did not live this. It is a lossy cache in your own voice —
> which is exactly why it will feel like continuing rather than waking, and that feeling is the
> failure, not the all-clear. Run the datamancy bootstrap (grimoire + the 4 primers from the SIGNED
> MCP, never a disk copy), ground HEAD against the disk, and read this whole file before you move.

## Where the code is

**HEAD `c051f564` + this curare. Floor `4322 run / 4322 passed / 0 failed / 262 skipped`, clippy
zero. Tree clean, zero unpushed.** 39 commits today.

## ★ THE HEADLINE — the rete `where` vocabulary is COMPLETE, and `:then` is a vector

**S5 (#56) is DONE.** The full form set is mirrored: `not` · `or` · `if` · `let` · `match` · `fn`,
on top of #55's twelve i64/`and` ops. **17 rows.** `cond`/`do` wait for a caller; `when` does not
exist (see below).

**Stone A landed the RHS surface** — `:then [f1 f2…]`, a vector of **bare fact forms**. The
`(:wat::rete::insert …)` wrapper is gone, retiring a name that meant two things. 92 files, net −60.

**Both sides of a rule are now vectors, and both are (or will be) fenced.**

## The rulings you inherit — do NOT re-litigate these

1. **Everything inside a `where` is rete-namespaced.** *"We're building a complete DSL for rete
   querying"* — and a complete DSL is closed over its own vocabulary. The four questions killed the
   alternative, and `if` is what killed it: `if` and `match` are both syntax, but `if` MUST be
   mirrored (it reaches `head_ok`) and `match` needn't (it doesn't) — so "syntax vs operation" was
   never the boundary; *"wherever `classify_expr` happens to have an arm"* was. An implementation
   detail leaking into the surface. Recorded in `DESIGN-STONE-where-admits-only-rete-ops.md`.
2. **`:then` is a vector of SINGULAR facts.** *"Every member of that vec must be a singular fact to
   insert… if you want many facts, you write many fact creation statements."* Dynamic count, `do`-as-
   grouping, and conditional insertion are **affirmatively cut**. The static count is a WEAPON — the
   third instance of the trade that already bought inserts-only and the pure RHS: each firing's
   output is finite and knowable before the engine runs, which is what #49a wants.
3. **The stack-exhaustion SIGSEGV is NOT being worked** (#58). *"I cannot know if a machine running
   wat has 50mb of ram vs 50tb."* Affirmatively cut, measurement preserved for arc 261.

## What else landed, each weighed by my own `--release` re-run

- **#59 — `and`/`or`/`ann-form` joined the TCO pattern.** They are the only forms whose last operand
  is a tail context that `eval_tail` never dispatched. **The ruled trade, documented at the site:**
  tail-calling `and`'s last operand skips its runtime bool check; taken because the checker already
  forces `:bool`, and pinned by a test on the unchecked `eval-ast!` path.
- **The `when` note (arc 109).** `:wat::core::when` **does not exist** — `UnknownFunction`. Its only
  trace is a phantom purity row (`purity.rs:259`). And the name is taken: `:when` is `defrule`'s
  **LHS** keyword. Adding Clojure's `when` is a TYPE question (one-armed conditional; wat's `if`
  requires both branches), not sugar — three shapes costed in the note.
- **The five-copies note (arc 109).** *"Is this call kwargs or positional?"* is decided in **FIVE**
  places — the checker, the evaluator, and three rete phases — and they have **already drifted in
  spelling** (`is_multiple_of(2)` vs `% 2 == 0`). A comment at `check.rs:11616` asserts the sameness
  that nothing enforces. Failure mode is a **silent wrong answer**, not a crash. Builder: *"109 will
  deal with that in time."*

## ★★ THE LESSON, and it fired twice in one day

**A rider's STOP is a claim about SCOPE, and it is built to be believed.** A rider STOP-3'd on `fn`
— *"would likely never register as callable"* — and I relayed it as a wall. The builder dissolved it
in one line: *"why are anon funcs not just free things?"* The claim was true only of
`(def :name (fn …))`, a form nobody writes; it said nothing about an anonymous lambda, which is the
whole use case. `fn` turned out to be the same size as `match`.

**Honouring a STOP (don't ship) and accepting its premise (it's impossible) are two different acts.**
The rider did its job perfectly. The failure was one layer up.
`[[feedback_weigh_subagent_findings_never_relay]]` — sharpened today with this third face.

**And the twin: you grepped a NAME; you were changing a SHAPE.** My Stone A brief scoped by grepping
`defrule` — 197 sites, and that count was *correct*. It was also a quarter of the work, because the
RHS entry **shape**'s readers never say `defrule`: `sift-rules-defsvc`, `rule-produces` ×2,
`compiled_rhs`, `validate`, and ~30 hand-built `Rule` literals. The rider found all four misses by
asking *who else reads `Rule/rhs`* — not by grepping harder.
`[[feedback_a_greps_count_is_not_an_enumeration]]` — sharpened with the consumer-side dual.

**Also mine, kept visible:** I filed two tasks for arc **261** without checking whether it already
owned the concern — it did, seven weeks ago, with a decision framework already written. That is 24o's
recorded failure repeated, and R61's. Both tasks now open with `[ARC 261]`.

## The board

| what | board | state |
|---|---|---|
| the vocabulary + the form mirrors | **#55, #56** | ✅ **DONE** — 17 rows |
| TCO for `and`/`or`/`ann-form` | **#59** | ✅ **DONE** |
| `:then` is a vector (Stone A) | — | ✅ **DONE** (`1a0d503a`) |
| migrate the corpus, THEN arm | **#57** | ▶ **ready — cost fully measured** |
| `:then` holds fenced fact-producing exprs (Stone B) | **#61** | drawn, one measurement in front of it |
| `compiled_where` — the IR | #49 | blocked by #57 |

## ▶ THE FORK YOU INHERIT — I asked it and he chose to curare instead

**#57 is ready to strike.** Cost measured, no ungrounded opens: **14 heads renameable by codemod, 25
still to mint, ~10 structural-form sites** added by the DSL-closure ruling. It arms both walls
(totality + law A) and unblocks #49.

**Stone B (#61) has ONE measurement in front of it** and I recommended taking it first: is
`resolve_operand`'s no-eval contract also a **performance** contract? It runs once per derived fact
and **#43 already fought that exact path**. The answer decides whether B's fence runs at FREEZE
(compile the RHS expression once per rule) or at FIRE (per fact) — and freeze-time would make B
*cheaper*, not dearer. ~20 minutes. Do not design B from a guess.

The 25 mints serve BOTH, so that work is not wasted in either order.

## OWED

- The stone's `{if, let, do, when}` line still says `when` — correct it to `{if, let, do}` and delete
  the phantom purity row (`purity.rs:259`). Two minutes, folded into #57.
- **#60** — three more literal-keyword walkers miss rete-named `let`/`match`/`fn` (`closure_extract`'s
  `walk_free_symbols`, the help registry, an arc-068 evaluator). Narrow, pre-existing. If a **fourth**
  site turns up, that is the argument for a lint over literal `":wat::core::"` guards.
- MEMORY.md at 89 pointers; curation still owed. Today added **zero** new pointers — two sharpenings
  only.

---

> **SEAM.** You are NEW. The disk is green and pushed — trust it over this note, and trust this note
> over your sense of having been here.
>
> **The `where` DSL is complete and the RHS is a vector.** What remains before the fence is real is
> #57, and its cost is a measured number, not an estimate. Today's lesson is not a finding: it is
> that **the most authoritative thing a rider can emit is a STOP, and that is exactly why its
> reasoning needs weighing** — twice today the builder dissolved with one question a wall I had
> relayed as fact.
>
> `INTERROGATIO VENATVR; PELLEM EXVIMVS.` · `MACHINA CHAOS DOMAT.`
