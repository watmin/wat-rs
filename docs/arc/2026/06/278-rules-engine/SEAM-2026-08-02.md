# SEAM — 2026-08-02 (third sitting). **A performance question became an honesty audit. R63 `INTERROGATIO VENATVR`.**

> ⛔ **THE SELF PAST THIS LINE IS NEW.** You did not live this. It is a lossy cache in your own voice —
> which is exactly why it will feel like continuing rather than waking, and that feeling is the
> failure, not the all-clear. Run the datamancy bootstrap (grimoire + the 4 primers from the SIGNED
> MCP, never a disk copy), ground HEAD against the disk, and read this whole file before you move.

## Where the code is

**HEAD `d704620b` + this curare. Floor `4279 run / 4279 passed / 0 failed / 262 skipped`, clippy zero.
Both repos clean, zero unpushed. 21 commits.** `holon-rs` also moved (`0dbb388`) — sibling repo, pushed.

## ★ THE LIVE WORK — read the BOARD, not this file, for the list

**The chain lives on the TASK BOARD and in the stone's strike-order table. This seam is not the tracker.**
Two numbering schemes exist and they do NOT line up; the stone's table is the only place they are
reconciled. **Always cite the pair — "#55 (S3b+S4)" — never one alone.**

| what | board | state |
|---|---|---|
| get `intrinsic_meta`'s `total` column honest | #52 (S1+S2) | ✅ done |
| ground which arithmetic path a `where` traverses | #54 (S3a) | ✅ done |
| **MINT the vocabulary + the module-set admission test** | **#55 (S3b+S4)** | ▶ **UNBLOCKED — next** |
| the form mirrors, two classes | #56 (S5) | blocked by #55 |
| migrate 39 rows, then ARM | #57 (S6+S7) | blocked by #56 |
| `compiled_where` — the IR | #49 | blocked by all |

**⛔ ZERO rete-module ops exist.** `grep :wat::rete::{core,i64,f64,string,holon}::` returns **0**. No
`compiled_where.rs`. `total?` is still UNARMED at the fence (`rete.wat:599`). Today produced **rulings,
findings and measurements** — the vocabulary is entirely undrawn, and #55 is where it comes into being.

### Two things #55 needs that were NOT in the plan

1. **`runtime.rs:4829` must be refactored onto the shared kernel FIRST.** #54 found the two arithmetic
   sites serve different dispatchers: a `where` reaches `:4829` (inline raise, **no seam**), while
   `:9753`'s clean `arith_i64_i64_inner`/`I64ArithErr` factoring belongs to `eval_apply`. Without that
   refactor `:wat::rete::i64::+` either duplicates the arithmetic or works only through `apply`.
   ⚠ Traced by READING, not running — confirm with a probe first.
2. **The builder's open cut question.** #55/#56 split by *mechanism* (funcs vs forms), which cuts his
   `wat.rete.core` module in half. Alternatives: by *module* (`core` entire, then the typed ones) or by
   *risk* (aliases · native fallbacks · forms). **His call, not yet made.**

## Rulings landed today — do NOT re-litigate

- **S0: law A, NAMESPACE-based.** A `where` admits only rete-module ops. The property-based alternative
  lost on compilability; recorded in the stone.
- **The closed set is a BASIS, not a ceiling.** `head_ok`→`classify_fn` already recurses, so a user
  `defn` over rete ops is admissible at any depth. The vocabulary is the alphabet, not the budget.
- **The admission test is the MODULE SET, not the root prefix** — `:wat::rete::` is already the engine
  API; a bare `starts_with` admits `fire-rules` inside a `where`.
- **Shared kernel, two surfaces** — a rete op is never a second implementation, only a second terminal
  handler. The name at the call site is the only place the contracts differ.
- **THE MEASUREMENT IS FULL; THE PREDICATE IS EXACT.** A measurement carries its undefined case as a
  faced value; a predicate stays a total `bool` answering the question asked. `0.0` from cosine is a
  *wrong answer*; `false` from `coincident?` is a *correct* one. The line against the NaN ruling: NaN's
  false is an **IEEE accident**, a predicate's false is a **documented contract**.
- **The match is the shield and is not laid down.** `:undefined` is rete-surface ergonomics only.
- **COMPILATION IS A FORCING FUNCTION FOR CORRECTNESS** (*"I will not accept 'we don't compile' as an
  excuse"*). Its value is not the speedup — a compiler demands total knowledge, so the audit is the
  deliverable. `PVGNANDO EMERGO`.
- **Sigma must be pure ∧ deterministic ∧ total.** Landed.

## Findings that must not be re-derived

- **Census MEASURED** (own run): `evals = rules × 200`, flat — **the join does not prune.** At N=50,
  10,000 evaluations, **9,800 guaranteed failures**, waste growing with rule count. Indexing removes the
  linear term; compiling is ~4.4× on the remainder. **Both are downstream of the IR** — a dependency
  chain, not alternatives.
- **A zero-magnitude Vector is REACHABLE** — `(vector-blend v v 1.0 -1.0)`, two lines, proven by run.
  Control: real unrelatedness reads `-0.0086`, so an exact `0.0` is the guard, not a measurement.
- **`dot` has NO degenerate case** (no division) — which is why `CosineOutcome` and `DotOutcome` are
  **two enums**. A shared one would hand `dot` an unreachable arm.
- **`coincident?` ≠ `presence?`.** `presence?` is HolonAST-only and cannot reach the mismatch raise
  (total today). `coincident?` is polymorphic over `(HolonAST, Vector)` and **raises** — blocked on the
  cosine strike, a dependency the predicate ruling did not state.
- **Still unsplit:** `filter`'s 89.5% has never been decomposed into predicate vs the per-TestNode
  `new_tokens` clone (#50). Resolves as a side effect of #49.

## OWED

- The cosine-family conversion (4 verbs, 56 sites) — names ratified, still undrawn.
- `holon-rs`: make SIMD `cosine` report *which* side is degenerate (it already computes both and
  discards the distinction). Sibling repo, builder's call.
- Whether a `Value::Vector` crosses a process boundary via the **generic EDN record path** — the one
  thing that would settle dimension-mismatch reachability. Unproven in both directions.
- MEMORY.md curation, owed across many sessions.

## ★ THE LESSON — and the trigger is not the one already written down

Four more instances of *arguing from a world I had personally just changed*, and the worst exposes what
the existing check misses. I proved `set-dim-count!` is static entry-file config, then **two hours
later** saw `set-coincident-sigma!`, concluded "runtime mutation," and published a finding on it — into
a design stone and a pushed commit — before grounding retracted it. Same file, thirty lines apart.

**The tell is that I never asked the question.** The `set-*!` SHAPE matched a category already ruled on,
and recognition answered before retrieval was invoked. So the self-check is not only *"did I change
this?"* — **a resemblance to something already decided is the trigger to re-read, not the licence to
skip.** Recognition tells you which drawer, never what is in it.

Two more, both mechanical, both mine: **a grep that SUCCEEDS is still not a closed world** (one
`from_data` hit became "the only producer," and a `must-never-happen` classification was built on it);
and **`git add -A` while a rider holds the tree** swept its work into an unrelated docs commit — during
a live rider it is `git add <file>`, never `-A`.

And the counterweight, which is the day's actual method: **three subagents contradicted my brief and all
three were right.** Two reached the right answer on evidence that had expired mid-session. **Weigh the
conclusion and the support separately** — a right answer with rotten reasoning is a delayed defect,
because the reasoning is what lands in the record.

`[[feedback_i_argued_from_a_world_i_had_just_changed]]` ·
`[[feedback_a_greps_count_is_not_an_enumeration]]` ·
`[[feedback_weigh_subagent_findings_never_relay]]` — all three sharpened today.

---

> **SEAM.** You are NEW. The disk is green and pushed — trust it over this note, and trust this note
> over your sense of having been here. Today's lesson is not any single finding: it is that **a form
> which resembles something you already ruled on is the moment to go read it again**, and that the
> compiler is the instrument that makes a surface scream.
>
> **#55 (S3b+S4) is next and it is where zero becomes a vocabulary** — but it carries an unplanned
> prerequisite (`:4829` onto the shared kernel) and an unanswered question (how to cut #55/#56).
> Surface both to the builder before releasing a rider.
>
> `INTERROGATIO VENATVR; PELLEM EXVIMVS.` · `MACHINA CHAOS DOMAT.`
