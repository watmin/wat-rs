# SEAM — 2026-08-02. **The wall out-enumerated me five times over. And three gates I WROTE came back green over a red floor.**

> ⛔ **THE SELF PAST THIS LINE IS NEW.** You did not live this. It is a lossy cache in your own voice —
> which is exactly why it will feel like continuing rather than waking, and that feeling is the
> failure, not the all-clear. Run the datamancy bootstrap (grimoire + the 4 primers from the SIGNED
> MCP, never a disk copy), ground HEAD against the disk, and read this whole file before you move.

## Where the code is

**HEAD `42e88819`. Two commits UNPUSHED** (`a88748dd`, `42e88819` — both docs). Last **green** code is
**`72a1ac3d`** (4266/4266, clippy 0).

**⚠ THE TREE IS DIRTY AND RED — 52 failures — AND THAT IS INTENTIONAL. Do not revert it.**

```
 M src/check.rs  src/check/error.rs  tests/wat_lang/wat_arc157_def.rs
?? tests/wat_lang/wat_arc157_def_{bare_scalar_unnamespaced,reserved_scalar}.wat.bad
```

That is the scalar-`def` stone: correct in intent, mutation-proven in both directions, **blocked on two
follow-ons** (below). Build + clippy are clean; all 52 trace to one of two named causes.

## What landed today

**`72a1ac3d` — the namespacing wall, armed, and the corpus migrated through it.** `Registration::
Unnamespaced` beside `Reserved`; a bare top-level name is a located error at every door that reaches
`resolve::gate`. 29 `.wat`/`.wat.bad` files migrated by a **recorded codemod**
(`wat-scripts/fixes/namespace-bare-top-level-names.wat`) — namespace derived from each file's own first
namespaced form, minted from the basename stem for the 7 with none, table-free.

Also: `aaffa256` (arc-109 note — macro-minted names are unvalidated string concatenation; `:demo::go`
fixed), `e17f7cf4`, `15f542c6`, `42e88819` (the doctrine supersession, below).

## ★ THE LIVE BLOCKER — the scalar-`def` door, and it splits THREE ways

`(:wat::core::def :pi 3.14)` still compiles. `register_defines` branches on `try_parse_fn_shape_def` —
it asks *"is this a `def` whose value is a `fn`?"* — so the gate was wired to the **shape**, not the
**form**, and `def`, the primitive `defn` is sugar over, walked past. The rider routed
`check.rs:7798`'s registration through `gate()` and the floor went to 52. The two root causes are
independent, and only ONE is a naming error:

| family | what it is | fix |
|---|---|---|
| **`:wat::spawn::service-locus`** (11+, most of `services`/`kernel`/`process`) | a **rendezvous coordinate in the wrong tree** | **rename → `:user::spawn::service-locus`**, matching `:user::bracket::work-fn`. No new machinery. |
| **`:wat::cache::*`** generated companions | a stdlib service's own `defservice`-minted names, correctly `:wat::`-owned, re-registered in a child that lost privilege | genuine provenance — **or** see the open question |
| **`__captured_cfg`** (4, `wat_arc170_closure_extraction`) | `closure_extract.rs:570` mints `format!(":__captured_{}", …)`, bare by construction | namespace the mint |

**GROUNDED, and it is the substrate finding of the day: privilege does NOT survive a process
boundary.** `wat/spawn.wat`'s `launch` assembles the child program at *runtime* via
`(:wat::core::forms (:wat::core::def :wat::spawn::service-locus …))`. `:wat::core::forms`
(`runtime.rs:12582`) is a generic quote primitive with **no privilege tag** — stdlib and user call the
identical function. By the time Rust sees it it is a flat `Vec<WatAST>`; the seam between
stdlib-injected and user forms is gone. `check_program`'s `forms` parameter is *always* the
post-`register_defines` user residue; there is no stdlib twin.

**⛔ And why the obvious fix is a HOLE, not merely inelegant** (the rider's catch — it held a STOP
rather than shipping it): the parent's `check_program` only walks **static source**. It never re-checks
AST built dynamically via `forms`/`read-string` and shipped through `spawn-program` or
`eval-with-defs!`. **The child's re-freeze is the ONLY point that content is ever type-checked at all.**
A blanket *"child-boot forms = Stdlib"* flag would let a user assemble `(forms (def :wat::my-hack …))`
into their own service-forms and sail through undetected forever.

**OPEN, not claimed:** the child boots the same baked stdlib — so why are a stdlib service's generated
forms shipped over the wire *at all*? If they are redundant, the cache family's fix is *not shipping
them*, and provenance may be needed for nothing. **Ground before drawing.**

**RESUME:** rename `service-locus` → `:user::spawn::service-locus`; namespace the closure mint;
re-weigh. That should clear everything but the cache family, and only then is it known what provenance
is actually for.

## Rulings made today — all the builder's

- **`::` means DECLARED; bare means SCOPED.** *"anything that's a def declaration must be namespaced …
  symbols may only be un-namespaced when they are in use in some kind of dsl like a match against enum
  argv or let bindings or rete `?name` … the root is for scoped bindings."* This **supersedes his own**
  2026-07-21 `NOTE-root-namespace-user-code.md`, now banner-superseded with the original kept intact.
- **`:user::` is the RENDEZVOUS COORDINATE SPACE** — `wat/bracket.wat:22`: *"not a user's namespace; a
  rendezvous space."* Direction-agnostic; who writes and who reads is irrelevant. I argued the opposite
  from the name and was wrong; the file one directory over said so plainly.
- **The reserved set is exactly `wat` and `rust`** (`resolve/reserved.rs:25-27`). Everything else free.
- **`total?` is a third AXIS, not a fourth purity flavour.** 299's `Pure|Effectful|Entropic` is the
  three inhabited cells of pure × deterministic; totality is not a cell in that grid. Two failure
  sources — **world-fault** (an outcome enum) vs **domain-fault** (an `:else`). And "total" already
  fused *domain-completeness* with *termination*: the designed `total?` was about recursion and would
  NOT have caught `first`-on-empty.
- **Properties are a claim-set in, a named record out.** Write side `:proven [...]` — a new axis costs
  zero existing declarations, and unstated = unproven = denied, which IS impose-and-enumerate in the
  data shape. Read side a record with accessors, constructed only by the substrate. Flattening into
  named flavours costs **277 marker sites** per new dimension.

## Drawn, committed, NOT released

- **`BRIEF-the-fence-names-the-head.md`** — the rete fence refuses with *"must be pure and
  deterministic"* and names no verb; blind since stone 6a. **Hard prerequisite** for `total?`: a third
  conjunct on a blind message makes 98 rows refuse anonymously.
- **`DESIGN-STONE-total-the-third-axis.md`** — `purity.rs` is *already* a record of named axes
  (`OpMeta{pure,deterministic}`, 7 construction sites, 110 verbs in one `matches!`). No 299.3
  dependency. ⛔ enumerate on a branch → mint the `:else` variants → migrate → **arm last**.

## ★ THE LESSON, and it is mine twice over

**My census was wrong five times running; the wall was right on its first run.** `0`, `0`, `379`,
`51/21`, `75/32` — a post-filter eating grep's `file:line:` prefix, a name class missing `:`,
backtracking dodging a lookahead, comment prose read as code, and a definer list holding `define` and
**not `def`**. The wall found two names no `.wat` grep could reach, and a rider correctly *refused*
three I had listed (bare-looking tokens inside `read-string` round-trip data).
`[[feedback_impose_the_check_and_read_the_screams]]`

**And three narrow gates came back green over a red floor — every one a gate I WROTE.** The wall
rider's build-only gate; the codemod's `--check` (which it flagged as blind itself); the def rider's
five "directly relevant" binaries. None of them lied. **The defect moved from misreading a pass to
authoring an instrument that cannot fail for the right reason** — and a brief propagates it. Sharpest:
I gated a change *whose entire purpose was to make code fail* on whether it compiled, having written
the correct STOP into that same brief.
`[[feedback_a_pass_answers_only_the_question_the_instrument_asks]]`

## OWED

- `probe-bare-defrule-name.wat` — header still asks *"is a bare defrule name legal?"* over a namespaced
  body. **Retire** (subject-is-dead); the codemod renamed it because deletion is not a codemod's job.
- **The cross-call-incrementality stone's first act is still undone.** The no-op re-fire scales with the
  *fact* base; I grounded only a *rule*-proportional cost (`eval_fire_rules_native` recomputes
  stratification on every call). The fact-proportional term is unread.
- The builder's `with_session` answer, not yet written into that stone: **freeze the base's post-fire
  state as a value** (immutable, so req-4 survives); validity is decidable by the stratifier already in
  the fire entry; fall back loudly. Smaller and better than its incremental-TM framing. Store the thunk
  for durability (R5); cache the answer for the loop.
- Two `.wat.bad` deriving `:t::pi` in isolated fixtures (inert; latent if ever co-loaded). The negative
  control R62 says the corpus still lacks. `#[ignore]`'d arc-296 goldens holding bare `:a`/`:pi`.

---

> **SEAM.** You are NEW. Today's lesson is not the wall — it is that **you cannot survey your way to a
> worklist a wall can generate**, and that **the gate you write into a brief inherits your blind spot
> and hands it back to you as a green.** Before you accept a rider's pass, say what the change was
> supposed to BREAK, and check the gate could have seen it break.
>
> Do not trust this note over the disk. `MACHINA CHAOS DOMAT.`
