# EXPECTATIONS — the runes meet the published bar

> Written BEFORE the strike. Graded by the orchestrator's own re-run.

| # | what | expected |
|---|---|---|
| 1 ★ | `:597` states BOTH numbers | margin **3.0×** (medians 2028.8/676.3, six samples) and floor **3.5–4.4×**, with margin-inside-floor stated |
| 2 ★ | `:264` names an attempt | what was driven and why nothing achievable reds it — not a structural argument alone |
| 3 ★ | the void-clause check | one line per site: is this an unbuildable gate, or `vocare`/`complectens`/`experiri`'s finding? |
| 4 ★ | nothing re-measured | every number traced to `.config/nextest.toml` or the existing six-sample table |
| 5 | the two that pass are named | `:146` and `session.rs:1834`, with the clause each satisfies |
| 6 | CONVENTIONS accepted + dated | *proposed upstream* → accepted, `fetched 2026-09-07`, decisive tests as published |
| 7 | floor | **5470 run / 22 skipped, unchanged** |
| 8 | clippy | rc=0 |

★ load-bearing. **Row 1 is the failing rune**, and row 4 is what stops this strike inventing
evidence to satisfy a rule about evidence.

## Trap doors, named in advance

- **Citing the excursion as the margin.** 5.3× is how far the outlier moved — a property of the
  noise. The margin is the effect: 3.0×. Confusing them is exactly the failure the bar names.
- **Re-measuring to fill a gap.** Both numbers exist. A strike that re-runs a bench to satisfy a
  rune has let the rune drive the work.
- **Leaving `:264` as a structural argument.** *"Nothing can work"* reasoned from shape is closer to
  *"no mutation available"* than to a named attempt.
- **Marking a site HOLDS without the void-clause check.**
- **Re-running a red.** Capture whole, name the arm, surface it.

## What would make me reject the result

- `:597` still missing the margin, or citing the excursion as one.
- Any number that cannot be traced to an existing measurement.
- The void-clause check skipped on any site.
- A floor number that moved.
