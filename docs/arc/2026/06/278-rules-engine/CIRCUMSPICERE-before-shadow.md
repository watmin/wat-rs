# Circumspicere before ShadowNode

> Cast 2026-08-18. The suite was blind to *derived inners*. These
> are the side quests. `(b)` ShadowNode **waits on this page being
> all closed.** CURRENT-STATE points here. Do not start 297.

The inward tests compared two engines on fixtures they already
understood. Clara twins are insert-only by design. Circumspicere
turned around. This page is the loot list.

Do **not** spray these onto every Clara grid axis. Named programs.

## Status

| # | Item | Status | Gate |
|---|---|---|---|
| 1 | `rule_consumes` walks `:exists` / acc `:from` | **closed** | `derived_exists_and_acc_*` |
| 2 | Oracle cannot consume an Export | **closed** | `spec_refuses_imported_export` |
| 3 | native ≡ spec on derived-inner class | **closed** | `*_spec_matches_native` on #1 and #5 |
| 4 | fn-headed `:then` produces `T`; another rule consumes `T` | **closed** | `fn_headed_then_produces_type_another_rule_consumes`; `produced_type` uses ret |
| 5 | `rule_negates` walks `:not` of `:and`/`:or` | **closed** | `not_and_over_derived_*` |
| 6 | `:exists` over a type this fire derives | **closed** | folded into #1 |
| 7 | acc `:from` a type this fire derives | **closed** | folded into #1; bag +1 when produced |
| 8 | Export of exists/acc-over-derived | **closed** | #1 and #5 import gates |
| 9 | insert after fire, query without fire | **closed** | `query_is_last_fire_harvest_*` |
| 10 | fire again after more inserts | **closed** | last beat of #9 |
| 11 | same-stratum acc into derived | **closed-as** | bag +1; join cascade stays +0 |
| 12 | ABI is contract, not Merkle of the circuit | **closed** | `import_refuses_abi_mismatch`; ABI is format+classes+`RETE_OPS`, not nodes/deps |
| 13 | wat `rule-consumes` / `rule-negates` vs Rust | **closed** | wat walks lockstep; #3 is the proof |
| 14 | Debug(`WatAST`) on the wire | **closed** | `Symbol(Identifier` forbidden |
| 15 | ShadowNode / 297 silent start | **n/a** | not started |
| 16 | pretty vs compact Export | **accepted** | both mouths exist |

## Closed means

A named test would go red if the fix were deleted. A comment is not a close.

## Open — the remaining crawl

**None.** Every numbered row is closed, n/a, or accepted. `(b)` may start when the builder says so — not before, and not because this page still has homework.

ABI bound (honest): FNV-1a of `v1` + class/field layout + live `RETE_OPS` names. A flipped `:deps` or driver still imports. That is the contract wall, not a Merkle of the circuit.

## Not this page

Keyed gather. Service-ify. 297. `(b)` until every **open** row is **closed**.
