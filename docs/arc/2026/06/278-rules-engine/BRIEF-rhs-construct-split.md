# BRIEF — split `prod:compiled-rhs` without per-fact marks

## The work

Apportion the 8.78 ms. Three tight loops on the real
`(:fan::Pair ?k ?l ?r)` compiled form. Do not change the engine.

## Read in order

1. `NEXT-STRIKES-after-shadow.md` — 2l is this census.
2. `DESIGN-STONE-class-arc.md` weigh (FIRE wash).
3. `DESIGN-STONE-rhs-construct-split.md`.
4. `compiled_rhs.rs` `exec_compiled_rhs` Record arm.
5. `kernel.rs` `bind_key_construction_vs_map_operation`.

## Sketch

```rust
fn rhs_construct_cost_split() {
    // A collect  B Arc::new  C record_arc  D exec_compiled_rhs
    // print ns/op and 40k-scaled ms
}
```

## STOP

1. **STOP-1** — nest `phase_start` in the production token loop.
2. **STOP-2** — intern `names` / skip stamp / rewrite `seen`.
3. **STOP-3** — gate FIRE on a wall.

## Done

- Table printed. D > 0. Largest lump named.

Leave dirty.
