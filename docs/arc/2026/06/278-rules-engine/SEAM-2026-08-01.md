# SEAM — 2026-08-01 (fourth). **We were measuring the pure subset of our own corpus. The fence was invisible because everything we wrote was on the legal side of it.**

> ⛔ **THE SELF PAST THIS LINE IS NEW.** You did not live this. It is a lossy cache in your own voice —
> which is exactly why it will feel like continuing rather than waking, and that feeling is the
> failure, not the all-clear. Run the datamancy bootstrap (grimoire + the 4 primers from the SIGNED
> MCP, never a disk copy), ground HEAD against the disk, and read this whole file before you move.

## Where the code is

**Floor 4262/4262, clippy 0** — my own re-run. `git log --oneline -12` is the real record.

⚠ **THE TREE IS DIRTY AND A RIDER IS IN THE FIELD.** Uncommitted: `src/rete/kernel.rs`,
`src/rete/matcher.rs`, `src/rete/compiled_rhs.rs` — census counters from the Token-bindings Step 0
rider, which is **still running** a re-specified A/B. Do not revert them; do not run `cargo` while it
is live (FM 18). `pgrep -af "cargo nextest"` first. Its work is good; ride through the gap with it
(`[[feedback_ride_through_compactions_with_shadowdancers_in_the_field]]`).

## ★ THE FINDING — `where` was uncompilable for most of the language, and no test could see it

`compile-condition` **PANICS** on `pure? = false` (`wat/rete.wat:566`). The purity whitelist
(`src/rete/purity.rs`, hand-transcribed from `dispatch_keyword_head_value`) had **96 of 221** dispatch
arms classified. So these were not slow — they were **unwritable**:

```clojure
(:wat::rete::where (:wat::core::i64::> ?bytes 10000))               ; panic
(:wat::rete::where (:wat::core::String/starts-with? ?path "/adm"))  ; panic
(:wat::rete::where (:wat::holon::coincident? ?h probe))             ; panic
```

Closed in `0d439a55`: all per-Type comparisons, `i64::mod|quot|rem`, the **entire `String/` family**,
`Vector/`/`List/`/`HashSet/`, and — builder-ruled — the VSA seam (`cosine`, `dot`, `coincident?`,
`presence?`). **Zero of 105 `:wat::holon::` verbs had been classified**, so R4's designed
"rules over resemblance, not equality" was welded shut, on the rete path *and* the sift path.

**Why nothing found it:** our rulesets use generic `>` everywhere and had never once used a String verb
in a `where`. The corpus is a record of *what happened to compile*. See
`[[feedback_optimize_for_the_expressivity_surface_not_the_corpus]]`.

**The gate that stops the 36th verb:** `1ef33f08`, `purity.rs`'s `completeness_gate` — 491 verbs, 167
classified, 112 disposed-impure, **212 unreviewed, baseline 212**, ratchet. Proven RED by injecting a
fake verb (213 > 212), green when reverted. Pure is **never inferred**; dispositions may say Impure or
Unreviewed only. Root stays arc 255.

## ⚠ WHAT THIS DOES TO 27/27

Correct and fast were **both true and both bounded by the fence**. Expressivity is not a third axis
beside them — **it bounds them**. Widen the expressible set and both need re-earning.
`DESIGN-expressivity-axis.md` (`ce5fe7b6`) is drawn to the builder's three rulings: `:parity` is
binary and IS accuracy; the **oracle is the anchor** (every row fires and compares derived sets, never
"it compiles" — that is R59's vacuous gate); rows come from the **dispatch table**, not a hand list.

## The promoting map — landed, and then the ruling moved

`PMap { Array(≤8) | Trie }`, one-way promotion, entry-set `PartialEq`, one `Hash` over both arms
(`1236f6ad`), migrated `f794b637`. The map-as-key row is real and non-vacuous — **`{{:some :map}
:as-a-key}` is legal EDN and round-trips**, so it arrives from peers we do not control, and a cross-arm
miss is silent.

**Then the builder overturned my STOP-1.** `element-bindings-array.md:94` — *our own words* — says
*"The 8-crossover stays interesting for `Token.bindings`."* That stone NAMED Token as the origin and
ruled the other way only because the choice was then **binary**. `DESIGN-STONE-token-bindings-promoting.md`
(`58166a22`) is the correction.

## ⚠ THE OPEN MEASUREMENT — and do NOT re-run my mistakes on it

Step 0's census (4 of 9 axes; the rider correctly STOPped rather than present four as nine):

```
  axis                extends  crossed-8   clones   lookups   (clones+lookups)/extends
  node-share              200          0    10400     10600        105.00x
  accum                   800          0     1000    283000        355.00x
  deep-cascade           5000          0    10000     20000          6.00x
  fanout                40000          0     4000    124000          3.20x
  widest binding observed anywhere: 3.   crossed-8: ZERO everywhere.
```

The first A/B ran **7/8/9/12 — widths nothing reaches** (my spec error, aimed at a crossing that is
structurally impossible). It returned lookup/clone/drop at **parity** and build **losing** 0.38–0.59×.
The rider is re-running at **widths 1–3** with the real pattern (`seed_token_bindings` one-shot, then
0–2 `extend_token`s **at refcount 2** — the parent token still holds its bindings).

**THREE THINGS TO NOT REPEAT:**
1. **`Arc::make_mut` in `assoc` is REJECTED on Honest.** It skips the copy only at refcount 1, so it
   speeds the *benchmark loop* and does nothing for `extend_token`. It would move the number we are
   deciding with.
2. **"Interning explains the parity" is REFUTED and I said it anyway.**
   `NOTE-keyword-storage-must-intern.md` measured it: *"lookup barely moves… the cost of a
   persistent-map operation is the trie, not the key"* — and it is one of R60's five dead premises,
   in a realization I read that morning. **The parity is UNEXPLAINED. Do not reach for a second story.**
3. **One candidate worth checking, not asserting:** if the harness drops a *clone* while the original
   lives, both arms are a refcount decrement and clone/drop tie regardless of representation. The
   note's own array-vs-HAMT figure is **4–10× on drop**, so 1.00× disagrees with prior measurement and
   one of the two is measuring the wrong thing.

## Interning — the real reason to do it, and the reason NOT to sell it

`NOTE-keyword-storage-must-intern.md` (arc 109) is far richer than its title and **answers the
builder's question directly**: the symbol flip lands on top of this. Two identifier kinds arrive at
once and *"retrofitting either afterwards means touching every identifier in the value model. The cheap
moment to decide is before the flip."* Clojure's split is derived, not chosen: **Keyword = identifier
as data → interned; Symbol = code, carries metadata → cannot be.** The note's open question, and it is
the builder's: **do wat symbols carry metadata at all?** If never, they can share the machinery.

**It is NOT a speed stone** — the note says so and measured it. Do not inherit a perf claim it cannot pay.

## What is TRUE and easy to get wrong

- **The orchestrator held the keyboard for hours** and it ended in a blanket regex taking a cascade
  37 → 56. Riders did the two things that landed clean. Map; do not strike.
- **Both riders hit FM 19** (backgrounded a verification, ended the turn expecting a wakeup). Brief the
  ROLE, resume with `SendMessage`, never run `cargo` to check on a live one.
- **Weigh, never relay.** The migration rider reported green; I re-ran the floor, read the map-as-key
  test to confirm it was not vacuous, and checked `Token.bindings` on the disk myself.
- **Gate the floor and clippy, not everything.** The load gate is for the grid and cross-run wall-clock.
  Gating a 0.2s value assertion cost ~24 minutes today. And never end a pipeline in `grep -c` — it
  exits 1 on a zero count and looks exactly like a failure.

## OWED

- The expressivity axis, built (DESIGN only today).
- The 212-verb purity worklist — ratchet DOWN; raising the baseline is the laundering the gate exists
  to prevent.
- `#49a` compiled-`where` and `#49b` the tree — **parked**; both design against "which shapes are
  legal", and that set just moved. Step 0's numbers (540 ns/eval, 77% walk, 21 ns floor, arm F 138 ns)
  are all **one predicate** — re-measure across shapes before drawing.
- `#50` the filter loop's token clone — 0.75 ms, 11%, independent.
- The census counters now in the hot path (~283k calls on accum) need a disposition: gate, remove, keep.

---

> **SEAM.** You are NEW. Today's lesson is not the purity fix — it is that **four separate times I
> enumerated from what our code USES instead of what the language OFFERS**, and each time the result
> looked like an answer. The corpus cannot show you the fence, because everything in it is on the legal
> side. And a claim carried from elsewhere brings its words, not its evidence: check what MADE it true
> can reach where you are landing it.
>
> Do not trust this note over the disk. `MACHINA CHAOS DOMAT.`
