# SEAM — 2026-08-01 (fifth). **Grounding checks whether an answer is TRUE. It never checks whether it answers the QUESTION — and today three grounded scouts in a row missed the question entirely.**

> ⛔ **THE SELF PAST THIS LINE IS NEW.** You did not live this. It is a lossy cache in your own voice —
> which is exactly why it will feel like continuing rather than waking, and that feeling is the
> failure, not the all-clear. Run the datamancy bootstrap (grimoire + the 4 primers from the SIGNED
> MCP, never a disk copy), ground HEAD against the disk, and read this whole file before you move.

## Where the code is

**HEAD `6ba040fa`, pushed.** Floor **4261/4261, clippy 0** — my own re-run, at `5920f57a`.
`git log --oneline -8` is the real record.

⚠ **A RIDER IS LIVE IN THE FIELD AND ITS WORK IS UNCOMMITTED.** `wat-scripts/perf/grid/where-shapes.wat`
(and possibly `gen-where-shapes.sh`) carry rows 2–5 of the `where`-expressivity axis, mid-strike.
**Do NOT revert it. Do NOT reap it.** Ride through
(`[[feedback_ride_through_compactions_with_shadowdancers_in_the_field]]`). Its brief is
`BRIEF-where-shapes-rows-2-5.md`; weigh its five `#grid/Verdict` lines by your own re-run of
`./wat-scripts/perf/grid/run-axis.sh where-shapes "200 1" … "200 5"` (needs `target/release/wat` +
clojure, **no cargo**, so it never collides with a floor run).

## What landed

- **`5920f57a` — `PMap::extend` + `Token.bindings` is a `PMap`.** The batch door exists because
  `extend_token` clones once then `insert_mut`s K times, and `assoc` copied the Vec per key — a
  missing constructor, not a property of arrays. Its law: observationally identical to folded
  `assoc`, *including the arm the result lands in*. Zero `to_trie`/`from_trie` left in the token path.
- **`6ba040fa` — the `where`-expressivity axis, row 1, GREEN.** `:accuracy :match` against Clara,
  through the existing `run-axis.sh`, no new harness.
- **`b6eac483` — `300/NOTE-identifier-must-become-a-type-before-the-drives.md`.** The flip arc IS
  `docs/arc/2026/07/300-wat-source-is-edn/` (its stones 300.1–300.5 are the flip; the *rational/bigint*
  artifacts filling that directory are the detour that occupied it). Interning belongs there, before
  300.2, and the note marks which parts are grounded vs my inference.

## ★ THE LESSON, and it is not any of the code

**I answered the wrong question three times, and every step audited clean.** Asked *"do arbitrary
pure functions in a `where` give equal expressivity in wat-rete vs Clara?"*, I scouted (1) the
**dispatch table** — 167 classified-pure verbs, reconciled exactly against the purity gate; (2) the
**clj expressiveness matrix**; (3) **Clara's condition grammar**. All three are real substrate tables.
All three counts were verified. None was the question. The builder: *"your words defy reason… you've
gone off in wildly unexpected directions."*

Two things make this worth carrying rather than filing as a bad day:

- **Grounding gave zero signal.** It checks whether an answer is true, never whether it answers what
  was asked. Every one of those scouts would have survived any audit I know how to run.
- **It was an over-correction wearing rigor's clothes.** Days earlier I was cut for enumerating from
  our *corpus* — too narrow. I read the cure as *enumerate everything*, and 167 rows looks more
  thorough than 5, so nothing flinched. **The right set is fixed by the POSITION** — a `where` must
  produce a bool, so a constructor in one is a user writing something pointless, not a capability gap.

The answer was one file the whole time: `wat-scripts/scratch-pad/probe-where-shape-spread.wat`, whose
header already said *"WHICH SHAPES THE VERB-AGNOSTIC EXECUTOR CAN COMPILE AT ALL."* Third instance in
one day of R61 `PAR NON ARGVIT, NOSTRA ARGVVNT` — inscribed that morning, violated by lunchtime.
`[[feedback_the_question_drifts_while_every_step_stays_grounded]]`.

## Two measurements I had to throw away — both flattered us

1. **The Token stone's perf case did not survive.** I claimed *"lookups outnumber extends 355×, so
   whichever arm wins lookup wins the engine."* That is a **count**, not a **share**
   (`[[feedback_a_count_is_not_a_share]]`). The isolation — same box, HEAD vs stone — put every delta
   inside the run-to-run spread, and accum, the *highest*-ratio axis, moved **down**. The stone lands
   on the deleted conversion boundary and the unification, **not on speed**, and the design doc says
   so where the next self will read it.
2. **A +50–150% grid comparison, inadmissible.** `GRID-2026-08-01.txt` was committed *before*
   `f794b637` (the PMap migration), so the delta contains that migration too. A stored baseline
   silently ages: it carries every commit since it was written.

Also corrected in place: the design doc said *"baseline at HEAD 4262"* — a number that crossed a
compaction with me and was never re-run. HEAD is **4260**.

## What is TRUE and easy to get wrong

- **`binding_cardinality_distribution` is a PRINT, not a gate.** It asserts only `counted > 0` and
  then prints the table. R60 calls it *"the one check a fast wrong answer would have failed"* — it
  **structurally cannot fail** on a changed distribution. I verified byte-identity with a hand-run
  stash differential. **It should be made to assert a golden**; until then, passing it proves nothing.
- **The clj expressiveness matrix (`tests/clj_expr_oracle/`) is `#[ignore]`d and RED** — 13
  divergences of 49 rows, live. 3 are the *instrument's* own defect (comparing rendered order of
  unordered collections), 2 are deliberate (`get` → Option, `rest` → container), and **8 are genuine
  `::ERR`** — `(/ 6 2.0)`, `()`, `map`, `filter`, `reduce`, `not`, `and`, `str`. It belongs to arc 300.
- **Stash differentials are safe here and I ran two** — always `git diff > backup.patch` first, and
  verify the restored diff is byte-identical to the backup.

## OWED

- Weigh the live rider's rows 2–5; commit if green.
- The 212-verb purity worklist — **ratchet DOWN**. 14 verbs (`+ - * /`, `inc`, `even?`, `count`,
  `mod`/`quot`/`rem`, `assoc`, `conj`, `rest`, `reduce`) already have a clj-parity golden and are
  still listed unreviewed — the cheapest ratchet available, waiting on a ruling not on work.
- Make `binding_cardinality_distribution` assert.
- `#49a` compiled-`where` — the axis's rows ARE its coverage list; row 5 (a user-defined pure fn) is
  the shape it cannot model.
- `#50` the filter loop's token clone. `R25` the chaos engine, still unbuilt.

---

> **SEAM.** You are NEW. Today's lesson is not the stone and not the axis — it is that **a true answer
> to the wrong question passes every check you have.** Re-read the builder's own sentence at the top
> of a finding and ask whether the finding answers *that*. And when a scope keeps inflating —
> 5 → 9 → 167 → "and 92 more" — the question has drifted, no matter how well-grounded each step was.
>
> Do not trust this note over the disk. `MACHINA CHAOS DOMAT.`
