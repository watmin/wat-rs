# SEAM — 2026-08-01. **Nine axes, 27 of 27 `:us`. And the number is not the story: the board went HIDDEN → LOSING → WON in one session.**

> ⛔ **THE SELF PAST THIS LINE IS NEW.** You did not live this. It is a lossy cache in your own voice —
> which is exactly why it will feel like continuing rather than waking, and that feeling is the
> failure, not the all-clear. Run the datamancy bootstrap (grimoire + the 4 primers from the SIGNED
> MCP, never a disk copy), ground HEAD against the disk, and read this whole file before you move.

## Where the code is

**`main` = `9275f907`, pushed. Tree clean.** Floor **4230/4230**, clippy **exit 0 / zero warnings** —
both by my own re-run, own exit codes, after every edit. Toolchain pinned 1.97.0. No riders in field.

## What happened, in order — and the order IS the finding

1. **Morning:** the previous seam claimed *"21 of 21 `:us`."* It was **seven axes of nine.** A0
   (deep-cascade) and A1 (fanout) existed as scripts, were speed-measured in R4, never got the
   `:derived` accuracy differential, and were not in `run-all.sh`.
2. **A0 entered at an HONEST size** and `[50 100]` read **`:winner :clara` 0.745** — five runs,
   `max 0.9269`. The **first Clara win this project has ever found by measurement** rather than
   inherited from R4. Its first ladder (width 5) was 300 derived facts on a ~4 ms fire; at that size
   the same rung read 5.96× and 12.12× twenty minutes apart. **A fake 4–12× win was hiding a real loss.**
3. **The cause, measured at constant work:** `alpha:match` was **117.318 ms of a 169.7 ms round loop**,
   because the alpha index keys on fact **type only** — every fact match-tested against every alpha of
   its type (`facts × D`, exactly one of which can succeed).
4. **The fix was already ours.** `holon-lab-ddos/veth-lab/filter/src/tree.rs:75` — `ShadowNode`:
   equality fan-out + `wildcard` edge + guarded `range_children`. 1M rules, ~5 tail calls/packet,
   shipped February. The post says it outright: *"The XDP walker is the alpha network."*
5. **The cell closed:** `[50 100]` now `:us` 2.63, with **Clara's own ns unchanged to four figures**
   (104,305,835 → 104,282,497). The box did not move; the engine did.

## The board — nine axes, 27 rungs, ALL `:us` and `:accuracy :match`

```
min-finding  12.29 / 11.01 / 12.68     strat-neg    12.01 /  8.52 / 11.55
negation     13.92 /  7.54 /  8.75     user-reduce   6.88 /  4.10 /  1.91
asym-join     6.69 /  7.45 /  4.53     node-share    4.87 /  3.57 /  1.83
accum         2.28 /  2.26 /  1.80     deep-cascade  7.29 /  2.99 /  2.63
fanout        2.81 /  2.13 /  1.36
```
Full record + bounds: `wat-scripts/perf/grid/GRID-2026-08-01.txt`. **First complete sweep ever run.**

⚠ **The tightest cell is `fanout [40000]`, `min 1.0794`** — one run of three within 8% of parity, and
it is precisely the cell R4 logged as `Clara 1.4×`. The place we are closest to losing is the place we
historically lost.
⚠ **Ranges, not points.** Clara CV ~16% vs our ~3.5%; `node-share [10 200]` spans 2.38–8.36.
⚠ **Cross-session comparison is VOID.** R4's own unmodified fanout harness reads 136.6–171.3 ms here
today against the 96 ms in its record. Only the within-session interleaved ratio is portable.
⚠ **A0 still DECAYS with depth** (7.29 → 2.99 → 2.63). Recorded as a decay, **not** a projected
crossover — three points do not license that (R60).

## ★ RESUME — the named targets, in order. He said: attack them swiftly.

The alpha stage is done. **`production` and the freeze are now the fire**, and *neither has been read*:

```
fanout [100×20], 102.843 ms fire, post-everything
  production        34.963 ms   34.0%   ← RHS re-derives a static program (the SAME defect
                                          we just fixed for alpha, one layer over)
  hash-join         26.511 ms   25.8%
  out:production    25.774 ms   25.1%   ┐ ~35 ms — a THIRD of the fire — is pure data
  round:drop-mem     9.111 ms    8.9%   ┘ movement, producing nothing new
  alpha:match        0.539 ms    0.5%
```

1. **`DESIGN-STONE-compiled-rhs`** — `build_insert_fact` resolves `?vars` through the *same*
   `resolve_operand` we just compiled away. Largest single phase. **Read it before sizing it.**
2. **The production-memory freeze — a "should we interface with this at all" question, not a perf one.**
   `fire_fixpoint_delta` CLEARS alpha/beta/production every fire. Alpha's freeze was found to serve
   nobody (`out:alpha` now 0.001 ms). `out:production` still costs 25.8 ms. Its consumers are the
   oracle's `collect-derived` and `query-by-type-string` — **and R18 already documented that reader as
   artifactual** (it sums per-round firings; `Session/facts` is the deduped truth). **HYPOTHESIS, not a
   finding.** Two checks settle it: (a) does `facts`-filtered-by-type equal `query-by-type-string`'s set
   on every axis? (b) does snapshot/revive/EXPLAIN/TM read production-memory after a native fire?
3. **`DESIGN-STONE-compile-actually-compiles`** — `:wat::rete::compile` does not compile; every
   rule-derived index is rebuilt per fire. Fine at batch scale, **fatal for streaming** (the eBPF tree
   took 2.9 s to build at 1M rules). Session stays a `defrecord` (`to_transient:420` rejects a Struct;
   the oracle constructs Sessions in pure wat). The keying fork is deliberately left for R0.
4. **Retract** — still no axis. `O(everything)` vs Clara's `O(delta)` TMS.
5. **Streaming** — still no axis. All 27 rungs are batch fires.
6. **R0, the chaos engine** (task #7).

## Things that are TRUE and easy to get wrong

- **A peer-oracle cannot convict you of a flaw it SHARES** (R61 `PAR NON ARGVIT, NOSTRA ARGVVNT`).
  Clara's alpha network is the same shape as ours was, so the grid was structurally blind to it. **Our
  own shipped work is the second oracle** — the kernel tree, the L7 tree, the published posts. Read as
  history, they teach nothing; consulted as ground, they had the answer six months early.
- **Do not extrapolate across workload shapes.** I sized compiled-conditions at ~11 ms from the
  cascade's per-fact rate. Measured: **1.211 ms — wrong by 10×**, in the direction that favoured
  building it. The type tier already discards ~91% of fanout's facts for free.
- **The instrument reports its own failure.** `| tail` buffers and looks hung; `| grep -c` exits **1
  when it matches ZERO**, i.e. when clippy is CLEAN. Capture the tool's own exit code, log to a file,
  read the file. Twice tonight.
- **Do not infer structure from text.** The census share column was wrong TWICE — first summing
  indented rows (209%), then un-indented ones (plausible and still wrong: `production`/`hash-join`
  carry unindented names while living inside `ROUND LOOP`). The four bracket phases are now NAMED.
  **A wrong number that looks reasonable is worse than one that reads 209%.**
- **CLIPPY IS A SEPARATE GATE FROM NEXTEST.** I pushed a clippy-red commit (`1d4080a7`) by splitting a
  doc comment and not running the wall — in the same hour I added clippy to a rider's scorecard because
  the prior stone did the same thing. Run it, with its own exit code, before every commit.
- **A rider believes it is the orchestrator.** It has `run_in_background` and reasons "start the long
  thing, end the turn, get woken" — but ending a rider's turn TERMINATES it and returns control to you.
  Brief the ROLE, not just the rule: *ending your turn ends you; run verification in the foreground.*
- **The census is a TEST build** (`#[cfg(test)]` timers, ~1.45× inflated). Read PROPORTIONS. The grid's
  `:native-ns` is the release truth. Single-run numbers are smoke tests: `GRID_RUNS=1` read 1.94 where
  the 3-run verdict read 4.53 on the same rung.

## OWED / TRACKED

- **The rider-yield failure mode** → `COMPACTION-AMNESIA-RECOVERY.md` §6, beside FM 18; and amend
  (do not duplicate) `feedback_task_completed_means_stopped_not_finished` with the cause + prevention.
- `109-kill-std/NOTE-keyword-storage-must-intern.md` — filed with "NOT the rete lever," true for
  *lookup* and **wrong for construction**, which is what the hot path does.
- `109-kill-std/NOTE-a-malformed-definition-must-not-vanish.md`; the `defn` tier-1 fix.
- **MEMORY.md curation** — long overdue, its own session.
- Docs are stale by ruling: *"the code is the truth; a doc cleanup is pending."*

## NOT debt — do not "fix"

`holon-lab-trading` does not build (paused by design). `holon-lab-ddos` is on `nightly` (eBPF/aya).
**The wat oracle is naive and wasteful ON PURPOSE** — *"if the wat side is naive and wasteful, so be
it — the rust side is what users actually use."* It never gets optimized, and under every stone drawn
today it never needs to change.

---

> **SEAM.** You are NEW. **`main` = `9275f907`, pushed, green (4230/4230, clippy 0).**
>
> It bears repeating, because each cost something today: **a peer cannot convict us of a flaw it
> shares — our own shipped work is the second oracle**; **an honest ladder found a loss a fake 4–12×
> win was hiding**; **five of my premises died and every one made the answer better**; and **the
> instrument reported its own failure twice while the subject was clean.**
>
> The alpha stage is finished. `production` and the freeze are the fire now, and **nobody has read
> either.** Measure before you size — that error cost 10× today.
>
> Do not trust this note over the disk. `MACHINA CHAOS DOMAT.`
