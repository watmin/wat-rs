# SEAM — 2026-08-01 (second). **The fire went 118.85 → ~76 ms. But the number is the smallest thing here: FOUR premises died, three of them because a hedge had been restated until it lost its hedge.**

> ⛔ **THE SELF PAST THIS LINE IS NEW.** You did not live this. It is a lossy cache in your own voice —
> which is exactly why it will feel like continuing rather than waking, and that feeling is the
> failure, not the all-clear. Run the datamancy bootstrap (grimoire + the 4 primers from the SIGNED
> MCP, never a disk copy), ground HEAD against the disk, and read this whole file before you move.

## Where the code is

**Floor 4243/4243, clippy 0** — both by my own re-run, own exit codes, after every edit. Toolchain
pinned 1.97.0. No riders in field. `git log --oneline -12` is the real record; this file is a map.

## ★ RESUME — the builder's order, verbatim: *"the A/B testing and extend_token are our first targets"*

1. **#43's A/B is UNRUN.** The compiled-RHS stone landed measured only by COUNTS (`match:key-alloc`
   120,000 → 0). ⚠ **A test-build A/B on this one is CONFOUNDED and every other A/B today was not:**
   the compiled path carries **1 mark pair per fact against the interpreter's 4**, so ~120,000 mark
   pairs vanish — at the seam's own ~120-165 ns/pair calibration that is 14-20 ms of *instrument*
   removal, likely exceeding the real win. Measure in RELEASE (the grid; `phase_*` are
   `cfg(not(test))` no-ops there) or equalise the marks. Do not quote a census delta for this stone.
2. **`extend_token`'s double `Vec` allocation** — `tok.matches.clone()` sizes exactly, then `push`
   reallocs. One `Vec::with_capacity(len+1)` fixes it. ~2-3 ms, mechanical, safe. Inside
   `hj:catchup:probe` (~18.3 ms), which is the largest remaining named item.

## What landed (7 commits + this one)

```
ef9297a1  dedup-and-store       FIRE 118.85 → 105.76   −13.1 ms   one hash not two + a sized reserve
0416d1a5  the freeze             105.76 →  85.82       −19.9 ms   push_back_mut; 84% off out:production
efa9b5a2  the 35-site sweep       85.82 →  87.08        none      predicted no-op; it unblocked the wall
b2e0d347  no_rpds_rebuild_loop                            0 ms    a wall, armed at zero, proven red
3d1137e7  bindings dominance probe                        0 ms    a RULING UPHELD on new evidence
9d07f385  beta-traffic probe                              0 ms    a measurement that earned a stone
e9134ec9  the beta guard          82.31 →  76.02        −6.29 ms  a DELETION, not an optimisation
   (this)  compiled RHS + the :then wall + the Result differential
```

**Two commits earned 33 of the 43 ms. Four earned none** — and that is not a complaint: the walls and
probes are what made the next cut findable. Say it that way; a cumulative percentage hides it.

## ⚠ THE PATTERN THAT COST THE MOST TODAY — a hedge, restated, loses its hedge

Three separate times the record had grown MORE CERTAIN than the disk, with **no new evidence at any
step**, and each time the fix was to go read the thing:

- `compiled-conditions` wrote *"I have NOT READ those hot paths, so this stone does not claim it"* →
  the previous seam restated it as *"shape fully known"* → I restated that as *"designed, never
  built"* and said it three times. **There was no design doc.**
- R60 gave a *rationale for leaving `Token.bindings` alone*; I quoted it back as a **measured
  verdict**. It had never been measured against an array.
- The seam's *"NOT deletable"* for `out:production` became, in my mouth, *not worth optimising*. It
  was **84% waste** — a `push_back` ladder path-copying 40,000 times.

**The builder broke all three by asking the plain question** (*"is != not optimizable?"*, *"does this
still hold up?"*, *"what test don't we have?"*). Expect to be carrying at least one such inflation
right now and not to know which.

Two smaller instances of the same family, both mine, both grep-derived claims of ABSENCE that were
wrong: *"there is no `:then` validator"* (there is — `validate_and_reorder_then`; it validates SHAPE
and stops short of operands) and *"designed, never built"*. `feedback_a_grep_that_cannot_reach_is_not_evidence`.

## What is TRUE and easy to get wrong

- **Token.bindings stays a trie — MEASURED, not inherited** (`NOTE-token-bindings-stays-a-trie.md`).
  The array wins extend below ~12 and read below ~6 and loses badly above; the trie's GET is FLAT
  (~19-20 ns from cardinality 1 to 64) where the array climbs 4 → 234. No dominance ⇒ a threshold ⇒
  R60's cut stands. **Clojure independently ships the same crossover: `PersistentArrayMap.HASHTABLE_THRESHOLD
  = 16` array slots = 8 entries, read from the bytecode.** Its answer is not to pick — it **promotes
  per instance**, which is why its threshold is not a corpus bet. That third door was never on the
  table when the question was posed as array-vs-trie.
- **The biggest instance of that is NOT rete** — `Value::wat__core__PersistentMap` is an unconditional
  HAMT at 71 sites, so every `{:a 1}` a user writes allocates one. Scoped to rete for now *as a
  reference implementation*, and it is safe there for a reason that does not transfer: a
  `TokenBindings` is never a hash key, so the cross-representation `Hash`/`Eq` invariant — the part
  that would silently break joins — **does not arise**. At `Value` level it does.
- **A node's beta is needed IFF it parents a `HashJoinNode`.** Measured 16/16 across three shapes.
  The third world (`TRI_CENSUS_WORLD`, 3 conditions → a MIDDLE join) is load-bearing: fanout and
  cascade are both 2-condition, so every hash-join in them is a LEAF and neither could have refuted
  the rule. J1 read 251. **`beta_write_read_traffic` now asserts no node has reads with zero writes** —
  the dangerous direction, where a future reader gets `None` and the join silently loses tokens.
- **`compile_rhs` returns `None` IFF `build_insert_fact` errors.** The two accepted sets differ by
  exactly `Keyword`, and a RHS has no current fact, so a `:field` ref always errors. The fallback was
  never a safety net for compilable-but-unsupported forms; there are none.
- **CROSS-BATCH COMPARISON IS VOID.** Every A/B today stashed between arms in ONE batch. `round:drop-memories`
  read 4.4-11.5 ms across three consecutive runs — a mean off that is meaningless.
- **Name the control, and check it is one.** I called `hash-join` a control for the beta guard; it is
  `hj:catchup:emit`'s PARENT, so its move proved nothing. A control must be something the change
  cannot touch.
- **When the accounting does not close, say so.** Phase savings summed to ~10.6 ms against a fire
  delta of 6.29 for the beta guard, both inside ROUND LOOP. I quoted the conservative number. A tidy
  sum would have looked better and been less true.

## OWED — precise, not vague

- **The `:then` wall is HALF.** It rejects operands that can NEVER resolve (nested form, `:field`
  keyword, bare non-`?` symbol) — purely syntactic, zero false-positive risk, 0/470 corpus rejections.
  It does **not** reject an **unbound `?var`**, which needs binder analysis over `:when` (an `Or` arm
  binds conditionally, `exists` binds nothing outward, `accumulate` binds its result var).
  **Under-collecting that set rejects LEGAL rules** — the one failure a wall must not have. Its own stone.
- **The collapse this enables is NOT done.** With the wall live, `compile_rhs`'s operand-`None` is
  unreachable, so #43's fallback arm and the byte-identical-error patch (which existed only to bind
  two diverging paths together) can both come OUT. That deletion is owed.
- **Only ONE differential compares failures.** `compiled_rhs_result_identical_to_interpreter` now
  does, proven red-then-green **on the assertion** (build errors counted separately — a build failure
  is not a red test). `native == oracle` compares fact SETS; `compiled_cond` compares BINDINGS ON
  MATCH. Neither compares an error. The habit was never formed because `alpha_match_inner` returns
  `Option` and had no error to compare.
- **Coverage: specified, built, never run.** `docs/COVERAGE-RUNE.md` (arc 252) + `scripts/coverage-gate.sh`
  (252.1, managed cargo-llvm-cov, tiered, leak-safe). **Both checklist boxes unticked; zero
  `rune:coverage` across all nine `src/rete/*.rs`.** Same shape as CI never having run to completion
  until 2026-07-30. Pointing it at the rete home would have found today's two dark arms by machine
  instead of by hand.
- **`build_insert_fact:661` renders its `got` with `format!("{arg:?}")`** — Rust `Debug`, `Identifier
  { scopes: {} }`, absolute paths, triple-escaped, to a wat author. `validate.rs`'s `render_form` is
  right there. Now mostly unreachable behind the wall, still wrong.
- MEMORY.md curation — long overdue, its own session.

## NOT debt — do not "fix"

`holon-lab-trading` does not build (paused by design). `holon-lab-ddos` is on nightly. **The wat oracle
is naive and wasteful ON PURPOSE.** And per R61: **our own shipped work is a second oracle** — the eBPF
tree answered the alpha question six months early. It has NO beta network (it pre-joins at rule-load;
one packet, no cross-fact join), so consulting it on hash-join returns a genuine null. Consult it
anyway; accept the null.

---

> **SEAM.** You are NEW. The perf axis is nearly exhausted for this workload: what remains sums to
> maybe 6-8 ms of a ~76 ms fire, and **nothing left is a subtraction** — today's two big wins were
> both removals of waste, and the waste is gone.
>
> **The honest frontier is not in the phase table.** The grid is 27/27 `:us` and R60's caveat still
> stands: *both sides of every benchmark are ours; nothing here has met a workload we did not author.*
> R25's chaos engine (task #7) is unbuilt. And `beta_write_read_traffic` surfaced a structural fact
> nobody has examined — **fanout goes ENTIRELY through the catch-up path**; the semi-naive incremental
> machinery the P6 design is *about* never executes on it.
>
> Do the two named targets. Then ask whether the next millisecond is worth more than the first
> workload we did not write.
>
> Do not trust this note over the disk. `MACHINA CHAOS DOMAT.`
