# SCORE — Stone 251.2 — the value/ ward (the keystone sealed)

**Verdict: CONVERGED. `src/value/` STAMPED `2026-06-07T01:16:28Z`.** L1+L2=0; clippy-0 in-home;
lib 923/0/1 throughout. The first home of the great migration — the runtime data model lifted out
of the flat 31k runtime.rs (251.2a–e) — earns its vigilatum.

## The guard cast (the home's FIRST ward — code lifted from the unwarded monolith)

| Spell | Result | Disposition |
|---|---|---|
| intueri | 1 L1 + 6 L2 | FIXED (R1): stale contradictory HashMap doc deleted; TRANSFORMS markers folded into `///` docs; `shown`→`prose_span`; module docs (frame, encoding_ctx) + render_value doc added |
| solvere | 2 L1 + 1 L2 | RUNED (R1): extract_classifier back-arc + SpawnOutcome placement = historical-shape transitional (excusare-warranted); AssertionPayload L2 dissolved |
| conformare | 0 in-home | CONVERGED — RuntimeError held its arc-243 Pattern-A shape through the lift; freeze-pair spanless-by-domain documented. 3 L2 OUT-OF-HOME (thread_io/freeze) banked → #189 |
| purgare | 0 L1 + 1 L2 | RUNED (R1): eval_redef_allowed write-only scaffolding = future-fixture (excusare-HOLDS) |
| excusare | 0 L1 + 2 L2 | FIXED (R2): module_inception at-site comment; Config rune reason de-deferred. **Integrity check PASSED — all 4 runes warranted (2 OPEN-DEFERRAL, 2 HOLD); no fix dodged.** |
| struere | 1 L1 + 1 L2 | FIXED (R2): **THE BUG** — render_value's List arm rendered UNBOUNDED (`.collect()/.join()`, no SHOW_MAX_LEN guard — lifted verbatim from the monolith); now guarded like every sibling arm. + FrameGuard `#[must_use]` |
| perspicere | 1 L2 + 14 L3 | MINTED `BindingMetadata` (the one genuine 3-level type). **14 idiomatic 2-level wrappers WEIGHED L3** — `Arc<Vec<Value>>`/`Option<Arc<T>>` ARE their definitions (intueri's principle); an alias would force a lookup + obscure the variant-doc-narrated Arc story. perspicere's `2+<` heuristic over-fires on idiomatic data-model wrappers. |
| exigere | 2 L1 + 2 L2 | FIXED (R2/R2.1): deferral-prose ("a future arc"/"pending roadmap"/"opens IFF a caller surfaces") driven out of 2 rune reasons + 2 field docs → present-affirmative. The 2 L2 (transitional runes) arc-verified clean. |
| circumspicere (perimeter, last) | 0 L1 + 2 L2 + 3 L3 | FIXED (R3/R3.1): stale atomizable classification table updated to match `is_atomizable` + line-ref → name-ref; Duration overclaim retracted (honest: WAT-surface-enforced, i64 storage; u64 type-fix → #188). +15 stale `check.rs:3623` citations killed (orchestrator drift-grep caught them). L3: SEQ_TAG comment corrected; no-unit-tests + wider-pub-mod-egress noted (transitional). |

sequi + temperare: **declared converged-by-inspection** (data-model home — no multi-step
state-threading chains for sequi; a verbatim lift adds no new hot loops for temperare).
circumspicere judged the declaration **defensible**.

## Convergence basis (transparent)

Convergence established by **per-finding fix-verification** (each fix re-run: build + lib 923/0/1 +
clippy-in-home-0 + targeted greps) + **excusare's rune-confirmation** — NOT a second full vigilia
round. Defensible for a verbatim-lift + targeted-fixes home; a strict re-cast round is available on request.

## The apparatus read what the casts couldn't (weigh-don't-trust earned its keep)

Orchestrator drift-greps caught two finding-classes the spell-casts walked past:
- an **incomplete de-deferral** (R2 fixed the runes; an OR-grep found 2 more "future arc" doc-comment sites the exigere cast under-reported);
- **15 stale `src/check.rs:3623` line-citations** (the `check.rs:[0-9]` grep — circumspicere flagged only the table instance; the Hash-doc + 13 panic-message strings were the same drift, lifted verbatim).
The complementarity law in practice — the mechanical grep reads what the lensed casts turn their backs on.

## Banked follow-ons (named, tracked — not deferred-to-nowhere)

- **#188** Duration(u64) — make non-negativity type-enforced (✅✅✅; cross-file cascade, own stone).
- **#189** conformare out-of-home — thread_io `list_span`-into-closures + freeze `spanless-by-domain` runes (for the stdio/ + freeze/ wards).
- **eval_redef_allowed** delete-vs-wire — builder roadmap call (runed future-fixture interim; tracked here, out of the code comment).
- **Coverage** — circumspicere's negative-space L3 (no co-located unit tests; integration-covered). A `cargo-llvm-cov` coverage gate for warded homes is the structural answer (builder-raised 2026-06-07) — its own tooling stone.

## State

value/ now: mod · value · environment · symbol_table · signal · observe · frame · encoding_ctx —
8 files, stamped. runtime.rs 31,328 → 28,785 (~2,543 lifted). **251.2 CLOSED.** Next migration
stone per the SCOUT-LIFT-MAP dependency spine (scalar/ + the eval dispatch routing now unblocked by
value/ standing).
