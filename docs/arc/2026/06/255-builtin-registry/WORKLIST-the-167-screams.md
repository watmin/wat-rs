# WORKLIST — the 167 screams of the one-variant-separator wall

**Generated by the wall itself**, on the tree at `f88e7a31c` (③a-ii landed: floor 5318/5318,
clippy 0). Not a grep — `cargo nextest run --release -E 'test(only_identifier_rs_spells_the_variant_separator)'`
reproduces it exactly. `[[feedback_an_instrument_must_outlive_the_number_it_produced]]`

⛔ **EVERY LINE HERE IS NON-VARIANT BY CONSTRUCTION.** ③a-ii routed all twenty-one variant
sites through `compose_variant`/`decompose_variant`; what remains spells a `::` that is a
namespace, a type path, a display string, an EDN translation, or not a wat name at all. A
line that turns out to BE a variant separator refutes that census — it is a **finding**,
not a rune.

```
167 screams    Counter({'COMPOSE': 73, 'ACCESSOR': 49, 'DATA': 45})
```

## AREA A · EDN RENDERING — 35 lines

```
src/edn/render.rs:414  [DATA]  match body.rsplit_once("::") {
src/edn/render.rs:1610  [COMPOSE]  TypeFormHeadMode::Colon => WatAST::Keyword(format!(":wat::core::{tail}"), unk),
src/edn/render.rs:1619  [DATA]  } else if body.contains("::") {
src/edn/render.rs:1644  [COMPOSE]  TypeFormHeadMode::Colon => WatAST::Keyword(format!(":wat::core::{tail}"), unk.clone()),
src/edn/render.rs:1652  [ACCESSOR]  let tail = wat_reader::identifier::leaf(fqdn);
src/edn/render.rs:1657  [DATA]  } else if head.contains("::") {
src/edn/render.rs:2280  [DATA]  Some(ns) => format!(":{}::{}", ns.replace('.', "::"), k.name()),
src/edn/render.rs:2527  [DATA]  Some(ns) => format!(":{}::{}", ns.replace('.', "::"), k.name()),
src/edn/render.rs:3060  [ACCESSOR]  let expected_leaf = wat_reader::identifier::leaf(stripped);
src/edn/render.rs:3090  [COMPOSE]  expected: format!("{}::{} (unit → `{{}}`)", type_path, variant_name),
src/edn/render.rs:3101  [COMPOSE]  expected: format!("{}::{} (tagged map)", type_path, variant_name),
src/edn/render.rs:3110  [COMPOSE]  expected: format!("{}::{} field :{}", type_path, variant_name, fname),
src/edn/render.rs:3121  [COMPOSE]  "edn_to_enum_value: `{type_path}::{variant_name}` matched Tagged above but \
src/edn/render.rs:3146  [DATA]  if !stripped.contains("::") {
src/edn/render.rs:3153  [DATA]  let ns = wat_reader::identifier::path(stripped).replace("::", ".");
src/edn/render.rs:3154  [ACCESSOR]  let name = wat_reader::identifier::leaf(stripped).to_string();
src/edn/render.rs:3164  [DATA]  wat_reader::identifier::path(stripped).replace("::", ".")
src/edn/render.rs:3176  [ACCESSOR]  && enum_leaf == wat_reader::identifier::leaf(stripped)
src/edn/render.rs:3278  [DATA]  .replace("::", ".")
src/edn/render.rs:3287  [DATA]  stripped.replace("::", ".")
src/edn/render.rs:3551  [DATA]  format!(":{}::{}", ns.replace('.', "::"), name)
src/edn/render.rs:3573  [DATA]  if !body.contains("::") || body.ends_with("::") {
src/edn/render.rs:3577  [ACCESSOR]  let final_seg = wat_reader::identifier::leaf(body);
src/edn/render.rs:3578  [DATA]  let mut ns_parts: Vec<&str> = wat_reader::identifier::path(body).split("::").collect();
src/edn/render.rs:3948  [COMPOSE]  "reconstruct_enum_tagged: `{path}::{variant_name}` matched Tagged above but \
src/edn/render.rs:3960  [COMPOSE]  "variant `{path}::{variant_name}` missing map key :{fname}"
src/edn/render.rs:4551  [DATA]  if stripped.contains("::") {
src/edn/render.rs:4552  [DATA]  let ns = wat_reader::identifier::path(stripped).replace("::", ".");
src/edn/render.rs:4553  [ACCESSOR]  let name = wat_reader::identifier::leaf(stripped);
src/edn/render.rs:4593  [DATA]  if !stripped.contains("::") {
src/edn/render.rs:4600  [DATA]  let ns = wat_reader::identifier::path(stripped).replace("::", ".");
src/edn/render.rs:4601  [ACCESSOR]  let name = wat_reader::identifier::leaf(stripped);
src/edn/render.rs:4619  [DATA]  if !stripped.contains("::") {
src/edn/render.rs:4626  [DATA]  let ns = wat_reader::identifier::path(stripped).replace("::", ".");
src/edn/render.rs:4627  [ACCESSOR]  let enum_leaf = wat_reader::identifier::leaf(stripped);
```

## AREA B · THE DERIVES AND CRATES — 25 lines

```
crates/wat-doc/src/lib.rs:1024  [DATA]  if k.len() > prefix_len && k.starts_with(wat_type_path) && k.as_bytes()[wat_type_path.len()..].starts_with(b"::") {
crates/wat-doc/src/lib.rs:1026  [DATA]  if !rest.is_empty() && !rest.contains("::") {
crates/wat-doc/src/print.rs:140  [DATA]  if !body.contains("::") || body.ends_with("::") {
crates/wat-doc/src/print.rs:143  [ACCESSOR]  let final_seg = wat_reader::identifier::leaf(body);
crates/wat-doc/src/print.rs:144  [DATA]  let mut ns_parts: Vec<&str> = wat_reader::identifier::path(body).split("::").collect();
crates/wat-edn/src/vocab.rs:213  [DATA]  let translated = ns.replace("::", ".");
crates/wat-macros/src/edn_doc.rs:258  [DATA]  let wat_ns = ns.replace('.', "::");
crates/wat-macros/src/edn_doc.rs:259  [DATA]  if let Some((head, type_seg)) = wat_ns.rsplit_once("::") {
crates/wat-macros/src/edn_doc.rs:263  [COMPOSE]  return format!(":{head}::{type_seg}/{name}");
crates/wat-macros/src/edn_doc.rs:266  [COMPOSE]  format!(":{wat_ns}::{name}")
crates/wat-macros/src/lib.rs:901  [COMPOSE]  .name(format!("wat-test::{}", #deftest_name))
crates/wat-macros/src/wat_value.rs:191  [ACCESSOR]  attr.path().is_ident("wat_value")
crates/wat-source-derive/src/lib.rs:766  [COMPOSE]  format!("`{want}::{vname_bare}` uses a SURFACE SPLICE — splices resolve against a live TypeEnv, which does not exist at compile time"),
crates/wat-source-derive/src/lib.rs:773  [COMPOSE]  format!("`{want}::{vname_bare}`: field `{}` is not a `name <- :Type` triple", name.as_str()),
crates/wat-source-derive/src/lib.rs:782  [COMPOSE]  format!("`{want}::{vname_bare}`: field `{}` arrow slot holds {other:?}", name.as_str()),
crates/wat-source-derive/src/lib.rs:789  [COMPOSE]  format!("`{want}::{vname_bare}`: field `{}` uses arrow `{arrow_txt}`", name.as_str()),
crates/wat-source-derive/src/lib.rs:795  [COMPOSE]  format!("`{want}::{vname_bare}`: field `{}` type node has no source range", name.as_str()),
crates/wat-source-derive/src/lib.rs:804  [COMPOSE]  format!("`{want}::{vname_bare}`: unexpected item in the field vector: {other:?}"),
crates/wat-source-derive/src/lib.rs:1091  [DATA]  #[doc = concat!("GENERATED field names for `", #type_path, "::", #variant, "`, read from its wat `defenum` declaration.")]
crates/wat-source-derive/src/lib.rs:1128  [COMPOSE]  "`{want_type}::{want_variant}` is a UNIT variant (no fields) — \
crates/wat-source-derive/src/lib.rs:1143  [COMPOSE]  "`{want_type}::{want_variant}` uses a SURFACE SPLICE — splices \
crates/wat-source-derive/src/lib.rs:1152  [COMPOSE]  "`{want_type}::{want_variant}`: unexpected item in the field vector: {other:?}"
crates/wat-to-edn-derive/src/lib.rs:485  [ACCESSOR]  if !attr.path().is_ident("to_edn") {
crates/wat-to-edn-derive/src/lib.rs:563  [ACCESSOR]  if !attr.path().is_ident("to_edn") {
crates/wat-to-edn-derive/src/lib.rs:761  [ACCESSOR]  if !attr.path().is_ident("to_edn") {
```

## AREA C · TYPES · DECLARE · RESOLVE — 25 lines

```
src/declare/parse.rs:1041  [DATA]  if s.contains("::") || s.contains('.') {
src/declare/register.rs:1611  [DATA]  let predicate_name: String = if !stripped.contains("::") {
src/declare/register.rs:1615  [ACCESSOR]  let base = wat_reader::identifier::leaf(stripped);
src/declare/register.rs:1616  [ACCESSOR]  let prefix = wat_reader::identifier::path(stripped);
src/declare/register.rs:1617  [COMPOSE]  format!(":{}::is-{}?", prefix, base)
src/resolve/registration.rs:80  [DATA]  name.contains("::")
src/resolve/registration.rs:91  [ACCESSOR]  wat_reader::identifier::leaf(name).contains('.')
src/resolve/walk.rs:367  [DATA]  if head.starts_with(':') && !head.contains("::") {
src/types/surface.rs:708  [DATA]  if !r.contains("::") || r.starts_with(":wat::") {
src/types/surface.rs:879  [DATA]  if !r.contains("::") || r.starts_with(":wat::") {
src/types.rs:633  [COMPOSE]  Some(tail) => std::borrow::Cow::Owned(format!(":wat::core::{tail}")),
src/types.rs:2447  [COMPOSE]  ":{}::{}",
src/types.rs:2448  [DATA]  schema.tag_ns.replace('.', "::"),
src/types.rs:2707  [COMPOSE]  name: format!(":wat::runtime::{}", variant),
src/types.rs:2949  [COMPOSE]  "{surface_base}::{}Response",
src/types.rs:3010  [COMPOSE]  "{surface_base}::{}Request",
src/types.rs:3201  [COMPOSE]  enum_name: format!("{}::Reply", surface.name),
src/types.rs:3224  [COMPOSE]  let op_name = format!("{}::Op", surface.name);
src/types.rs:3225  [COMPOSE]  let reply_name = format!("{}::Reply", surface.name);
src/types.rs:3276  [COMPOSE]  let carrier_name = format!("{}::surface-forms", surface_name);
src/types.rs:3333  [COMPOSE]  format!("{}::{}-MAX-REQUEST-BYTES", surface.name, name.to_uppercase());
src/types.rs:3444  [COMPOSE]  name: format!("{}::{}/Request", surf.name, op_name),
src/types.rs:3449  [COMPOSE]  name: format!("{}::{}/Response", surf.name, op_name),
src/types.rs:4912  [COMPOSE]  Some(tail) => format!("wat::core::{}", tail),
src/types.rs:5183  [COMPOSE]  (true, Some(tail)) => format!(":wat::core::{}", tail),
```

## AREA D · CHECK · RECORD · HOLON · MATCH · CLOSURE — 22 lines

```
src/check.rs:1478  [DATA]  if entry.ends_with("::") {
src/check.rs:1574  [DATA]  !s.contains("::") && !s.contains('.')
src/check.rs:7529  [COMPOSE]  expected: format!(":wat::core::{}", bare),
src/check.rs:13954  [COMPOSE]  "map-ctor key `:{key}` is not a field of {type_path}::{variant_name}"
src/check.rs:13971  [COMPOSE]  "map ctor is missing declared field `:{decl_name}` of {type_path}::{variant_name}"
src/check.rs:17091  [COMPOSE]  let want_op    = reduce(&TypeExpr::Path(format!("{}::Op", ep)),    subst, types);
src/check.rs:17092  [COMPOSE]  let want_reply = reduce(&TypeExpr::Path(format!("{}::Reply", ep)), subst, types);
src/check.rs:21463  [COMPOSE]  format!(":wat::math::{}", name),
src/check.rs:21496  [COMPOSE]  format!(":wat::stat::{}", name),
src/check.rs:21528  [COMPOSE]  format!(":wat::time::{}", name),
src/check.rs:21557  [COMPOSE]  format!(":wat::time::{}", name),
src/check.rs:21581  [COMPOSE]  format!(":wat::time::{}", name),
src/check.rs:21603  [COMPOSE]  format!(":wat::time::{}", name),
src/check.rs:21617  [COMPOSE]  format!(":wat::time::{}", name),
src/check.rs:21647  [COMPOSE]  format!(":wat::time::{}", name),
src/closure_extract.rs:634  [COMPOSE]  ":user::closure-capture::{}",
src/holon/outcome.rs:350  [COMPOSE]  "rete Fallback arm's holon mode has no route for CosineOutcome::{variant} ({} field(s)) — add one before shipping this shape",
src/holon/outcome.rs:367  [COMPOSE]  "rete Fallback arm's holon mode has no route for DotOutcome::{variant} ({} field(s)) — add one before shipping this shape",
src/match_arm.rs:202  [DATA]  if name.is_empty() || name.contains("::") {
src/record/construct.rs:310  [COMPOSE]  "map ctor has {} key(s), variant {type_path}::{variant_name} declares {}",
src/record/construct.rs:327  [COMPOSE]  "map ctor is missing declared field `:{decl_name}` of {type_path}::{variant_name}"
src/record/construct.rs:348  [COMPOSE]  "map-ctor key `:{key}` is not a field of {type_path}::{variant_name}"
```

## AREA E · LOAD · RUNTIME · IO · KERNEL · HOST · RUST-DEPS — 31 lines

```
src/host/test_runner.rs:579  [ACCESSOR]  let path = entry.path();
src/io.rs:1651  [ACCESSOR]  Some(f) => Ok(f.path().display().to_string()),
src/io.rs:1682  [ACCESSOR]  Some(d) => Ok(d.path().display().to_string()),
src/io.rs:1722  [ACCESSOR]  let s = cell.with_ref(op, |f| f.path())??;
src/io.rs:1751  [ACCESSOR]  let s = cell.with_ref(op, |d| d.path())??;
src/io.rs:1824  [ACCESSOR]  let full_path = entry.path().to_string_lossy().into_owned();
src/kernel/spawn.rs:699  [COMPOSE]  .name(format!("wat-thread-peer::{}", fn_name))
src/load/loader.rs:947  [COMPOSE]  "this load form expects a :wat::verify::{}<algo> keyword; got {}",
src/load/loader.rs:1805  [ACCESSOR]  let file_path = dir.path().join("a.wat");
src/load/loader.rs:1807  [ACCESSOR]  let loader = ScopedLoader::new(dir.path()).expect("scope");
src/load/loader.rs:1821  [ACCESSOR]  std::fs::write(dir.path().join("helper.wat"), "hi").unwrap();
src/load/loader.rs:1822  [ACCESSOR]  let loader = ScopedLoader::new(dir.path()).expect("scope");
src/load/loader.rs:1832  [ACCESSOR]  let file_path = dir.path().join("digest.txt");
src/load/loader.rs:1834  [ACCESSOR]  let loader = ScopedLoader::new(dir.path()).expect("scope");
src/load/loader.rs:1846  [ACCESSOR]  let outside_file = outside.path().join("leak.txt");
src/load/loader.rs:1848  [ACCESSOR]  let loader = ScopedLoader::new(scope.path()).expect("scope");
src/load/loader.rs:1859  [ACCESSOR]  let sub = scope.path().join("sub");
src/load/loader.rs:1882  [ACCESSOR]  let secret = outside.path().join("secret.txt");
src/load/loader.rs:1884  [ACCESSOR]  let link = scope.path().join("link");
src/load/loader.rs:1886  [ACCESSOR]  let loader = ScopedLoader::new(scope.path()).expect("scope");
src/load/loader.rs:1896  [ACCESSOR]  let loader = ScopedLoader::new(scope.path()).expect("scope");
src/load/loader.rs:1897  [ACCESSOR]  let missing = scope.path().join("missing.wat");
src/runtime.rs:3197  [COMPOSE]  &format!("{protocol_fqdn}::Op"),
src/runtime.rs:3201  [COMPOSE]  &format!("{protocol_fqdn}::Reply"),
src/runtime.rs:3261  [COMPOSE]  "{}::{}-MAX-REQUEST-BYTES",
src/runtime.rs:3265  [COMPOSE]  let rtl_ctor_kw = format!("{resp_base}::RequestTooLarge");
src/runtime.rs:3854  [COMPOSE]  "{}::{}",
src/runtime.rs:11280  [COMPOSE]  op: format!(":wat::math::{}", op_name),
src/runtime.rs:11294  [COMPOSE]  op: format!(":wat::math::{}", op_name),
src/runtime.rs:14129  [COMPOSE]  "this form expects a :wat::verify::{}<algo> keyword; got {}",
src/rust_deps/sqlite.rs:172  [COMPOSE]  format!("malformed Param::{variant} (payload {payload:?})"),
```

## AREA F · RETE AND THE TEST TREE — 29 lines

```
src/rete/clause.rs:172  [DATA]  let (ty_path, op) = core_name.rsplit_once("::")?;
src/rete/clause.rs:178  [ACCESSOR]  let ty = wat_reader::identifier::leaf(ty_path);
src/rete/clause.rs:225  [DATA]  if kw.contains("::") {
src/rete/clause.rs:324  [COMPOSE]  format!(":wat::rete::core::{ty}::{op}"),
src/rete/clause.rs:325  [COMPOSE]  format!(":wat::rete::{ty}::{op}"),
src/rete/clause.rs:365  [DATA]  n.rsplit_once("::")
src/rete/expr_ir.rs:751  [ACCESSOR]  let last = wat_reader::identifier::leaf(tag).trim_start_matches(':');
src/rete/purity.rs:2655  [ACCESSOR]  let p = entry.path();
src/rete/purity.rs:2747  [DATA]  let ns = v.rsplit_once("::").map(|(a, _)| a.to_string()).unwrap_or_else(|| v.clone());
src/rete/validate.rs:974  [ACCESSOR]  let suffix = wat_reader::identifier::leaf(op);
src/rete/validate.rs:979  [COMPOSE]  Some(ty) => format!(":wat::rete::core::{ty}::{suffix}"),
src/rete/validate.rs:982  [COMPOSE]  None => format!(":wat::rete::core::<type-of-the-operands>::{suffix}"),
src/rete/validate.rs:1333  [DATA]  if k.contains("::") {
src/rete/validate.rs:1341  [DATA]  format!("{}::{name}", ns.replace('.', "::"))
tests/lint/holon_is_vsa_only.rs:95  [ACCESSOR]  let p = e.path();
tests/lint/holon_is_vsa_only.rs:288  [DATA]  let followed_by_path = code.as_bytes().get(end..end + 2) == Some(b"::");
tests/lint/holon_is_vsa_only.rs:341  [ACCESSOR]  collect_rs(&e.path().join("src"), &mut files);
tests/lint/no_inlined_edn.rs:117  [ACCESSOR]  let p = e.path();
tests/lint/no_rc_use.rs:49  [ACCESSOR]  let p = e.path();
tests/lint/no_rc_use.rs:92  [ACCESSOR]  collect_rs(&e.path().join("src"), &mut files);
tests/lint/unused_span_justified.rs:58  [ACCESSOR]  let p = e.path();
tests/program/probe_arc170_edn_bridge_unspellable.rs:159  [ACCESSOR]  let p = e.path();
tests/reflection/probe_arc255_reflection_parity.rs:154  [COMPOSE]  Value::Enum(ev) => format!("{}::{}", ev.type_path, ev.variant_name),
tests/reflection/probe_stone_metadata_of_whole_row.rs:50  [COMPOSE]  Value::Enum(ev) => WatAST::keyword(format!("{}::{}", ev.type_path, ev.variant_name)),
tests/resolve/probe_arc251_decl_migrator.rs:33  [COMPOSE]  let full = format!(":user::{}", fn_name);
tests/resolve/probe_stone_233_2_j_producer_migration.rs:210  [ACCESSOR]  let path = entry.path();
tests/rete/probe_arc278_P6_delta_asymmetric_join.rs:64  [DATA]  let (ns, name) = ty.rsplit_once("::").expect("namespaced type");
tests/rete/probe_arc278_P6_delta_asymmetric_join.rs:65  [COMPOSE]  format!("(:{ns}::q-{name})")
tests/types/probe_stone_233_2_k_variant_retired.rs:251  [ACCESSOR]  let path = entry.path();
```

