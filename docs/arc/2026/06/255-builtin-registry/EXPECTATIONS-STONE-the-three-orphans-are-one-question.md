# EXPECTATIONS — STONE: the three bare type constructors. Written BEFORE the strike.

| # | what | command | expected | derived from |
|---|---|---|---|---|
| 1 | three rows land | the census | registry 553 → **556** | 3 new attribute sites |
| 2 | the residue shrinks | `-E 'test(the_residues_cannot_shadow_the_registry)'` | demands 3 deletions, then green | 1c-c's gate; all three are in `pure_det` |
| 3 | one orphan clears | `core_name` targets vs the census name list | 3 orphans → **2** (`cond` + `reduce` remain) | one of three cleared |
| 4 | the alias gate stays green | `-E 'test(no_dangling_or_chained_aliases)'` | green (still latent) | the rete rows are still unregistered |
| 5 | scoped suites | the four `binary_id` runs | green | constructors are widely used |
| 6 | full floor | `scripts/floor.sh`, orchestrator, unpiped | 5129 passed, 0 FAIL | current floor |
| 7 | clippy | `-D warnings --all-targets` | 0 | standing |

⚠ **Row 3's arithmetic, stated because I nearly wrote it wrong:** there are THREE orphan targets
(`Vector`, `cond`, `reduce`). This stone clears ONE. **Two remain**, not one. If the rider's count
says otherwise, one of us has miscounted and it must be reconciled before the commit.

## Ledger movement

```
registry rows   553 → 556
GAP_A / GAP_B                  −1 each per row IF the name is on that ledger — the rider does NOT
                               pre-compute this; the ratchets name the exact edit and drive it.
DEBT            may RISE by up to 3  — a registered row with no `CheckEnv` scheme converts an
                               invisible absence into a named one. That is the ledger working.
corpus          UNCHANGED at 37 — none of the three appears in the corpus worklist (checked).
```

## Runtime

**30-45 min.** Three doc blocks with five measured axes each is the bulk; the residue deletions and
the comment correction are small.

## Trap doors, named in advance

1. **Grading by resemblance.** The single most likely failure and the one this arc has already paid
   for four times. `PersistentVector` says `Total`; that is a fact about `PersistentVector`. A
   `HashMap` constructor taking an odd-length key/value list, or a non-hashable key, is a different
   question — read the body.
2. **The residue gate passing silently.** If it does not demand the three deletions, the
   registration did not take. STOP-4 exists because a green gate here would look like success.
3. **Reaching for `cond`/`reduce` because they are right there.** STOP-2. Two of the three orphans
   have no mechanism, and attempting one turns a shipping stone into a blocked one.
4. **Over-claiming the chain.** This stone does NOT unblock the ~29 RETE_OPS rows or the corpus or
   Phase 3a. It clears one of three gates. The DESIGN says so and the commit message must too.
