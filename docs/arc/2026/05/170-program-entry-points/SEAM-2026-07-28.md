# SEAM — arc 170 closure, 2026-07-28 (second curare of the day)

> ⛔ **THE SELF PAST THIS LINE IS NEW.** You did not live the session below. It is a lossy cache in
> your own voice — which is exactly why it will feel like continuing rather than waking, and that
> feeling is the failure, not the all-clear. Run the datamancy bootstrap (grimoire + the 4 primers
> from the SIGNED MCP, never a disk copy), ground HEAD against the disk, and read this whole file
> before you move.
>
> **⚠ THE TREE IS DIRTY ON PURPOSE AND A RIDER WAS LIVE AT THE GAP. Do NOT revert it. See §RIDER.**

## Where the code is

**HEAD `a65a1e11`**, branch `arc-170-gap-j-v5-deadlock-state`, pushed.
Floor at last clean weigh: **`4164 tests run: 4164 passed, 262 skipped`**, `NEXTEST_EXIT=0`, my own
`--release` re-run, Summary read ANSI-stripped.

## ⚠ RIDER LIVE AT THE GAP — ride through, do not reap

A rider was in the field building **closure #6** when this was written. Its work is **uncommitted and
intentional**:

```
 M src/check.rs  src/kernel/spawn.rs  src/process/exec_plan.rs
 M tests/kernel/spawn_program_prime_process.rs
 M wat/bracket.wat  wat/spawn.wat
?? wat-scripts/intueri/proc-identity-vocabulary.wat.intueri
```

**Do NOT revert it, do NOT commit it blind.** Weigh its report by your OWN `cargo nextest run
--release` against **4164/4164/0**, read the diff yourself, then bank. `PER HIATVM EQVITAMVS` — the
crusade crosses the gap with the field populated; reaping a live rider converts a survivable gap into
lost work. (Also: harness diagnostics on its mid-edit files are STALE-SNAPSHOT PHANTOMS —
`cargo build --release` is the arbiter. That fired repeatedly today and was a phantom every time.)

## What landed — six commits, each green by my own re-run before the next began

| commit | what |
|---|---|
| `96221840` | the closure board was lying — items 2, 3, 5 marked closed with what proved them |
| `53d8ff6d` | the retired-name lint + 108 fixes — the **checker** stopped teaching verbs a user cannot type |
| `27a22964` | the dead `SenderInner::PipeFd` send half annihilated (−131) |
| `b051b0e3` | `Thread::send` stopped reporting a silent drop as delivery |
| `357a223a` | **closure #4** — a frame stops being called a line; `fix.wat` gained `rename-symbol-exact` |
| `a65a1e11` | the board, trued |

Plus an addendum to `docs/arc/2026/04/109-kill-std/NOTE-type-annotation-names-unchecked.md` carrying
the builder's ruling: **check must do more** — scoped to the ANNOTATION position, with the
STRING-LITERAL half named as explicitly not the checker's job (that half is the lint).

## The board — `CLOSURE-BACKLOG.md` is the tracker, and it is now TRUE

**Closed: 2, 3, 4, 5.** **Open: #6 (in flight) and #1.**

**#1 `wat --repl` is THE CLOSURE CONDITION** — arc 170 closes on it by the builder's ruling, and it
is mostly assembly: `eval-with-defs!` landed at `fe77b1f5`, the loop runs today as
`wat-scripts/demos/repl/repl.wat`, and the per-mode arity refactor already opened the door
(`argv.rs:33` names `--repl` in a comment). Order is the builder's: **debt, then victory.**

## ⛔ #6 — EVERY NAME IS CAST AND RATIFIED. Do NOT re-derive them.

intueri cast (`wat-scripts/intueri/proc-identity-vocabulary.wat.intueri`), builder-ratified:

```
/usr/local/bin/wat
/usr/local/bin/wat #wat.process/Bracket {:id 0}
/usr/local/bin/wat #wat.process/Service {:name :my::app::CounterSvc}
```

- **SHAPE 2 — a CLOSED set of two substrate-owned types.** Not caller-minted tags: `ps` output must
  be a set an operator learns once and matches exhaustively. An open set means their vocabulary is
  never complete.
- **New namespace `:wat::process::`** — grounded against all three neighbours; none owns "an OS
  process's outward, caller-supplied, boot-fixed identity." Kept OUT of `:wat::program::`
  deliberately: that namespace is **child-computed** facts, and blurring that line is what stopped
  the first strike.
- **`:name` is a `:wat::core::keyword`**, not a String — builder-ruled, precedent at
  `wat/telemetry.wat:238,245`. A keyword today **is** the symbol after the Clojure-syntax flip (the
  `::`↔`.` dial); a String would need re-parsing — a redesign wearing a swap's clothes. wat has no
  symbol value type at HEAD (`edn_shim.rs:1676`).
- **The `ProcessOpts` field is `label`, not `identity`** (and `with-identity` → `with-label`). The
  reason is the wall's own sentence at `exec_plan.rs:29-35`: *"a CLAIM where this is a WITNESS."*
  `identity` reads as a claim. **The name carries the invariant.**

**The wall ships in the same strike, not after:** the label DESCRIBES, never ROUTES; fd 3
(`LIFELINE_FD`) stays the sole gate; a gate must prove a forged label at a shell accomplishes
nothing, that a spawned child still sees empty `argv` with a label present, and **that the gate can
go red**.

**Already grounded, do not re-derive:** the label CANNOT leak into `(:wat::runtime::argv)` —
`spawned_runtime.rs:36` does `set_argv(Vec::new())` unconditionally and never reads OS argv.

## The lessons this session paid for

**★ THE SHAPE OF THE WHOLE DAY — an arm nothing reaches accumulates lies.** Four times, the "flaw" I
chased was a **defensive arm for a state nothing produces**: `typed_send`'s `PipeFd` arm (its only
caller passes `Comms`), `ConnectOutcome::Stopped` (crossbeam send cannot be stopped),
`Thread::send`'s drained arm (`close` takes the peer from the cell *before* draining), and the two
`Ok(_)` sender guards the purge surfaced. Each carried a comment or a fabricated reason that read
exactly like live behaviour, and each pulled me into designing against it. 24p named the mirror —
*walls need traffic or they stop being walls.* This is the inverse: **nothing ever contradicts an
unreachable arm, so its lies compound undisturbed.**

**★ THE SEAM HANDED ME A DECISION THAT DID NOT EXIST.** I carried `SendOutcome::Shutdown` →
`ConnectFail` → `ConnectOutcome` into the seam, the closure board, and six rounds with the builder.
It dissolved under grounding: the path cannot fire. I ran the four questions on it once and returned
**Honest = YES on a premise I had not checked**; the builder ratified it, and it was false. **Ground
the decision itself before posing it** — a fork is a claim like any other.

**★ A GREP THAT CANNOT REACH THE THING IS NOT EVIDENCE — five times, including while checking for
it.** The symbol-head verification defeated by a `[` before the `(`; three `StdIn/read-line` I flagged
that were a codemod naming its own subject; a `:Line` pattern that cannot tell a standalone keyword
from an FQDN tail. **Both riders were right and my verification was the weak link.** Check that your
pattern COULD have matched before reading silence as absence.

**★ TWO STOPS PAID FOR THEMSELVES.** A rider stopped rather than hand-edit two symbol heads to a
green — surfacing that `fix.wat`'s rename walk **silently skipped** bare symbols, a codemod reporting
success having done nothing. Another stopped rather than improvise past a design premise —
`user.program` crosses as a **recipe the child cooks**, never a value the parent holds. Both would
have been cheaper to paper over; both would have shipped something false.

**★ A WARD'S VERDICT IS A HYPOTHESIS.** The intueri cast was right on shape, namespace, types and
carrier — and **caught a factual error in my own target file** (I mis-attributed `PeerKind` to
`:wat::spawn::`). But its argument for `label` rested on a competing identity vocabulary in
`wat/capability.wat` that **does not exist** — zero hits. Weigh both directions.

**★ `.wat` MESSAGE STRINGS HAVE NO WALL.** Seven stale ones were swept by hand in #4, including the
header of the new primitive itself. `tests/lint/retired_name_justified.rs` covers `src/**/*.rs` only.

## Method that held

Study the lair before the strike: #4's scope went 22 files → 5 because `:wat::io::IOReader/read-line`
is a **different, correctly-named verb** — one glob would have renamed the honest one. Enumerate
extensions, never one glob. Names are **cast** (intueri), never narrated — and when the board offers
an illustration, that is not a cast. Dry-run a codemod on `/tmp` + diff + prove idempotency BEFORE
the corpus. Weigh by your OWN `--release` re-run; a rider's report is not a weigh.

---

> **SEAM.** You are NEW. HEAD `a65a1e11`, floor 4164/4164/0, six commits pushed — and **the tree is
> DIRTY with a live rider's #6 work that you must NOT revert.** Weigh it, then bank it. Every #6 name
> is already cast and ratified above; re-deriving them is wasted work. After #6, only **#1
> `wat --repl`** remains, and arc 170 closes on it.
>
> It bears repeating because it cost this session most of a day: **an arm nothing reaches accumulates
> lies — before you design against a defect, prove something can reach it**; **ground a decision
> before you pose it, or you will get a false premise ratified**; and **a grep that cannot match is
> not evidence of absence.**
>
> Do not trust this note over the disk.
