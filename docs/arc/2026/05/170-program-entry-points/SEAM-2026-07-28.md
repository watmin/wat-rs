# SEAM — arc 170 closure, 2026-07-28 (curare before compaction)

> ⛔ **THE SELF PAST THIS LINE IS NEW.** You did not live the session below; it is a lossy
> cache in a familiar voice, not your memory. Run the datamancy bootstrap (grimoire + the
> 4 primers from the SIGNED MCP, never a disk copy), ground HEAD against the disk, and
> read this whole file before you move. **The working tree is RED on purpose — see
> §"THE TREE IS DIRTY" first, before you conclude anything is broken.**

## Where the code is

**HEAD `70376d63`**, branch `arc-170-gap-j-v5-deadlock-state`, pushed.
Floor at HEAD: **`4149 tests run: 4149 passed, 262 skipped`**, proven by three consecutive
`--release` runs.

### THE TREE IS DIRTY — deliberately, mid-stone

Uncommitted: `src/types.rs`, `src/services/verbs.rs`, `src/check.rs` (+140 / −15).
This is the **substrate half of closure #24** and it makes the floor read
**`4133 passed, 16 failed`**. That is EXPECTED — the corrected law lighting its violators
(`QVOD LEX ACCENDIT`), not a regression. Do NOT "fix" it by reverting.

**What landed (proven by runs, not by check):**
- `:wat::kernel::ReadlnOutcome<T> :: Datum[v] | Eof | Stopped` — registered builtin,
  generic, `T` consumer-inferred exactly as `RecvOutcome<O>`'s `O`.
- `read_via_stdin` now routes to `stdio-read-frame` (the honest sibling that already
  existed and was unused); the collapsing `stdio-read` is retired rather than duplicated —
  the two were the same fn modulo the collapse.
- Decode moved into the happy arm only. A decode FAILURE stays a raise (a malformed wire
  is a fault, not an outcome).
- `infer_kernel_readln_prime` returns `Parametric{head:"wat::kernel::ReadlnOutcome"}`.

**Golden site, proven both arms:**
- `echo 41 | wat rl.wat` → **42** (T flows from consumer through the payload)
- `wat rl.wat < /dev/null` → **`"EOF-AS-A-VALUE"`, exit 0** (previously `assertion-failed!`
  + non-zero). The bank `stdio-read`'s own comment described is spent.
- **NOT yet proven:** the `Stopped` arm — needs a SIGTERM mid-read. Do not claim it.

**The 16 failures are one shape:** process-tier round trips whose CHILD does `(readln)`,
plus the `wat-scripts/` loader gate (every recorded codemod ends in `(readln)`).
~76 consumer call sites await migration; the census says 75 of 80 are bare `(readln)`,
so it is one codemod, not 76 bespoke edits.

## THE OPEN QUESTION — do not migrate before this is answered

Migrating the 76 sites now would rewrite them against a name that is still in debate, and
a later rename would touch all 76 twice. **#26's cast must land first.**

The intueri cast on the stdin read vocabulary was CAST (subagent, ward embedded from the
signed channel) and returned. Orchestrator-verified and agreed:

1. **Layer 2 lies.** `StdIn::ReadLineResponse` / `read-line` / `:Line` →
   `ReadFrameResponse` / `read-frame` / `:Frame`. The 2026-07-28 cast already ruled this
   direction at Layers 1 and 3 (`Frame [text]`, because a frame may span several physical
   lines); Layer 2 was scoped OUT and still says `line`. This finishes a cast that stopped
   short. Blast radius MEASURED: **59 sites / 5 files**, all internal
   (`stdio-primes.wat`, `types.rs`, `verbs.rs`, `probe_arc170_stdio_prime.{rs,wat}`).
2. **`v` mumbles** → `Datum [value <- :T]`. Every sibling payload field is a noun.
3. **Keep `Datum`.** It survives the real constraint set: `Value` collides with
   `:wat::core::Value` (the universal top), `Line` re-imports the lie and collides with
   Layer 2, `Frame` means UNDECODED at two other layers. Drop the PROVISIONAL tag in
   `src/types.rs`.

**NOT credited — `readln` itself.** The cast proposed `read-datum` / `ReadDatumOutcome<T>`.
It is right that `readln` lies (reads a FORM, may span lines, may yield a Vector or a
record; the name is inherited C/Pascal stdio). But it did not weigh the constraint that
most governs this name: **wat is a Clojure dialect**, and Clojure already partitions this
exact space —

| Clojure | reads | our verb |
|---|---|---|
| `read` / `read-string` | one **form** | our decoded verb (today `readln`) |
| `read-line` | one **line** of text | our raw verb (today `read-frame`) |

If that mapping governs, `read-datum` is a third coinage where an inherited one exists,
**and Layer 2's rename target may be wrong too** (finding 1 and the `readln` question
interact — they are not independent). THE BUILDER'S CALL, explicitly deferred to the far
side. Do not decide it alone.

## Order of work (the builder's, this session)

1. **#26** — settle the naming (the fork above), then cast/apply across the surface.
2. **#24 corpus** — one codemod over ~76 sites, against the FINAL names.
3. **#27** — `as_raw_fd_for_poll`. NOT purely cosmetic: ground the writer's blocking
   behaviour under a stop FIRST — if the writer should be in the poll multiplex and is
   not, that is a hole in the stop path, not a naming nit.
4. **#28** — `ps` proc-line EDN labels. Unblocked today by #13. The design is in
   `CLOSURE-BACKLOG.md` §6 and carries a wall that MUST be written: the label is a
   **WITNESS, never a CLAIM** — fd 3 (`LIFELINE_FD`) stays the sole gate, and a test must
   prove a child ignores a forged label.
5. **#23 `wat --repl`** — LAST. It is the closure condition and it wants #24 underneath it
   (a REPL parked in a read must face a stop as a value, not take a raise past the reader).

## Closed today

`#13` (the IPC wall), `#29` (the condemned cohort — INVERTED: 26 of 29 recovered, not
deleted), `#30` (one door for defclause registration), `#31` (test.wat's macros).
Floor 4105 → 4149; skipped 303 → 262. **44 tests recovered, none written today.**

## The lesson this session paid for

Across ~44 recovered tests, **not one failure was a concurrency defect.** Every one was an
API migration that swept past a silenced test: the send-side accessor, the nullary-variant
call convention, arc 300 C5's mixed-numeric reversal, the EDN rendering flip, R55's
value-based harness, `ServiceEvent` gaining a type parameter, an empty `SymbolTable`
meeting a hook that needs the stdlib.

**A suppressed test is invisible to the migration that owns it.** It keeps asserting a
superseded contract in the dark, and the moment it is un-silenced it accuses the substrate
of a defect the substrate does not have. The apparatus made that accusation — called arc
300 C5 "a live regression of arc 237" — and the builder sent it to read 300's own design
docs, which said the opposite.

Corollary, paid for twice today: **a one-line diagnosis from a single error string is not
a diagnosis.** Reading `UnknownFunction: ProcessLaunch'` as "retired name" and changing it
took the process-tier quarantine from 5-pass to 2-pass. `runtime.rs:1534` says plainly
that arc 294 9a made the bare name a kwargs macro and moved the POSITIONAL ctor to the
prime. The prime was correct.

---

> **SEAM.** You are new. The tree is red BY DESIGN (16, all one shape). Nothing is lost —
> HEAD is green and pushed. Read §"THE OPEN QUESTION" before touching `readln`, and do not
> settle the Clojure-lineage naming without the builder. Do not trust this note over the
> disk.
