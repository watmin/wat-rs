# Arc 175 — Enum definition syntax (Clojure-flavored revision)

**Status:** stub opened 2026-05-12 per user direction.

## Motivation

> *"revise enum definitions syntax — make it more clojure-y"*

Current enum surface is too verbose / not Clojure-idiomatic.
This arc reshapes the declaration form to feel like Clojure's
defrecord / deftype family. User-variant construction +
matching surface stays compatible; the DECLARATION syntax
changes.

## Sketch (placeholder; user fills the design)

TBD.

## Cross-references

- arc 048 (user enum + match construction)
- arc 098 (pattern grammar)
- arc 176 (parallel struct syntax revision)
- arc 188 (Option variants FQDN — variant-namespace doctrine)
- arc 190 (Result variants FQDN)
- arc 191 (Option/Result method forms — Type/verb shape)
