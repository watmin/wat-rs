[package]
name = "console-demo"
version = "0.1.0"
edition = "2021"
publish = false
description = "Walkable demo of the ambient-stdio API. Emits five domain events as EDN-encoded stdout lines via :wat::kernel::println; stderr stays empty (eprintln is wat's PANIC channel). Arc 170 slice 1f-η — Console driver retired."

[[bin]]
name = "console-demo"
path = "src/main.rs"

[dependencies]
wat = { path = "../.." }

# Inherit the workspace clippy wall (root [workspace.lints.clippy]): a finding is a
# FAILURE, not advice. NB `[lints]` is a TOP-LEVEL table — writing `lints.workspace`
# inside [package] parses as `package.lints` and cargo silently discards it.
[lints]
workspace = true
